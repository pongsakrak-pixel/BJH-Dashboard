# STATE — BJH Revenue Dashboard
อัปเดต: 2 ส.ค. 2569 · เวอร์ชันล่าสุด **V636.0**

---

## กติกาการส่งงาน
ส่ง **ไฟล์เดียวต่อรอบ** = `patch_Vxxx.ps1` · guard รับหลาย minor เพราะ patch เขียนไฟล์ทั้งไฟล์

---

## รอบนี้ (V636.0) — หน้าสร้างคำขอ จบในหน้าเดียว

**เดิม:** `wrap` 1180px กลางจอ · การ์ด 4 ใบเรียงลงล่าง → ต้องเลื่อน 2-3 หน้าจอกว่าจะถึงปุ่มส่ง
**ใหม่:** เต็มความกว้าง 1680px · 3 คอลัมน์บน + เครื่องเต็มความกว้างล่าง

```
┌── 1 ข้อมูลโครงการ ──┬── 2 เงื่อนไขบริการ ──┬── 3 ไฟล์แนบ & หมายเหตุ ──┐
└─────────────────────┴──────────────────────┴──────────────────────────┘
┌── 4 เครื่องที่ขอราคา (เต็มความกว้าง · เลื่อนในกล่องได้ถ้าเกิน 210px) ──┐
└────────────────────────────────────────────────────────────────────────┘
                                              [ ดูร่างเอกสารก่อนส่ง → ]
```

- **`.wrap.wide` สลับเฉพาะหน้าสร้างคำขอ** หน้าอื่นคงความกว้างเดิม (`go()` toggle ให้)
- เลขหัวข้อเรียงใหม่ 1-4 ตามที่ตาเห็นจริง (เดิมเครื่องเป็นข้อ 2 แต่อยู่กลาง)
- การ์ด padding 12px · field ชิดขึ้น · dropzone เตี้ยลง — บีบให้พอดีจอ
- รายการเครื่อง `max-height:210px` + scroll ในกล่อง — ใส่ 10 รุ่นก็ไม่ดันปุ่มตกจอ
- Responsive: <1200px → 2 คอลัมน์ · <820px → 1 คอลัมน์

---

## ทดสอบ
- `node --check` ผ่าน
- jsdom **43/43 ผ่าน**
  - `.wrap.wide` ติดเฉพาะหน้า request · ไปหน้าอื่นถอดออก · กลับมาติดใหม่
  - การ์ดบน 3 ใบใน `.rq-top` · การ์ดเครื่องอยู่นอก (เต็มกว้าง) · อยู่ล่างสุดก่อนปุ่ม
  - ช่องกรอกครบ 14 ตัว (`r_project2` … `r_draftbtn`)
  - เลข sec-num เรียง 1,2,3,4 ไม่ข้าม
  - เพิ่ม/ลบแถวเครื่อง · datalist · ปีประกัน/ปีรวม · แถบสรุปปี · validate · ไปหน้าร่างนับ 2 เครื่องถูก
  - หน้า verify / config ไม่ถูกกระทบ · แท็บ Template ยังซ่อน (V635)

---

## วิธีลง
```powershell
cd C:\bjh-dashboard
& .\patch_V636.ps1
```
guard: รับ V635.x · ไม่แตะ Code.js → ไม่ต้อง Deploy New Version

---

## 🔴 งานใหญ่ที่ตกลงไว้แล้ว ยังไม่ได้ทำ — แยกราคารายเครื่อง

**Eak สั่งไว้:** แต่ละเครื่องคิดราคาแยก ไม่รวมยอด · เอกสารออกเป็น **ข้อ 7, 8, 9…** ตามจำนวนเครื่อง
แต่ละข้อมี ยี่ห้อ + รุ่น + ตารางราคาของตัวเอง

**โครงจริงของกลุ่มราคา** (ตรวจจาก `SalesProspect.html` แล้ว)
```
row.plain          → ไม่รวมอะไหล่
row.full           → รวมอะไหล่ (ทุกชิ้น)
row.excl[i]        → { type, label, parts[], price }   ← มีได้หลายชุด
     type==='exclude_except' → "ไม่รวมอะไหล่ยกเว้น <label>"
     อื่นๆ                    → "รวมอะไหล่ยกเว้น <label>"
```
`_PT_TABS = [['plain','ไม่รวมอะไหล่'],['excl','รวมอะไหล่ (ยกเว้น)'],['full','รวมอะไหล่']]`

**บั๊กที่ค้างอยู่ตอนนี้**
1. MA ผูกตายไว้ 3 คอลัมน์ (`none/partial/full`) และหยิบแค่ `excl[0]` → ชุดยกเว้นที่ 2, 3 หายเงียบ
2. หัวคอลัมน์ไม่ตรง — "รวมอะไหล่ยกเว้น" อาจเป็น "**ไม่**รวมอะไหล่ยกเว้น" ขึ้นกับ `type`
3. `รวมทุกชิ้น = 0` ในใบทดสอบ — **ยังไม่ยืนยันสาเหตุ** ต้องรันสคริปต์ดู `_PRICE_GROUPS` ของกลุ่ม Dimensions 3D
4. `parts[]` คือรายการยกเว้นที่ควรไหลเข้าเอกสารอัตโนมัติ — ตอนนี้ต้องพิมพ์เอง

**ที่ต้องรื้อ**
| | |
|---|---|
| ชีท `Prices` | เพิ่ม `row_no` (ราคาของเครื่องไหน) · `type` เปลี่ยนเป็น `plain`/`full`/`excl0`/`excl1` |
| หน้า Manager | ตารางราคาแยกทีละเครื่อง · คอลัมน์ตามกลุ่มราคาของรุ่นนั้น · ตัดช่อง "รายการยกเว้น" (ดึงจาก config) |
| PDF | วนออกทีละเครื่องเป็นข้อ 7, 8, 9 พร้อมรายการยกเว้นของแต่ละแบบ |

**ยังค้างคำตอบ 2 ข้อ**
- เครื่องรุ่นเดียวกัน qty>1 → ก) ข้อเดียวยอดรวม  ข) ข้อเดียวโชว์ราคาต่อเครื่อง ×N  ค) แยกคนละข้อ
- ใบเก่าที่ออกแล้ว (เก็บ `none/partial/full` ไม่มี `row_no`) → ตั้งใจให้เปิดดูแบบยอดรวมเหมือนเดิม ไม่แปลงย้อนหลัง

---

## ค้างอยู่ (MA)
- ⬜ ยังไม่ทดสอบเส้นทาง "ส่ง Manager" กับใบจริง
- ⬜ ยังไม่ตรวจข้อความ default ในแท็บ Config (เปิด Config มาเจอแท็บนี้เป็นอันแรกแล้ว)
- ⬜ ยังไม่ได้ดู PDF ทั้งใบหลัง V633
- ไฟล์แนบยังเป็น UI เปล่า — `attach_urls` ส่ง `[]`
- ยังไม่ล็อกสิทธิ์แยก Admin/Manager
- ยังไม่มีปุ่ม "สร้างคำขอใหม่จากใบนี้"

## ค้างอยู่ (อื่นๆ)
- แท็บ 4 อะไหล่: UI ตาม mockup v5 · ปุ่มรายงาน 1 หน้า A4 · **Battery ชื่อซ้ำ** (Yuasa 30 / ไม่ระบุยี่ห้อ 15)
- Performance: sheets 4.7s + ETL 3.9s
- 6 mobile menus · Renewal.html · M3 SR (blocked) · M8 mobile black screen
- Hardware List ฝั่ง SP · ชิป Sale Order ถ้าขึ้น (0) → ไล่ `_spPartsInfo(qn).hasBill`
- `_PB_SVC_RX` มี 2 ชุด ควรยุบ

## คำขอถึง SmartFlow
1. ใบเสนอราคา — เพิ่ม `BRAND` / `MODEL` / `SN`
2. RAW_CONTRACT สัญญาเหมา — ระบุ SN เครื่องที่คลุม

## ดองไว้ตั้งใจ — ห้ามเสนอซ้ำ
- V610 "อะไหล่รอในคลัง" + "ติดตามสัญญา"
- % โอกาสเสียรายปี — ข้อมูลมีแค่ 2.6 ปี
- ใบเสนอราคาเข้าหน้าราคาอ้างอิง — รอ SmartFlow เพิ่มฟิลด์
- Template 5 Level — เลิกใช้ (V635) ซ่อนไว้ ไม่ได้ลบ

---

## เกร็ดที่ได้รอบนี้
- **"เต็มหน้า" ไม่ใช่แค่ขยาย max-width** — ต้องจัดคอลัมน์ให้ของที่กรอกพร้อมกันอยู่แถวเดียวกัน ไม่งั้นกว้างขึ้นแต่ยังต้องเลื่อนเท่าเดิม
- **ส่วนที่ยาวไม่จำกัด (รายการเครื่อง) ต้องมี max-height + scroll ในกล่อง** ไม่งั้นใส่ 10 รุ่นแล้วปุ่มส่งตกจอ
- **สลับ layout ต้องผูกกับหน้า ไม่ใช่ใส่ถาวร** — `.wrap.wide` toggle ใน `go()` หน้าอื่นจึงไม่โดนลาก
