# STATE — BJH Revenue Dashboard
อัปเดต: 31 ก.ค. 2569 · เวอร์ชันล่าสุด **V628.0**

---

## กติกาการส่งงาน
ส่ง **ไฟล์เดียวต่อรอบ** = `patch_Vxxx.ps1` (ฝัง .html gzip+base64 · verify SHA · เขียน STATE.md ลง repo · push เอง)
ห้ามแนบ .html หรือ STATE.md แยก

---

## รอบนี้ทำอะไร (V628.0) — หน้าราคาอ้างอิง · `SalesProspect.html`

**เคส:** V627 ตั้งเกณฑ์ซ้ำ ≥4 แล้วช่องว่างเยอะเกินไป · Eak ถามว่า "เอา 3 ถ้าไม่มีก็ 2 แล้วลง 1"

**ข้อสังเกตที่ทำให้เปลี่ยนวิธี:** ตัวเลขที่โชว์คือ **mode (ราคาที่ซ้ำบ่อยสุด)** ซึ่งมีค่าเดียวต่อช่องอยู่แล้ว
ซ้ำ 5 ครั้งหรือ 1 ครั้ง เลขก็ตัวเดิม → การไล่เกณฑ์ 3→2→1 ไม่ได้ทำให้เลขแม่นขึ้น แค่ทำให้คนหาข้อมูลไม่เจอ

### วิธีที่ใช้แทน: โชว์ทุกช่องที่มีข้อมูล ให้ความเข้มของสีบอกความน่าเชื่อถือ

| ซ้ำ | สีเลข | ความหมาย |
|---|---|---|
| ≥ 3 ใบ | ขาวเข้ม | เป็นราคามาตรฐาน |
| 2 ใบ | เทา | พอใช้อ้างอิง ระวัง |
| 1 ใบ | เทาจาง | ยังไม่ใช่ราคามาตรฐาน |
| ไม่มีเลย | `—` | ไม่เคยมีสัญญาประเภทนี้สำหรับรุ่นนี้ |

- ไม่ใช้ ⚠ ไม่เพิ่มคอลัมน์ — บรรทัด "ซ้ำ X/Y สัญญา" บอกอยู่แล้ว แค่ทำให้ตาเห็นก่อนอ่าน
- `—` กลับมามีความหมายเดียว = ไม่มีข้อมูลจริง (เดิมกำกวม อาจแปลว่า "มีแต่ไม่ผ่านเกณฑ์")

### แยกค่าคงที่ 2 ตัว
```js
var _PB_MIN_MODE = 1;   // ขั้นต่ำที่จะแสดง
var _PB_STRONG   = 3;   // ซ้ำถึงเท่านี้ = ถือเป็นราคามาตรฐาน (สีเข้ม + นับในการ์ด)
function _pbTone(n)     // คืน [สีเลขหลัก, สีบรรทัดรอง]
```
การ์ด KPI ทั้ง SC และ SP นับด้วย `_PB_STRONG` ไม่ใช่ `_PB_MIN_MODE` — ไม่งั้นจะนับทุกแถวแล้วการ์ดไร้ความหมาย

---

## ทดสอบ
- `node --check` ผ่านทุก block
- vm **29/29 ผ่าน** — โค้ดจริงจากไฟล์ที่ patch แล้ว
  - ซ้ำ 6/3/2/1 โชว์ราคาครบทุกกรณี · ไม่มีข้อมูล → `—`
  - 3 ระดับสีต่างกันจริง · ขอบเกณฑ์ที่ 3 ได้สีเข้ม
  - MIN เหลือใช้ 2 ที่ = ประตูแสดงผล (SC cell / SP row) ไม่ใช่เกณฑ์คุณภาพ
  - คำอธิบายใต้ตารางตรงกับพฤติกรรมจริง ไม่เหลือคำว่า "ซ้ำไม่เกิน 3 ใบ"
  - ของ V627 ไม่ถูกรื้อ: top brands · ตัดงานบริการ · รวมใบยกเลิก · ไม่มี P25–P75 กลับมา
- diff SalesProspect 30 บรรทัด · overrides 4 · บรรทัดสุดท้ายเหมือนเดิม

---

## วิธีลง
```powershell
cd C:\bjh-dashboard
& .\patch_V628.ps1
```
guard: ฐานต้องเป็น V627.0 · SHA ไม่ตรง = revert อัตโนมัติ
ไม่แตะ Code.gs · ไม่แตะ classify → ไม่ต้องกด Load Data สด

---

## เรื่องที่ปิดจบแล้ว — ห้ามรื้อ

**ใบเสนอราคาเข้าหน้าราคาไม่ได้** (ตรวจกับข้อมูลจริง 31 ก.ค. 69)
`_jsupQuo` 5,830 ใบ มีฟิลด์ที่มีค่าจริงแค่:
`DOC_NUMBER · CUSTOMER_NAME · STATUS_NAME · CONTRACT_TYPE_NAME · TEAM_NAME · QH_TYPE · REQ_DATETIME · SUBJECT_EMAIL · QH_TOTAL_AMT · QH_TOTAL_DISCOUNT · CONTRACT_TYPE_TYPE_NAME · QH_CONTRACT_START_DATE · QH_CONTRACT_END_DATE · SALES_SERVICE_NAME · NOTE_LOG`

**ไม่มี BRAND · MODEL · SN เลยสักใบ** → หน้านี้จัดแถวด้วยยี่ห้อ+รุ่น จึงลงแถวไหนไม่ได้
(ยอดเงินมี 95% · เป็นสัญญา 1 ปี 4,900/5,626 ใบ = 87% ข้อมูลส่วนอื่นพร้อมหมด ติดแค่ไม่รู้ว่าเป็นเครื่องรุ่นอะไร)

ทางอ้อมที่ตันแล้ว: `DOC_NUMBER → RAW_CONTRACT.QUOTATION_NO` ได้เฉพาะใบที่กลายเป็นสัญญา ซึ่งมีในตารางอยู่แล้ว · เดารุ่นจาก `SUBJECT_EMAIL` เป็น free text เดาผิดราคาไปลงผิดรุ่น

**→ ต้องขอ SmartFlow เพิ่ม `BRAND` / `MODEL` (หรือ `SN`) ใน export ใบเสนอราคา** เพิ่มมาแล้วต่อได้ทันที

---

## คำขอถึง SmartFlow (รวมชุดเดียว)
1. ใบเสนอราคา — เพิ่ม `BRAND` / `MODEL` / `SN`
2. RAW_CONTRACT สัญญาเหมา — ระบุ SN เครื่องที่คลุม (ตอนนี้ผูกได้ทาง CON_NO อย่างเดียว)

---

## ค้างอยู่
**แท็บ 4 อะไหล่ (V626)**
- **ชื่ออะไหล่ซ้ำ** — `Battery 12V/9AH Yuasa (NPW53-12)` 30 เครื่อง กับ `Battery 12V/9Ah` 15 เครื่อง เป็นชิ้นเดียวกัน สถิติโดนผ่าครึ่ง ต้องมี Config กลุ่มอะไหล่ให้คนจับคู่
- UI ตาม mockup v5 (Top 10 + แถบ % + รายงาน 1 หน้า A4) ยังไม่ทำ
- ปุ่ม "สร้างรายงาน 1 หน้า ส่งลูกค้า" (print/PDF) ยังไม่ทำ

**อื่นๆ**
- Performance: sheets 4.7s + ETL 3.9s
- 6 mobile menus · Renewal.html · M3 SR (blocked) · M8 mobile black screen
- Hardware List ฝั่ง SP (`renderSPDetail`)
- ชิป Sale Order ถ้าขึ้น `(0)` → ไล่ `_spPartsInfo(qn).hasBill`
- `_PB_SVC_RX` (ตัดงานบริการ) ตอนนี้มี 2 ชุดคนละที่ = แท็บ 4 กับหน้าราคา ควรยุบเป็นตัวเดียว

## ดองไว้ตั้งใจ — ห้ามเสนอซ้ำ
- V610 "อะไหล่รอในคลัง" + "ติดตามสัญญา" — รอ Weekly Report ใช้จริง 2-3 สัปดาห์
- **% โอกาสเสียรายปี** — ข้อมูลมีแค่ 2.6 ปี ซอยรายปีเหลือ 1 เหตุการณ์/ช่อง · รอครบ 5+ ปี
- **ใบเสนอราคาเข้าหน้าราคา** — รอ SmartFlow เพิ่มฟิลด์ (เหตุผลด้านบน)

---

## เกร็ดที่ได้รอบนี้
- **ก่อนปรับเกณฑ์ ให้ถามก่อนว่าเกณฑ์นั้นเปลี่ยนอะไร** — ที่นี่การซ่อน/แสดงไม่ได้เปลี่ยนค่าที่คำนวณเลย เพราะ mode มีค่าเดียวต่อช่อง · การซ่อนของอ่อนจึงเป็นการซ่อนข้อมูล ไม่ใช่การเพิ่มความแม่น
- **"ไม่มีข้อมูล" กับ "มีแต่ไม่ผ่านเกณฑ์" ต้องหน้าตาต่างกัน** ไม่งั้น `—` กำกวมและคนไปหาข้อมูลผิดที่
- **แยกค่าคงที่ตามหน้าที่** — "ขั้นต่ำที่จะแสดง" กับ "ขั้นที่ถือว่าน่าเชื่อถือ" คนละเรื่องกัน ใช้ตัวเดียวคุมทั้งคู่แล้วการ์ด KPI พังเงียบ
- **วัดข้อมูลก่อนออกแบบเสมอ** — เสียเวลาไป 2 รอบกับใบเสนอราคา (คิดสูตร คิด UI ปุ่ม 3 ตัว) กว่าจะรู้ว่าไม่มีฟิลด์ยี่ห้อ · ครั้งหน้า dump ฟิลด์ที่มีค่าจริงก่อนคุยเรื่อง logic
