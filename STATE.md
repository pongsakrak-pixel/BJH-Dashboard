# STATE — BJH Revenue Dashboard
อัปเดต: 1 ส.ค. 2569 · เวอร์ชันล่าสุด **V634.0**

> **ลง V633 หรือยัง?** repo ตอนเช็คยังเป็น V632.0 — ถ้ายังไม่ได้ลง `patch_V633.ps1` (PDF ข้อ 1 เป็นรายการข้อความ) ให้ลงตัวนั้นก่อน แล้วค่อยลง V634

---

## กติกาการส่งงาน
ส่ง **ไฟล์เดียวต่อรอบ** = `patch_Vxxx.ps1` · guard รับหลาย minor (`-like 'V63x*'`) เพราะ patch เขียนไฟล์ทั้งไฟล์

---

## รอบนี้ (V634.0) — ใบที่ออกเอกสารแล้วต้องแก้ไม่ได้

**ปัญหา:** `status='auto'` / `'approved'` = PDF ถึงมือลูกค้าแล้ว
แต่กด "ดู" ในรายการยังเข้าหน้าตรวจที่แก้ราคา/ข้อความได้ และกด Approve ซ้ำได้
→ **เอกสารที่ลูกค้าถืออยู่กับข้อมูลในระบบไม่ตรงกัน ตรวจย้อนไม่ได้ว่าใครแก้อะไรหลังออกเอกสาร**

**แก้ — ล็อก 3 ชั้น**
1. **UI** — `#v_detail` ติดคลาส `.locked` · ทุก input/select/textarea ใส่ `disabled` · ชิป `pointer-events:none`
2. **ปุ่ม** — ซ่อน "บันทึก" · "อนุมัติ & สร้าง PDF" · "↓ ดึงจาก Template" (เหลือแค่ "ดูตัวอย่าง PDF")
3. **ฟังก์ชัน** — `saveVerify()` และ `approve()` เช็ค `vIsIssued(vCur)` แล้ว return ทันที
   (กันกรณีเรียกผ่าน console หรือ DOM ถูกแก้)

**แถบบนหน้า**
```
🔒 ออกเอกสารแล้ว — แก้ไขไม่ได้
   ออกอัตโนมัติ (ราคามาตรฐาน) · 2026-08-01 22:21 · a@b.c
   ต้องแก้ไข → สร้างคำขอใหม่ เพื่อให้ได้เลขที่เอกสารใหม่และตรวจย้อนได้     [⬇ ดาวน์โหลด PDF]
```
ถ้า PDF สร้างไม่สำเร็จ → ขึ้นข้อความแทนปุ่ม ไม่มีลิงก์เปล่า

**สถานะที่ยังแก้ได้:** `pending` · `verifying` (Manager กดบันทึกไว้แต่ยังไม่อนุมัติ)

---

## ทดสอบ
- `node --check` ผ่าน
- jsdom **32/32 ผ่าน**
  - auto → ล็อก · 24 ช่อง disabled ครบ · ปุ่มบันทึก/อนุมัติ/Template ซ่อน · มีปุ่ม PDF
  - เรียก `saveVerify()` / `approve()` ตรงๆ → ถูกบล็อก + ขึ้นข้อความ
  - approved → ล็อกเหมือนกัน แต่เขียนว่า "อนุมัติแล้ว"
  - ออกแล้วแต่ PDF ล้มเหลว → ยังล็อก + บอกให้ไปดูหน้าสถานะ
  - pending / verifying → แก้ได้ตามปกติ ไม่โดน guard
  - **สลับไปมาไม่ค้างสถานะ** (เปิดใบล็อก → เปิดใบปกติ → ปลดล็อกถูก → กลับไปล็อกได้อีก)
- diff 51 บรรทัด · บรรทัดสุดท้ายเหมือนเดิม

---

## วิธีลง
```powershell
cd C:\bjh-dashboard
& .\patch_V634.ps1
```
guard: รับ V632.x หรือ V633.x · **ไม่แตะ Code.js** → ไม่ต้อง Deploy New Version

---

## Flow MA (ยืนยันแล้ว)
```
Admin คีย์  →  ดูร่าง  →  ระบบตัดสิน
                        ├── ราคามาตรฐานครบ + ไม่ขอลด → ออก PDF เลย (status='auto')  🔒 ล็อกทันที
                        └── มีรุ่นไม่มีราคา / ขอส่วนลด → Manager → Approve → PDF     🔒 ล็อกหลังอนุมัติ
แก้ใบที่ออกแล้วไม่ได้ → สร้างคำขอใหม่ (ได้เลขที่ใหม่ ตรวจย้อนได้)
```

### Config 3 แท็บ — ยังต้องมีทั้งหมด
| แท็บ | ใช้ทำอะไร |
|---|---|
| **ข้อความเอกสาร (Default)** | **สำคัญสุด** — ไม้บรรทัดวัดว่าอะไรคือ "มาตรฐาน" (`vFillClauses` เทียบกับค่านี้) · เก็บหัวเอกสาร โลโก้ เพดานปี ตัวเลือกปีประกัน รายการยกเว้น |
| ราคา Template 5 Level | สำรอง — ปุ่ม "↓ ดึงจาก Template" ตอนเจอรุ่นที่ไม่มีในกลุ่มราคา |
| ฐานข้อมูลรุ่นเครื่อง | เติม `spec_pulled` ลงชีท Machines · dropdown ยี่ห้อ/รุ่นดึงจากกลุ่มราคาแล้ว |

---

## ค้างอยู่ (MA)
- ⬜ ยังไม่ทดสอบเส้นทาง "ส่ง Manager" กับใบจริง (รุ่นไม่มีราคา / ขอส่วนลด)
- ⬜ ยังไม่ตรวจข้อความ default ในแท็บ Config ว่าตรงกับที่ใช้จริง (ทุกใบใช้ชุดนี้)
- ⬜ ยังไม่ได้ดู PDF ทั้งใบหลัง V632/V633
- ไฟล์แนบยังเป็น UI เปล่า — `attach_urls` ส่ง `[]`
- ยังไม่ล็อกสิทธิ์แยก Admin/Manager (`tool_ma` เห็นได้ทั้ง admin/manager/sales)
- ราคาบันทึกเป็นยอดรวมทุกรุ่นต่อปี ไม่แยกรายรุ่น
- **ยังไม่มีปุ่ม "สร้างคำขอใหม่จากใบนี้"** — ตอนนี้ต้องคีย์ใหม่ทั้งหมด (แถบล็อกบอกให้ทำแต่ยังไม่มีทางลัด)
- เสนอไว้ยังไม่ทำ: เรียงแท็บ Config ใหม่ให้ข้อความเอกสารขึ้นก่อน

## ค้างอยู่ (อื่นๆ)
- แท็บ 4 อะไหล่: UI ตาม mockup v5 · ปุ่มรายงาน 1 หน้า A4 · **Battery ชื่อซ้ำ** (Yuasa 30 / ไม่ระบุยี่ห้อ 15 = ชิ้นเดียวกัน)
- Performance: sheets 4.7s + ETL 3.9s
- 6 mobile menus · Renewal.html · M3 SR (blocked) · M8 mobile black screen
- Hardware List ฝั่ง SP · ชิป Sale Order ถ้าขึ้น (0) → ไล่ `_spPartsInfo(qn).hasBill`
- `_PB_SVC_RX` มี 2 ชุด ควรยุบ

## คำขอถึง SmartFlow
1. ใบเสนอราคา — เพิ่ม `BRAND` / `MODEL` / `SN`
2. RAW_CONTRACT สัญญาเหมา — ระบุ SN เครื่องที่คลุม

## ดองไว้ตั้งใจ — ห้ามเสนอซ้ำ
- V610 "อะไหล่รอในคลัง" + "ติดตามสัญญา"
- % โอกาสเสียรายปี — ข้อมูลมีแค่ 2.6 ปี
- ใบเสนอราคาเข้าหน้าราคาอ้างอิง — รอ SmartFlow เพิ่มฟิลด์

---

## เกร็ดที่ได้รอบนี้
- **เอกสารที่ส่งออกไปแล้ว = ข้อมูลตาย ห้ามแก้** — ถ้าแก้ได้ ระบบจะไม่ตรงกับกระดาษที่ลูกค้าถือ และตรวจย้อนไม่ได้ · ทางที่ถูกคือออกใบใหม่ให้ได้เลขที่ใหม่
- **ล็อกที่ UI อย่างเดียวไม่พอ** — ต้องมี guard ในฟังก์ชันด้วย เพราะ `disabled` ถอดออกจาก DevTools ได้ใน 2 วินาที
- **ตอนล็อก/ปลดล็อกต้องทำทั้ง 2 ทาง** — ถ้าเขียนแค่ "ถ้าออกแล้วให้ disable" โดยไม่มี else มาถอด พอเปิดใบถัดไปจะค้างสถานะ (เทสข้อ 7 จับตรงนี้)
