# STATE — BJH Revenue Dashboard
อัปเดต: 1 ส.ค. 2569 · เวอร์ชันล่าสุด **V635.0**

> **ลำดับการลง (ถ้ายังค้าง):** `patch_V633.ps1` → `patch_V634.ps1` → `patch_V635.ps1`
> V633 แตะ Code.js (ต้อง Deploy New Version) · V634/V635 แตะแค่ MA.html

---

## กติกาการส่งงาน
ส่ง **ไฟล์เดียวต่อรอบ** = `patch_Vxxx.ps1` · guard รับหลาย minor เพราะ patch เขียนไฟล์ทั้งไฟล์

---

## รอบนี้ (V635.0) — เลิกใช้ Template สำรอง

**เหตุผล:** ราคามาจากกลุ่มราคา (Tools → ตั้งราคาสัญญาบริการ แบบกลุ่ม) แล้ว
รุ่นที่ไม่มีในกลุ่มราคา → Manager กรอกในตารางเองได้เลย ไม่ต้องมี Template 5 Level มาเป็นทางอ้อม

**แก้**
1. **หน้าตรวจสอบ** — ตัดแถว "Template สำรอง" (select Level + ปุ่ม ↓ ดึงจาก Template) ออก
2. **Config** — ซ่อนแท็บ "ราคา Template (5 Level)" · เปิด Config มาที่ **ข้อความเอกสาร** แทน (แท็บที่สำคัญสุด)
   **เนื้อหาและ logic บันทึกยังอยู่ครบ ไม่ได้ลบ** — เผื่ออยากกลับมาใช้ แค่เปลี่ยน `style="display:none"` ออก
3. `pullTemplate()` ใส่ guard — ถูกเรียกจาก console ก็ไม่พัง ขึ้นข้อความบอกว่าเลิกใช้แล้ว

**Config เหลือ 2 แท็บที่ใช้จริง**
| แท็บ | ใช้ทำอะไร |
|---|---|
| **ข้อความเอกสาร (Default)** | **สำคัญสุด** — ไม้บรรทัดวัดว่าอะไรคือ "มาตรฐาน" (`vFillClauses` เทียบกับค่านี้) · เก็บหัวเอกสาร โลโก้ เพดานปี ตัวเลือกปีประกัน รายการยกเว้น |
| ฐานข้อมูลรุ่นเครื่อง | เติม `spec_pulled` ลงชีท Machines · dropdown ยี่ห้อ/รุ่นดึงจากกลุ่มราคาแล้ว ไม่ใช่ตรงนี้ |

---

## ทดสอบ
- `node --check` ผ่าน
- jsdom **24/24 ผ่าน**
  - ไม่มี `v_level` / ปุ่มดึง Template / ข้อความ "Template สำรอง" ที่ผู้ใช้เห็น
  - เปิดใบไม่ throw (เดิม `vOpen` เคยอ่าน `v_level`) · ราคาจากกลุ่มราคายังเติมได้ · สรุปราคายังทำงาน
  - รุ่นไม่มีในกลุ่มราคา → สรุปขึ้น "ยังว่าง" + ตารางเครื่องเตือน + ช่องราคากรอกได้
  - `pullTemplate()` เรียกตรงๆ ไม่พัง
  - Config: แท็บ Template ซ่อน · เปิดมาที่ข้อความเอกสาร · สลับแท็บได้ · `cpaneT` ยังอยู่
  - V634 lock ยังทำงาน
- diff 17 บรรทัด · บรรทัดสุดท้ายเหมือนเดิม

---

## วิธีลง
```powershell
cd C:\bjh-dashboard
& .\patch_V635.ps1
```
guard: รับ V632.x–V634.x · ไม่แตะ Code.js → ไม่ต้อง Deploy New Version

---

## Flow MA (ยืนยันแล้ว)
```
Admin คีย์  →  ดูร่าง  →  ระบบตัดสิน
                        ├── ราคามาตรฐานครบ + ไม่ขอลด → ออก PDF เลย (status='auto')  🔒 ล็อกทันที
                        └── มีรุ่นไม่มีราคา / ขอส่วนลด → Manager → Approve → PDF     🔒 ล็อกหลังอนุมัติ
แก้ใบที่ออกแล้วไม่ได้ → สร้างคำขอใหม่ (ได้เลขที่ใหม่ ตรวจย้อนได้)
```

---

## ค้างอยู่ (MA)
- ⬜ ยังไม่ทดสอบเส้นทาง "ส่ง Manager" กับใบจริง (รุ่นไม่มีราคา / ขอส่วนลด)
- ⬜ ยังไม่ตรวจข้อความ default ในแท็บ Config ว่าตรงกับที่ใช้จริง — **ตอนนี้เปิด Config มาเจอแท็บนี้เลย ควรไล่อ่านสักรอบ**
- ⬜ ยังไม่ได้ดู PDF ทั้งใบหลัง V633
- ไฟล์แนบยังเป็น UI เปล่า — `attach_urls` ส่ง `[]`
- ยังไม่ล็อกสิทธิ์แยก Admin/Manager (`tool_ma` เห็นได้ทั้ง admin/manager/sales)
- ราคาบันทึกเป็นยอดรวมทุกรุ่นต่อปี ไม่แยกรายรุ่น
- ยังไม่มีปุ่ม "สร้างคำขอใหม่จากใบนี้" — แถบล็อกบอกให้ทำแต่ต้องคีย์ใหม่หมด

## ค้างอยู่ (อื่นๆ)
- แท็บ 4 อะไหล่: UI ตาม mockup v5 · ปุ่มรายงาน 1 หน้า A4 · **Battery ชื่อซ้ำ** (Yuasa 30 / ไม่ระบุยี่ห้อ 15 = ชิ้นเดียวกัน)
- Performance: sheets 4.7s + ETL 3.9s
- 6 mobile menus · Renewal.html · M3 SR (blocked) · M8 mobile black screen
- Hardware List ฝั่ง SP · ชิป Sale Order ถ้าขึ้น (0) → ไล่ `_spPartsInfo(qn).hasBill`
- `_PB_SVC_RX` มี 2 ชุด ควรยุบ

## คำขอถึง SmartFlow
1. ใบเสนอราคา — เพิ่ม `BRAND` / `MODEL` / `SN`
2. RAW_CONTRACT สัญญาเหมา — ระบุ SN เครื่องที่คลุม

## ดองไว้ตั้งใจ — ห้ามเสนอซ้ำ
- V610 "อะไหล่รอในคลัง" + "ติดตามสัญญา"
- % โอกาสเสียรายปี — ข้อมูลมีแค่ 2.6 ปี
- ใบเสนอราคาเข้าหน้าราคาอ้างอิง — รอ SmartFlow เพิ่มฟิลด์
- **Template 5 Level** — เลิกใช้แล้ว (V635) ซ่อนไว้ ไม่ได้ลบ

---

## เกร็ดที่ได้รอบนี้
- **ถอดฟีเจอร์ให้ซ่อนก่อน อย่าเพิ่งลบ logic** — ซ่อนแท็บ + ใส่ guard ในฟังก์ชัน ใช้เวลาน้อยกว่าและกลับมาเปิดได้ทันทีถ้าคิดผิด
- **ถอด element ที่ฟังก์ชันอื่นอ่านอยู่ ต้องใส่ guard** — `pullTemplate` อ่าน `v_level` ถ้าไม่ใส่ null check จะ throw ตอนถูกเรียกจาก console
- **ถอดของแล้วต้องเปลี่ยนค่าเริ่มต้นด้วย** — ซ่อนแท็บ T แต่ลืมย้าย `on` ไปแท็บอื่น จะเปิด Config มาแล้วเจอหน้าว่าง
