# STATE — BJH Revenue Dashboard
อัปเดต: 3 ส.ค. 2569 · เวอร์ชันล่าสุด **V643.0**

> **patch นี้รวม V642 ที่ยังไม่ได้ลงมาด้วย** (repo ค้างที่ V641.0) — รันตัวเดียวจบ

---

## กติกาการส่งงาน
ส่ง **ไฟล์เดียวต่อรอบ** = `patch_Vxxx.ps1` · guard รับหลาย minor
**สะสมให้ครบ 3 เรื่องก่อนเขียนโค้ด** · patch ที่ยังไม่ได้ลง ให้ยุบรวมกับตัวถัดไป
**ไฟล์ .txt / .log ที่ Eak แนบมา มาถึงเป็นค่าว่างเสมอ** — ขอให้วางเป็นข้อความ หรือส่งเป็น .csv / .xlsx แทน

---

## รอบนี้ (V643.0)

### A. [V642 ที่ค้าง] Tools → ตั้งราคาสัญญาบริการ
**1. ปุ่ม 📈 Analysis** — เทียบราคาที่ตั้งไว้กับราคาฐานต้นทุน
```
ราคาฐาน = Incident/ปี × ค่าแรงต่อ Incident ÷ (1 − margin)
Incident = PM + CM
```
- config บนหัว: ค่าแรง (ตั้งต้น 9,700) + margin 3 ระดับ (40/50/60 แก้ได้) · จำใน localStorage
- ตาราง: กลุ่ม · แบรนด์ · รุ่น · เครื่อง · PM · CM · Incident · ต้นทุน · ราคาที่ 3 margin · ราคาที่ตั้งไว้ · สถานะ
- PM/CM แก้รายกลุ่มได้ + ปุ่มคืนค่าเดิม

**2. ฝังข้อมูล Incident จริง** `_INC_DATA` 29 แบรนด์ · 124 รุ่น
**ช่วง 1 ส.ค. 2568 – 31 ก.ค. 2569** · 1,305 เครื่อง · 33,450 รายการ
- กรอง `Completed` + `สัญญาบริการ (ไม่รวมอะไหล่)`
- PM = Service Contract (SC) / PM-MA · CM = Repair / On Call / Onsite / Project Onsite
- **นับ SR ไม่ซ้ำต่อเครื่อง** ไม่ใช่นับแถว (1 SR ≈ 2.1 แถว เพราะช่างหลายคน) · แยก SN ที่รวมกัน (4%)
- **เฉลี่ยกลุ่ม = ถ่วงน้ำหนักด้วยจำนวนเครื่อง แล้วปัดขึ้น** — ไม่ใช่หารจำนวนรุ่น
- **เตือนเมื่อ PM/CM ในกลุ่มต่างกันเกิน 2** → ควรแยกกลุ่ม

**3. ไม่เก็บราคา 0 + เรียงแบรนด์ที่ตั้งแล้วขึ้นบน**
ตอนบันทึกลบช่องที่เป็น 0/ว่างทิ้ง (ยกเว้นปีที่ 1 ที่ต้องเป็น 0 เพราะอยู่ในประกัน)
→ แก้ต้นเหตุ **"รวมทุกชิ้น = 0"** ที่โผล่ในหน้า MA

### B. เอกสาร MA 2 ภาษา
- **Config** เพิ่มช่อง EN 9 ช่อง (`titleMain_en` · `clause1_en`–`clause6_en` · `notes_en` · `contact_en`)
  ไม่ต้องแก้ฝั่ง server — `maApiSaveSettings` เขียนทุกคีย์ที่ client ส่งมาอยู่แล้ว
- **Admin เลือกภาษาตอนสร้างคำขอ** (`r_lang` · ไทย / English) · เก็บลงชีทเป็น `doc_lang`
- **PDF** ใช้ `pickL()` — ลำดับ: `ov_xxx` → `clauseN_en` (ถ้าเลือก EN) → `clauseN`
  **EN ว่าง → ถอยไปใช้ไทยอัตโนมัติ** ไม่ปล่อยว่าง
- ตารางเครื่องในข้อ 1 มี 2 ภาษา: `comprising` / `× 2` / `Total 3 unit(s) — this proposal covers only the equipment listed above.`
- หน้าร่างและหน้า Manager โชว์ภาษาที่เลือก

**ข้อความ EN แปลไว้แล้วในแชท** — เอาไปวางใน Config ได้เลย (ข้อ 5 อาจขาดท้าย ตรวจอีกรอบ)

---

## ทดสอบ
- `node --check` ผ่านทั้ง 3 ไฟล์
- **V642: jsdom 42/42** · **V643 ภาษา: vm 28/28**
  - ไทย/อังกฤษเลือกถูก · EN ว่างถอยไปไทย · `ov_xxx` ชนะทั้ง 2 ภาษา
  - `doc_lang` ตัวใหญ่/แปลกๆ/ไม่มี → จัดการถูก · ไม่มีเครื่อง/qty 0 ไม่พัง
  - Config 9 ช่อง EN ครบ · payload ส่ง `doc_lang` · ชีทเก็บ `doc_lang`
  - ยืนยัน V642 ติดมาครบ
- **บั๊กที่เทสจับได้:** `var L` (ป้าย 2 ภาษา) ถูกประกาศ**ก่อน** `_lang` → ตารางเครื่องจะเป็นไทยเสมอแม้เลือก EN · ย้ายลำดับแล้ว
- diff: MA 27 · Code.js 44 · SalesProspect 178 · บรรทัดสุดท้ายเหมือนเดิมทุกไฟล์

---

## วิธีลง
```powershell
cd C:\bjh-dashboard
& .\patch_V643.ps1
```
guard: รับ V641.x หรือ V642.x

> ⚠ **แตะ Code.js → เช็คว่า Deploy ขึ้นเวอร์ชันใหม่จริง**

---

## ผลวิเคราะห์ราคา (Excel แยกไฟล์)
```
ต้นทุน/Incident 9,700 = ช่าง 2 วัน × 3,000 + ที่พัก 700 + เดินทาง 3,000
PM เฉลี่ย 1.9 (96% อยู่ที่ 1–3) · CM เฉลี่ย 0.5 ครั้ง/เครื่อง/ปี
ที่ margin 50% → 42 รุ่น · 395 เครื่อง ต่ำกว่าฐาน → +8.1 ล้าน/ปี
```
**ราคาใหม่ = สูงกว่าระหว่าง [ราคาปัจจุบัน] กับ [ราคาฐาน] — ไม่ลดราคาใคร**
(ถ้าใช้ `ต้นทุน ÷ margin` กับทุกรุ่น จะกลายเป็นลดราคา SHD/BARCO รายได้หาย ~15 ล้าน/ปี)

**ตัวแปรที่ผู้บริหารต้องเคาะ 2 ตัว** — ค่าแรงต่อ Incident และ margin
(3,000 → ไม่ต้องขึ้นราคาใครเลย · 9,700 → ได้เพิ่ม 8.1 ล้าน)

---

## ค้างอยู่
**ตั้งราคากลุ่ม**
- ตั้งไปแล้ว **1 / 154 แบรนด์** — ทำ 18 กลุ่มที่คุมหลายรุ่นก่อน (350+ เครื่อง) แล้วต่อด้วยรุ่นเดี่ยวที่เครื่อง > 20
- ยังไม่มีปุ่มเลือกประเภท 3 แบบตอนเพิ่มคอลัมน์ (รวมอะไหล่ทุกส่วน / รวมยกเว้น / ไม่รวมยกเว้น + บังคับเลือก parts)

**MA After Sales**
- ⬜ ยังไม่ทดสอบเส้นทาง "ส่ง Manager" กับใบจริง
- ⬜ ยังไม่ได้ดู PDF ทั้งใบหลัง V633 · ยังไม่ตรวจข้อความ default ใน Config
- ⬜ ยังไม่ได้กรอกข้อความ EN ลง Config
- ยังไม่ล็อกสิทธิ์แยก Admin/Manager · ยังไม่มีปุ่ม "สร้างคำขอใหม่จากใบนี้"
- ไฟล์แนบยังไม่แสดงในหน้า Manager

**อื่นๆ**
- แท็บ 4 อะไหล่: UI ตาม mockup v5 · ปุ่มรายงาน 1 หน้า A4 · Battery ชื่อซ้ำ
- Performance: sheets 4.7s + ETL 3.9s · 6 mobile menus · Renewal.html · M3 SR (blocked) · M8 mobile black screen

## คำขอถึง SmartFlow
1. ใบเสนอราคา — เพิ่ม `BRAND` / `MODEL` / `SN`
2. RAW_CONTRACT สัญญาเหมา — ระบุ SN เครื่องที่คลุม
3. จำนวน PM ที่ระบุในสัญญา — จะได้เทียบกับที่ไปจริง

## ดองไว้ตั้งใจ — ห้ามเสนอซ้ำ
- V610 "อะไหล่รอในคลัง" + "ติดตามสัญญา" · % โอกาสเสียรายปี · ใบเสนอราคาเข้าหน้าราคาอ้างอิง
- Template 5 Level (V635) · ช่องขอส่วนลดในฟอร์มคำขอ (V640)

---

## เกร็ดที่ได้รอบนี้
- **ประกาศตัวแปรผิดลำดับ = พังเงียบ** — `var L` อ่าน `_lang` ก่อนถูกกำหนดค่า ได้ `undefined` แล้วตกไปสาขาไทยเสมอ · `node --check` จับไม่ได้ ต้องรันจริงถึงเจอ
- **ฟีเจอร์ 2 ภาษาให้ทำ fallback เสมอ** — EN ว่างแล้วปล่อยว่างในเอกสารคือความเสียหาย ถอยไปใช้ไทยดีกว่า
- **เช็คก่อนว่า server ต้องแก้ไหม** — เกือบเพิ่ม whitelist คีย์ใน Code.js ทั้งที่ `maApiSaveSettings` เขียนทุกคีย์ที่ส่งมาอยู่แล้ว
