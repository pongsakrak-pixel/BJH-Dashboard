# STATE — BJH Revenue Dashboard
อัปเดต: 1 ส.ค. 2569 · เวอร์ชันล่าสุด **V632.0**

---

## กติกาการส่งงาน
ส่ง **ไฟล์เดียวต่อรอบ** = `patch_Vxxx.ps1` (ฝัง .html/.js gzip+base64 · verify SHA · เขียน STATE.md ลง repo · push เอง)
**guard ให้รับหลาย minor version** (`-like 'V63x*'`) เพราะ patch เขียนไฟล์ทั้งไฟล์ ไม่ได้ diff — เจอปัญหา ABORT ตอน V631 เพราะ guard ตรึง V630.0 เป๊ะ แต่เครื่องเป็น V630.1

---

## รอบนี้ (V632.0) — MA After Sales: UI ทั้งเมนู

**ปัญหา:** `MA.html` มี stylesheet ของตัวเองทั้งชุด สีฝังตายตัว (`#171a21` `#2f6df6` `#e8ebf0`) ฟอนต์ `IBM Plex Sans Thai / Sora`
→ หน้าตาเป็นคนละแอปกับหน้าอื่น **และโหมดสว่างพังทั้งหน้า** (พื้นเข้ม + ตัวหนังสือเข้ม อ่านไม่ออก)

**แก้:** เขียน stylesheet ใหม่ทั้งก้อน ใช้ token ของแอป — `--sur --sur2 --sur3 --bd --bd2 --tx --mu --mu2 --ac --ac2 --grn --red --ylw --font --mono`
ครบทั้ง **55 คลาส** ที่ HTML/JS ใช้ · ไม่เหลือสีธีมเดิมสักตัว

| ส่วน | เดิม | ใหม่ |
|---|---|---|
| แถบหัว | โลโก้ 34px + nav pill ในกล่องลอย | โลโก้ 26px + **sub-nav ขีดล่าง** แบบ `.sp-tab` (สี active `#7c6fe0` เดียวกับแอป) |
| ตารางรายการ | 13px · padding 10-11px | **12px · th 11px · td 7px 8px** เข้าชุดกับ SP Monitor |
| การ์ด | radius 16px · padding 22px | radius 8px · padding 14-16px |
| ตัวเลข | ฟอนต์ปกติ | `var(--mono)` |
| โหมดสว่าง | พัง | ใช้ได้ |

**ยกเว้นตั้งใจ:** บล็อก `.paper` (กระดาษร่างเอกสาร) ยังขาวเสมอทั้ง 2 ธีม — เพราะจำลองเอกสารจริงที่จะพิมพ์ออกมา

**ไม่แตะ JS เลย** — เปลี่ยนแค่ CSS + ข้อความในแถบหัว

---

## ทดสอบ
- `node --check` ผ่าน
- jsdom **29/29 ผ่าน**
  - ไม่เหลือสีธีมเดิม · ใช้ `var(--sur/--tx/--bd/--font/--mono)` ครบ
  - สี hex ที่เหลือมีแค่ `#7c6fe0` (สี active ตรงกับแอป) + สีในกระดาษร่าง · ตรวจว่าสีกระดาษอยู่ในบล็อก `.paper` จริง
  - คลาสครบ 55 ไม่มีหลุด
  - ตาราง: 12px · th 11px `--mu` · td 7px 8px · fixed · sticky · hover
  - โหมดสว่าง: ไม่มีพื้นเข้ม/ตัวหนังสือฝังตาย
  - ฟังก์ชันยังทำงาน: หน้า Admin · แถวเครื่อง · ตารางรายการ 9 td · Config 3 แท็บ
- diff 202 บรรทัด · บรรทัดสุดท้ายเหมือนเดิม

---

## วิธีลง
```powershell
cd C:\bjh-dashboard
& .\patch_V632.ps1
```
guard: รับ V631.x · **ไม่แตะ Code.js** → ไม่ต้อง Deploy New Version

---

## Flow MA (ยืนยันแล้ว)
```
Admin คีย์  →  ดูร่าง  →  ระบบตัดสิน
                        ├── ราคามาตรฐานครบ + ไม่ขอลด → ออก PDF เลย (status='auto')
                        └── มีรุ่นไม่มีราคา / ขอส่วนลด → Manager
Manager  →  เห็นเฉพาะที่ต้องตัดสิน (ข้อความที่แก้ + ราคาที่ว่าง/แก้เอง)  →  Approve → PDF
```

### Config 3 แท็บ — ยังต้องมีทั้งหมด
| แท็บ | ใช้ทำอะไร |
|---|---|
| **ข้อความเอกสาร (Default)** | **สำคัญสุด** — เป็นไม้บรรทัดวัดว่าอะไรคือ "มาตรฐาน" (`vFillClauses` เทียบกับค่านี้) · เก็บหัวเอกสาร โลโก้ เพดานปี ตัวเลือกปีประกัน รายการยกเว้น |
| ราคา Template 5 Level | สำรอง — ปุ่ม "↓ ดึงจาก Template" ในหน้า Manager ใช้ตอนเจอรุ่นที่ไม่มีในกลุ่มราคา |
| ฐานข้อมูลรุ่นเครื่อง | เติม `spec_pulled` ลงชีท Machines (ยังไม่ได้เอาไปแสดงใน PDF) · dropdown ยี่ห้อ/รุ่นดึงจากกลุ่มราคาแล้ว ไม่ใช่ตรงนี้ |

---

## ค้างอยู่ (MA)
- ⬜ ยังไม่ทดสอบเส้นทาง "ส่ง Manager" กับใบจริง
- ⬜ ยังไม่เปิด PDF จริงดูว่าตารางเครื่อง `{MACHINES}` มาครบไหม
- ⬜ ยังไม่ตรวจข้อความ default ในแท็บ Config ว่าตรงกับที่ใช้จริง (ทุกใบจะใช้ชุดนี้)
- ไฟล์แนบยังเป็น UI เปล่า — `attach_urls` ส่ง `[]`
- ยังไม่ล็อกสิทธิ์แยก Admin/Manager (`tool_ma` เห็นได้ทั้ง admin/manager/sales)
- ราคาบันทึกเป็นยอดรวมทุกรุ่นต่อปี ไม่แยกรายรุ่น
- เสนอไว้ยังไม่ทำ: เรียงแท็บ Config ใหม่ให้ข้อความเอกสารขึ้นก่อน + ใส่วงเล็บบอกหน้าที่

## ค้างอยู่ (อื่นๆ)
- แท็บ 4 อะไหล่: UI ตาม mockup v5 · ปุ่มรายงาน 1 หน้า A4 · **Battery ชื่อซ้ำ** (Yuasa 30 / ไม่ระบุยี่ห้อ 15 = ชิ้นเดียวกัน)
- Performance: sheets 4.7s + ETL 3.9s
- 6 mobile menus · Renewal.html · M3 SR (blocked) · M8 mobile black screen
- Hardware List ฝั่ง SP · ชิป Sale Order ถ้าขึ้น (0) → ไล่ `_spPartsInfo(qn).hasBill`
- `_PB_SVC_RX` มี 2 ชุด ควรยุบ

## คำขอถึง SmartFlow
1. ใบเสนอราคา — เพิ่ม `BRAND` / `MODEL` / `SN`
2. RAW_CONTRACT สัญญาเหมา — ระบุ SN เครื่องที่คลุม

## ดองไว้ตั้งใจ — ห้ามเสนอซ้ำ
- V610 "อะไหล่รอในคลัง" + "ติดตามสัญญา"
- % โอกาสเสียรายปี — ข้อมูลมีแค่ 2.6 ปี
- ใบเสนอราคาเข้าหน้าราคาอ้างอิง — รอ SmartFlow เพิ่มฟิลด์

---

## เกร็ดที่ได้รอบนี้
- **หน้าที่นำเข้ามาจากที่อื่นมักมี CSS ของตัวเอง** — MA.html ฝังสีตายตัวทั้งชุด นอกจากหน้าตาไม่เข้าพวกแล้ว **โหมดสว่างพังเงียบ** ไม่มีใครสังเกตเพราะไม่ค่อยมีคนสลับธีม
- **ก่อนเขียน CSS ใหม่ ให้ dump รายชื่อคลาสเดิมก่อน** (`grep -o '#v-ma \.[\w-]*'`) แล้วเทียบว่าครบ — ตกคลาสเดียวหน้าพังเฉพาะจุดที่ไม่ค่อยเปิด
- **guard เวอร์ชันควรรับหลาย minor** — ตรึง `V630.0` เป๊ะทำให้ ABORT ทั้งที่ patch ใช้ได้ปกติ (patch เขียนไฟล์ทั้งไฟล์)
- **ให้ patch พิมพ์เวอร์ชันที่เจอออกมาด้วย** — ตอน ABORT จะรู้ทันทีว่าเครื่องอยู่เวอร์ชันอะไร ไม่ต้องเดา
