# STATE — BJH Revenue Dashboard
อัปเดต: 30 ก.ค. 2569 · เวอร์ชันล่าสุด **V625.0**

---

## กติกาการส่งงาน

ส่ง **ไฟล์เดียวต่อรอบ** = `patch_Vxxx.ps1` (ฝัง .html แบบ gzip+base64 · เขียนเอง · verify SHA เอง · เขียน STATE.md ลง repo เอง · push เอง)
**ห้ามแนบ .html หรือ STATE.md แยก**

---

## รอบนี้ทำอะไร (V625.0) — `SalesProspect.html`

**เคส:** เปิดเครื่อง HOLOGIC 3Dimensions TOMO · SN `3DM160701059` (มูลนิธิกาญจนบารมี) แล้วแท็บ 2 ประวัติสัญญา = 0 รายการ ทั้งที่เครื่องอยู่ในสัญญาจริง

### สาเหตุจริง (พิสูจน์จาก console ของเครื่องจริง)
- ค้น `3DM160701059` ใน **ทุก field** ของ RAW_CONTRACT 5,445 ใบ → **0 แห่ง**
- แต่ Install Base ระบุ `CON_NO = กบม.ส.001/2565` ไว้ชัดเจน
- สัญญาใบนั้นคือ **สัญญาเหมา 195.9M** — `SERAIL_NUMBER = G3024212` (Hitachi ตัวเดียวจากทั้งชุด) · `PRODUCT_NAME = "Hitachi, Hologic, Dell, BCN, Barco, Enedo, CISCO, Etc al."` ไม่มี SN สักตัว
- → ค้นด้วย SN อย่างเดียวไม่มีวันเจอ ไม่ใช่บั๊กของตรรกะเดิม แต่เป็นข้อจำกัดของวิธี

**ทางที่ไม่เลือก:** คลาย SN ให้ match แบบ prefix — SN ตระกูล `3DM160701xxx` ต่างกันแค่ 3 หลักท้าย มี 9 ใบใช้ prefix เดียวกัน ถ้าคลายจะลากสัญญาเครื่องอื่นเข้ามาทันที (ซ้ำรอยเคส SN 00806 → LEICA 6.9M ที่แก้ไปตอน V601)

### สิ่งที่ทำ
**A. ชั้นที่ 3 — match ด้วย `CON_NO`**
```
ชั้น 1  SERAIL_NUMBER ตรง        (แข็งสุด — เดิม)
ชั้น 2  SN อยู่ในข้อความ          (อ่อน — เดิม)
ชั้น 3  CON_NO ของเครื่อง = CONTRACT_NO   ← ใหม่
```
- `CON_NO` ไม่ใช่การเดา — เป็นกุญแจที่ Install Base ระบุมาเอง
- normalize เบา (ตัดช่องว่าง/จุด/ขีด) เพราะเขียนไม่ตรงกัน: `กบม.ส.001/2565` vs `กบม.ส 001/2565`
- **เทียบแบบตรงตัวทั้งก้อน ไม่ใช่ substring** — กัน `001/2565` ไปชนสัญญาคนละใบ
- รองรับหลายเลขในช่องเดียว (`004/2567 , 008/2567`) → split comma
- ใบที่มาทางนี้ตั้ง `snWeak=false` เพื่อไม่ให้ตัวกรอง V607 เขี่ยทิ้งตอนมีใบ SN แข็งอยู่ด้วย

**B. ป้ายกำกับ กันอ่านผิด**
- ป้าย teal `📦 สัญญาเหมา · N เครื่อง` ในคอลัมน์ QN
- ใต้ราคาเขียน `ราคาทั้งสัญญา` — 195.9M คือทั้งใบ ไม่ใช่ค่า SC ของเครื่องเดียว
- นับ N จาก **`_spInstallAll`** (ฐานเต็ม) เฉพาะเครื่องของลูกค้ารายเดียวกัน — ห้ามใช้ `_ibAllRows` เพราะผ่านตัวกรอง UI แล้ว

**C. ข้อความตอนว่าง**
`— ไม่พบประวัติสัญญาของลูกค้ารายนี้ —` → `— ไม่พบสัญญาที่ผูกกับเครื่องนี้ (ค้นจาก SN และเลขที่สัญญา) —`
ของเดิมเขียนว่า "ลูกค้า" แต่โค้ดค้นด้วยเครื่อง ทำให้เข้าใจผิดทุกครั้งที่เจอ

**ไม่แตะ** ตาราง "ประวัติเสนอราคา" (แท็บ 6) — ยัง match ด้วย SN อย่างเดียวเหมือนเดิม

---

## Decision

| เรื่อง | สรุป |
|---|---|
| เกณฑ์ match ใหม่ | `CON_NO` เท่านั้น · ห้ามคลาย SN เป็น prefix/fuzzy เด็ดขาด |
| นับ N เครื่อง | เฉพาะลูกค้ารายเดียวกัน (เลขสมเหตุสมผลกว่านับทั้งระบบ) |
| แท็บ 6 | ยังไม่แตะรอบนี้ |
| ตัวกรอง V607 | ใบจาก CON_NO ถือเป็นหลักฐานแข็ง ไม่โดนเขี่ย |

---

## ทดสอบ

- `node --check` ผ่านทุก script block
- vm **30/30 ผ่าน** — รันโค้ดจริงที่ตัดจากไฟล์ที่ patch แล้ว ด้วยข้อมูลจริงจาก console
  - เคส 3DM160701059: เจอ 1 ใบ (เดิม 0) · ราคา 195.9M · `pkgN=3` · ไม่นับเครื่องลูกค้าอื่น
  - เครื่อง 656 เปิดดู → ได้เฉพาะสัญญาตัวเอง ใบเหมา 195.9M ไม่หลุดเข้ามา
  - regression: SERAIL_NUMBER ตรง ยังชนะใบที่จับจากข้อความ · ไม่มีของแข็งค่อยใช้ของอ่อน (พฤติกรรมเดิม)
  - SN แข็ง + CON_NO พร้อมกัน → ได้ทั้งคู่ ใบเหมาไม่โดนเขี่ย ใบข้อความอ่อนยังโดนเขี่ย
  - เคสขอบ: ไม่มี CON_NO / `CON_NO="-"` / หลายเลขในช่องเดียว / `_spInstallAll` ว่าง / ไม่มีสัญญาเลย — ไม่พังสักเคส
  - ตรวจตำแหน่งในไฟล์: ตรรกะใหม่ไม่ล้ำเข้าไปในส่วนประวัติเสนอราคา
- SHA256 byte-exact · diff: SalesProspect 53 บรรทัด · overrides 4 (bump version) · บรรทัดสุดท้ายเหมือนเดิม

---

## วิธีลง

```powershell
cd C:\bjh-dashboard
& .\patch_V625.ps1
```
guard: ฐานต้องเป็น V625 ไม่ใช่ → ABORT · SHA ไม่ตรง = revert อัตโนมัติ

> ไม่แตะ Code.gs → ไม่ต้อง Deploy → New Version ด้วยมือ
> ไม่แตะ classify → ไม่ต้องกด Load Data สด

---

## ค้างอยู่

- **ข้อมูลต้นทาง (แจ้ง IT/SmartFlow ได้):** RAW_CONTRACT ของสัญญาเหมาไม่ระบุ SN เลย ทำให้ผูกเครื่องกับสัญญาได้เฉพาะทาง CON_NO — ถ้าเติม SN ในสัญญาได้ ระบบจะแม่นขึ้นทุกหมวด
- Performance: sheets 4.7s + ETL 3.9s
- 6 mobile menus (ยังไม่เริ่ม)
- **Renewal.html** — Predict `customer_id + brand + model` · A=KPI · B=Gap · C=Funnel (ยังไม่เริ่ม)
- M3 SR (blocked) · M8 mobile black screen
- Hardware List ฝั่ง SP (`renderSPDetail`) — ตกลงกันไว้ว่าเอา SC ก่อน
- ชิป Sale Order ถ้าขึ้น `(0)` ให้ไล่ตัวกรอง `_spPartsInfo(qn).hasBill`
- คอลัมน์ Model จะเปลี่ยนเป็นอังกฤษ (`2 Models` / `3 Units`) หรือไม่

## ดองไว้ตั้งใจ — ห้ามเสนอซ้ำ
V610 "อะไหล่รอในคลัง" + "ติดตามสัญญา" — รอ Weekly Report ใช้จริง 2-3 สัปดาห์ก่อน

---

## เกร็ดที่ได้รอบนี้

- **"หาไม่เจอ" ≠ "ไม่มี"** — ก่อนคลายเกณฑ์ match ให้ไล่ดูก่อนว่าข้อมูลผูกกันด้วยกุญแจอื่นอยู่แล้วหรือเปล่า เคสนี้ CON_NO ถูกกรอกครบมาตลอด แค่ไม่เคยถูกใช้
- SN ตระกูลเดียวกันต่างกันแค่หลักท้าย (`3DM160701` + 3 หลัก) → **ห้าม match แบบ prefix/substring กับ SN เด็ดขาด** จดไว้คู่กับกฎ V601 (ห้ามเอา CONTRACT_NO ใส่ haystack ของ SN)
- ตัวเลขเงินจากสัญญาเหมาต้องมีป้ายเสมอ ไม่งั้นคนอ่านเข้าใจว่าเป็นค่าเครื่องเดียว
- ข้อความ empty-state ที่ไม่ตรงกับตรรกะจริง ทำให้ debug หลงทาง — เขียนให้ตรงว่าค้นด้วยอะไร
- **patch script:** เมื่อ 2 ตารางมี markup เหมือนกันเป๊ะ อย่าใช้ anchor ข้อความล้วน ให้ยึดตำแหน่งจากของที่เพิ่งแทรกไป (`sp.index(marker)` แล้วหา anchor ตัวถัดไป + assert ระยะห่าง)
- **เทส:** อย่า assert ด้วย "จำนวนครั้งที่พบคำ" — เปราะมาก ให้ assert ด้วยตำแหน่งสัมพัทธ์ในไฟล์แทน
