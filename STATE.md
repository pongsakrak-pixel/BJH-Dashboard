# STATE — BJH Revenue Dashboard
อัปเดต: 31 ก.ค. 2569 · เวอร์ชันล่าสุด **V626.0**

---

## กติกาการส่งงาน

ส่ง **ไฟล์เดียวต่อรอบ** = `patch_Vxxx.ps1` (ฝัง .html แบบ gzip+base64 · เขียนเอง · verify SHA · เขียน STATE.md ลง repo · push เอง)
ห้ามแนบ .html หรือ STATE.md แยก

---

## รอบนี้ทำอะไร (V626.0) — แท็บ 4 อะไหล่ · `SalesProspect.html`

### สิ่งที่วัดได้จากข้อมูลจริง (console 31 ก.ค. 69 · กลุ่ม Proactive Mammogram)

| | |
|---|---|
| Install Base ทั้งกลุ่ม | **165 เครื่อง** (7 รุ่น) |
| เคยมีงานเบิก | 107 เครื่อง ← ตัวหารเดิม |
| งานเบิก | 417 รายการ · จับคู่ SN ได้ **98%** |
| ช่วงข้อมูล | ม.ค. 2567 → ก.ค. 2569 = **2.6 ปี** |
| อายุ fleet | median 7.5 ปี · ต่ำกว่า 3 ปี 31 เครื่อง |
| เบิกเฉลี่ยตามอายุ | 0-3 ปี 0.8 · 3-6 ปี 2.0 · 6-9 ปี 2.9 · 9+ ปี 3.4 ครั้ง/เครื่อง |

เส้นเบิกไต่ขึ้นตามอายุเรียบๆ = ข้อมูลสะท้อนการสึกหรอจริง ใช้ได้
คอมเมนต์เดิมในโค้ดที่เขียนว่า "จับคู่ SN ได้ราว 77%" **ล้าสมัยแล้ว** อย่างน้อยกลุ่มนี้ได้ 98%

### A. ตัวหารผิด — แก้แล้ว
`modelSNs[_pSn]=1` อยู่ในลูป `_jsupParts` → นับเฉพาะ**เครื่องที่เคยเบิก** เครื่องที่ใช้ดีไม่เคยเบิก 58 เครื่องหายจากสมการ
→ นับใหม่จาก `_ibSrc` (Install Base เต็ม) ตามรุ่นในกลุ่ม เก็บที่ `window._ibFleetN`
→ **% ทุกตัวลดลง ~35%** (Battery 28% → 18%)
มี fallback: ถ้านับ fleet ไม่ได้ (ไม่มีวันติดตั้ง/รุ่นสะกดไม่ตรง) ใช้ตัวเดิม ไม่พัง

### B. เลิกปัดอายุเป็นปีเต็ม
เดิม `_p25/_p50 = Math.round()` และเทียบกับ `Math.round(อายุเครื่อง)`
→ อะไหล่ช่วง 2.6 กับ 3.4 ปี กลายเป็น "3 ปี" เท่ากัน ทั้งที่ห่างกัน 10 เดือน
→ ใช้ค่าดิบเทียบ · แสดงผลทศนิยม 1 ตำแหน่ง (2.4–3.2 ปี)
ผลจริง: เครื่อง 3.0 ปี กับอะไหล่ median 3.2 — เดิมตอบ "ถึงรอบแล้ว" ตอนนี้ตอบ "ใกล้ช่วง" ซึ่งถูกกว่า

### C. เปลี่ยนคำ — เลิกตำหนิลูกค้า
| เดิม | ใหม่ |
|---|---|
| 🔴 ถึงรอบแล้ว | 🟠 อยู่ในช่วงที่มักเปลี่ยน |
| 🟡 ใกล้ถึงรอบ | 🟡 ใกล้ช่วงที่มักเปลี่ยน |
| 🟢 ยังไม่ถึงรอบ | 🟢 ยังไม่ถึงช่วง |
| 4️⃣ แผนดูแลอะไหล่ตามอายุการใช้งาน | 4️⃣ อะไหล่ที่ควรวางแผน |
| หัวข้อกลุ่ม "ควรคุยกับลูกค้าตอนนี้" | "คุยกับลูกค้าได้" |

สีแดง → ส้ม ด้วย · "ถึงรอบ" สื่อว่าลูกค้าปล่อยเครื่อง ทั้งที่จริงคือเครื่องถึงช่วงอายุตามปกติ

### D. กรองค่าบริการออกจากรายการอะไหล่
`ค่าบริการสำหรับ Mammo` ติด Top 10 ของกลุ่มรุ่น — ไม่ใช่อะไหล่ ห้ามโผล่ในรายงานลูกค้า
กรอง: `ค่าบริการ|ค่าแรง|ค่าเดินทาง|ค่าขนส่ง|SERVICE CHARGE|LABOR COST`

---

## Decision สำคัญ — **ไม่ทำ "% โอกาสเสียภายใน 1 ปี"**

เคยตกลงกันว่าจะทำ Predict 1 ปี เป็น % รายชิ้น · ทดลองกับข้อมูลจริงแล้ว **ทำไม่ได้**

ช่วงอายุ 3→4 ปี มีเครื่องที่ระบบมองเห็นครบแค่ **15 เครื่อง** และ Top 10 ออกมาแบบนี้:
```
1. ASY-10628 CDA Housing   7% (1/15)
2. PCB-00186 Paddle Sensor 7% (1/15)
...ทุกตัว 1/15 เท่ากันหมด
```
ลำดับมาจากลำดับใน object ไม่ได้มาจากความเสี่ยง · เพิ่มอีก 1 เคสตัวเลขกระโดดเท่าตัว
**ต้นเหตุ: ข้อมูลมีแค่ 2.6 ปี** ซอยเป็นช่วงอายุรายปีแล้วเหลือ 0-1 เหตุการณ์ต่อช่อง

**ใช้ 2 ตัวเลขที่ข้อมูลรองรับแทน:**
1. **% ระดับ fleet** — 30 จาก 165 เครื่อง (n=21-30 นิ่ง ตรวจย้อนได้)
2. **ช่วงอายุที่มักเปลี่ยน** เทียบกับอายุเครื่องนี้ (ของเดิม V599 รวม 404 รายการ ไม่ซอยย่อย จึงไม่เจอปัญหา 1/15)

ประโยคที่ลูกค้าเห็น: *"Battery 12V/9Ah — 30 จาก 165 เครื่องในกลุ่มรุ่นเดียวกันเคยเปลี่ยน · มักเปลี่ยนช่วง 2.4–3.2 ปี · เครื่องท่านอายุ 3 ปี"*
ข้อเท็จจริงล้วน ไม่มี % ที่ป้องกันตัวไม่ได้

**ถ้าจะทำ % รายปีจริง ต้องรอข้อมูลครบ 5+ ปี** (อีกราว 2-3 ปี) ห้ามรื้อเรื่องนี้ก่อนถึงตอนนั้น

---

## ทดสอบ
- `node --check` ผ่านทุก block
- vm **36/36 ผ่าน** — รันโค้ดจริงที่ตัดจากไฟล์ที่ patch แล้ว
  - เลิกปัดปี: p25=2.4 (ไม่ใช่ 2) · แสดง "2.4–3.2 ปี"
  - เครื่อง 3.0 ปี + median 3.2 → `soon` (เดิมตอบ `due` ผิด)
  - ขอบ 2.4 / 3.2 / 2.3 / 3.3 ถูกทุกจุด
  - เคสขอบ: ไม่รู้อายุ / ไม่มีข้อมูลอายุ / เปลี่ยนไปแล้ว / ลำดับ sort
  - ตัวกรอง: ตัดค่าบริการ 4 แบบ · เก็บอะไหล่จริง 6 ตัวครบ
  - ตัวหาร: มี fallback · `_dotsOf` ใช้ตัวใหม่ · แถบฐานติดป้าย
- diff: SalesProspect 62 บรรทัด · overrides 4 · บรรทัดสุดท้ายเหมือนเดิม

---

## วิธีลง
```powershell
cd C:\bjh-dashboard
& .\patch_V626.ps1
```
guard: ฐานต้องเป็น V625.0 · SHA ไม่ตรง = revert อัตโนมัติ

> **แก้ตรรกะ classify → ต้องกด Load Data สด 1 ครั้งหลังลง** ไม่งั้นยังเห็นตัวเลขเดิม
> ไม่แตะ Code.gs → ไม่ต้อง Deploy New Version

**ตัวเลขจะเปลี่ยนทั้งหน้า** — % ลดลง ~35% ทุกแถว เป็นเรื่องปกติ ของเดิมสูงเกินจริง

---

## ค้างอยู่ (แท็บ 4)
- **ชื่ออะไหล่ซ้ำ** — Battery โผล่ 2 ชื่อ (`Battery 12V/9AH Yuasa (NPW53-12)` 30 เครื่อง / `Battery 12V/9Ah` 15 เครื่อง) เป็นของชิ้นเดียวกัน สถิติโดนผ่าครึ่ง · ต้องมี Config กลุ่มอะไหล่ให้คนตัดสิน (คอมเมนต์ V599 ในโค้ดระบุไว้แล้วว่ารอเรื่องนี้)
- UI ตาม mockup v5 (Top 10 + แถบ % + รายงาน 1 หน้า A4) — ยังไม่ทำ รอบนี้แก้แค่ตรรกะ
- ปุ่ม "สร้างรายงาน 1 หน้า ส่งลูกค้า" (print/PDF) — ยังไม่ทำ
- คอมเมนต์ "จับคู่ SN 77%" ในกล่องวิธีคิด ล้าสมัย ควรวัดใหม่ทุกกลุ่มแล้วอัปเดต

## ค้างอยู่ (อื่นๆ)
- Performance: sheets 4.7s + ETL 3.9s
- 6 mobile menus · **Renewal.html** (ยังไม่เริ่ม) · M3 SR (blocked) · M8 mobile black screen
- Hardware List ฝั่ง SP (`renderSPDetail`)
- ชิป Sale Order ถ้าขึ้น `(0)` → ไล่ตัวกรอง `_spPartsInfo(qn).hasBill`

## ดองไว้ตั้งใจ — ห้ามเสนอซ้ำ
- V610 "อะไหล่รอในคลัง" + "ติดตามสัญญา" — รอ Weekly Report ใช้จริง 2-3 สัปดาห์
- **% โอกาสเสียรายปี** — รอข้อมูลครบ 5+ ปี (เหตุผลด้านบน)

---

## เกร็ดที่ได้รอบนี้
- **ตัวหารสำคัญกว่าตัวเศษ** — โค้ดดึง Install Base มาไว้แล้วตั้งแต่ V594 แต่ใช้แค่หาวันติดตั้ง ไม่ได้เอามานับ ของอยู่ในมือแต่ไม่ได้ใช้
- **อย่าทำ mockup ด้วยเลขสมมติ** — v2-v4 ผมใส่ 92%/88% ลอยๆ พอเจอของจริงคือ 7% (1/15) ทิศทางออกแบบเปลี่ยนหมด เสียเวลา 3 รอบ · ต่อไปวัดข้อมูลก่อนวาด
- **ปัดเลขก่อนเทียบ = สร้างบั๊กเงียบ** ที่จอโชว์ขัดกันเอง (คอมเมนต์ V599 เคยเจอแล้วครั้งหนึ่ง แก้แค่ตอนแสดง ไม่ได้แก้ตอนเทียบ)
- **คำที่ใช้มีผลต่อการขาย** — "ถึงรอบแล้ว" อ่านเหมือนตำหนิลูกค้า · "อยู่ในช่วงที่มักเปลี่ยน" เป็นข้อเท็จจริงล้วน เถียงไม่ได้ และไม่ทำให้ลูกค้าตั้งการ์ด
- **ข้อมูล 2.6 ปีบอกได้ว่า "อะไรบ่อย" แต่บอกไม่ได้ว่า "ปีไหนเสี่ยง"** — จำข้อจำกัดนี้ไว้ก่อนรับงาน predict อะไรอีก
