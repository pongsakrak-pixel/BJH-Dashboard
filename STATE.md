# STATE — BJH Revenue Dashboard
อัปเดต: 31 ก.ค. 2569 · เวอร์ชันล่าสุด **V630.0**

---

## กติกาการส่งงาน
ส่ง **ไฟล์เดียวต่อรอบ** = `patch_Vxxx.ps1` (ฝัง .html/.js gzip+base64 · verify SHA · เขียน STATE.md ลง repo · push เอง)

---

## รอบนี้ (V630.0) — MA After Sales flow ใหม่

### Flow ที่ตกลงกัน
```
Admin คีย์คำขอ  →  ดูร่างเอกสาร  →  ระบบตัดสินเส้นทางเอง
                                    ├── ราคามาตรฐานครบ + ไม่ขอลด → ออก PDF เลย (status='auto')
                                    └── มีรุ่นไม่มีราคา / ขอส่วนลด → Manager อนุมัติ
```
**เหตุผล:** Service เป็นคอขวดตอนใกล้ยื่นซอง · เคสปกติไม่มีอะไรให้ตัดสิน (ราคามาจากตารางที่ตั้งไว้แล้ว)
Manager เหลือดูแค่ 2 อย่างที่คนอื่นทำแทนไม่ได้ — **เคาะราคาที่ยังไม่มีในระบบ** กับ **อนุมัติส่วนลด**

### กันความเสียหายจากการคีย์ตก (Admin ไม่ใช่เจ้าของ spec)
1. **เอกสารระบุเครื่องเป็นรายการ** ไม่ใช่ประโยคคลุม — ท้ายตารางเขียน *"ครอบคลุมเฉพาะเครื่องตามรายการข้างต้นเท่านั้น"*
   คีย์ตก 1 เครื่อง → เอกสารคุ้มแค่ที่ระบุ ไม่ลามไปเครื่องที่ไม่เคยคิดราคา
   **ห้ามเขียนประโยคแบบ "ครอบคลุมเครื่องทั้งหมดในโครงการ" เด็ดขาด**
2. **บังคับใส่เลขที่ใบเสนอราคาอ้างอิง + ชื่อผู้ขอ** — ตรวจย้อนได้ว่าคีย์จากใบไหน ใครขอ
3. **แถบนับเครื่องรวมตัวใหญ่** ในหน้าร่าง — เทียบกับใบเสนอราคาได้ใน 2 วิ
4. **Admin เห็นร่างก่อนส่ง** — จับผิดตัวเองได้ก่อน

### Code.js
- `maEnsureColumns(sh, cols)` — เติมคอลัมน์ในชีทที่มีข้อมูลแล้ว (`maEnsureSheetWithHeader` เดิมเติมเฉพาะชีทว่าง)
- `maMigrateColumns(ss)` — เรียกจาก `maSetupSheets` · เพิ่ม 6 คอลัมน์ใน Requests:
  `ref_quotation_no · requester_name · discount · discount_reason · auto_issued · issued_at`
- `maApiCreateRequest` — รับฟิลด์ใหม่ + `pm_times_per_year` (เดิมกรอกตอน verify เท่านั้น)
- **`maApiIssueDirect(payload)`** ใหม่ — สร้างคำขอ + บันทึกราคา + ออก PDF + `status='auto'` ในครั้งเดียว
  มี guard: ถ้า client ส่ง `needManager:true` มาจะ throw (กันการอ้อมเส้นทาง)
- `maBuildPdfHtml` — รองรับ **`{MACHINES}`** ในข้อ 1 = ตารางเครื่อง · ถ้าข้อความไม่มีแท็บนี้แต่มีเครื่อง จะต่อท้ายให้เอง

### MA.html
- **หน้า Admin** — หลายรุ่น (input + datalist จากกลุ่มราคา แต่พิมพ์เองได้) · เลขที่ใบเสนอราคา · ผู้ขอ · ส่วนลด+เหตุผล · ครั้ง/ปี ย้ายมาที่นี่
- **หน้าร่าง** (ใหม่) — แถบนับเครื่อง · แถบเส้นทาง (เขียว/ส้ม พร้อมเหตุผล) · เอกสารจำลอง
- **หน้า Manager** — เปลี่ยนจากการ์ดเป็น **ตาราง** ทั้งรายการและหน้าตรวจ
  - รายการ: แท็บ รอตรวจ / อนุมัติแล้ว / ออกอัตโนมัติ / ทั้งหมด + ค้นหา · ตั้งต้นเห็นเฉพาะรอตรวจ
  - หน้าตรวจ: 4 ตาราง (ข้อมูลคำขอ / เครื่อง / ราคา / ข้อความ) · ช่องพื้นส้ม = แก้ได้
- **ราคาดึงจากกลุ่มราคา** `window._PRICE_GROUPS` ฝั่ง client (ไม่ต้องแตะ backend)
  map แบบสัญญา: `none→plain · partial→excl[0].price · full→full` · ปี ≥10 ใช้แถว `10+`
  `autoFillFromGroups()` เติมเฉพาะช่องว่าง → **ค่าที่ Manager เคยแก้ชนะเสมอ**
- **Config page ไม่แตะเลย**

---

## ทดสอบ
- `node --check` ผ่านทั้ง Code.js และ script block ของ MA.html
- jsdom **67/67 ผ่าน** — รันโค้ดจริงจากไฟล์ที่ patch แล้ว
  - เพิ่ม/ลบ/แก้แถวเครื่อง · เปลี่ยนยี่ห้อล้างรุ่น · ป้ายกลุ่มราคาถูก
  - validate ครบ: โครงการ/รพ./เลขที่ใบเสนอราคา/ผู้ขอ/เครื่อง/ส่วนลดต้องมีเหตุผล
  - เส้นทาง: รุ่นไม่มีราคา→Manager · ขอลด→Manager · ครบ+ไม่ลด→ออกเลว
  - **ราคาคำนวณถูกต้อง** ปี 4 ไม่รวมอะไหล่ = 350,000×1 + 300,000×2 = 950,000 (ตรวจครบ 3 แบบ)
  - ไม่มีปีที่อยู่ในประกันหลุดเข้าไปในราคา
  - รายการ Manager: นับ/กรอง/ค้นหา/ป้ายสถานะ
  - เปิดใบ: เติมช่องใหม่ + ตารางเครื่อง + ราคาอัตโนมัติ + ค่าที่บันทึกไว้ทับ
  - **ใบเก่าก่อน V630 (ไม่มี machines) เปิดได้ไม่พัง**
  - Config ยังครบ
- diff: Code.js 77 บรรทัด · MA.html 565 · overrides 4 · บรรทัดสุดท้ายเหมือนเดิมทุกไฟล์

---

## วิธีลง
```powershell
cd C:\bjh-dashboard
& .\patch_V630.ps1
```
guard: ฐานต้องเป็น V629.0

> ⚠ **แตะ Code.js → ต้อง Deploy → New Version** (patch สั่ง `create-deployment` ให้แล้ว แต่ให้เช็คว่าเลขเวอร์ชันขึ้นจริง)
> ⚠ **รัน `maSetupSheets()` ใน Apps Script editor 1 ครั้ง** เพื่อเติม 6 คอลัมน์ใหม่ในชีท Requests
>    ไม่รัน = ฟิลด์ใหม่จะไม่ถูกบันทึก (ข้อมูลเดิมไม่หาย ปลอดภัย รันซ้ำได้)

---

## ค้างอยู่ (MA)
- **ยังไม่ทดสอบกับของจริง** — ต้องลองคีย์ 1 ใบทั้ง 2 เส้นทาง
- ไฟล์แนบยังเป็น UI เปล่า (dropzone ไม่ได้อัปโหลดจริง) — `attach_urls` ส่ง `[]`
- ยังไม่ล็อกสิทธิ์แยก Admin/Manager — ตอนนี้ `tool_ma` เห็นได้ทั้ง admin/manager/sales
- ราคาที่บันทึกเป็นยอดรวมทุกรุ่นต่อปี (ไม่แยกรายรุ่น) — ตาราง Prices ไม่มี `row_no`
- ยังไม่มีฟอร์มสาธารณะให้ Sales กรอกเอง (ตกลงกันว่า Admin คีย์แทนก่อน)
  ถ้าจะทำ: deploy ตัวที่ 2 แบบ "ทุกคนเข้าได้" + `doGet` เช็ก `Session.getActiveUser()` ว่างให้เสิร์ฟได้แค่หน้าฟอร์ม + token ต่อคน

## ค้างอยู่ (อื่นๆ)
- แท็บ 4 อะไหล่: UI ตาม mockup v5 · ปุ่มรายงาน 1 หน้า A4 · **Battery ชื่อซ้ำ** (Yuasa 30 เครื่อง / ไม่ระบุยี่ห้อ 15 เครื่อง = ชิ้นเดียวกัน สถิติผ่าครึ่ง)
- Performance: sheets 4.7s + ETL 3.9s
- 6 mobile menus · Renewal.html · M3 SR (blocked) · M8 mobile black screen
- Hardware List ฝั่ง SP · ชิป Sale Order ถ้าขึ้น (0) → ไล่ `_spPartsInfo(qn).hasBill`
- `_PB_SVC_RX` มี 2 ชุด ควรยุบ

## คำขอถึง SmartFlow
1. ใบเสนอราคา — เพิ่ม `BRAND` / `MODEL` / `SN` (ตอนนี้ไม่มีเลย)
2. RAW_CONTRACT สัญญาเหมา — ระบุ SN เครื่องที่คลุม

## ดองไว้ตั้งใจ — ห้ามเสนอซ้ำ
- V610 "อะไหล่รอในคลัง" + "ติดตามสัญญา"
- % โอกาสเสียรายปี — ข้อมูลมีแค่ 2.6 ปี
- ใบเสนอราคาเข้าหน้าราคาอ้างอิง — รอ SmartFlow เพิ่มฟิลด์

---

## เกร็ดที่ได้รอบนี้
- **เช็ค backend ก่อนออกแบบ** — ตาราง `Machines` รองรับหลายรุ่นมาตั้งแต่แรก (`request_id·row_no·brand·model·qty`) แต่ UI เดิมส่ง `machines:[]` ว่างมาตลอด · ถ้าไม่เช็คคงไปรื้อ schema เปล่าๆ
- **`maEnsureSheetWithHeader` เติม header เฉพาะชีทว่าง** — เพิ่มฟิลด์ในชีทที่ใช้งานแล้วต้องเขียนตัวเติมคอลัมน์เอง
- **เส้นทางอัตโนมัติต้องมี guard ฝั่ง server** — client บอกว่า "ราคามาตรฐาน" ไม่พอ เพราะแก้ payload ได้ · `maApiIssueDirect` เลย throw ถ้าเจอ `needManager`
- **ปัญหาจริงของ flow คือคอขวด ไม่ใช่การอนุมัติ** — ถามว่า "ใครควร verify" จะได้คำตอบผิด · ถามว่า "ใครตัดสินอะไรที่คนอื่นทำแทนไม่ได้" จะได้ flow ที่ถูก
- **คนคีย์ไม่ควรรับผิดเรื่อง spec ครบ** เพราะไม่ใช่เจ้าของ spec — แก้ด้วยการทำให้ขอบเขตเอกสารมองเห็นได้ (รายการเครื่อง) แทนการหาคนรับผิด
