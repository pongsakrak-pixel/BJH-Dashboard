# STATE — BJH Revenue Dashboard
อัปเดต: 1 ส.ค. 2569 · เวอร์ชันล่าสุด **V631.0**

---

## กติกาการส่งงาน
ส่ง **ไฟล์เดียวต่อรอบ** = `patch_Vxxx.ps1` (ฝัง .html/.js gzip+base64 · verify SHA · เขียน STATE.md ลง repo · push เอง)

---

## รอบนี้ (V631.0) — MA After Sales หลังทดสอบกับใบจริง

> **patch_V630_1.ps1 ยุบรวมมาไว้ในตัวนี้แล้ว ไม่ต้องรันตัวนั้น**

### A. [V630.1] ใบที่ออกอัตโนมัติไม่มีปุ่ม PDF
`loadResultList` เช็คแค่ `status==='approved'` แต่เส้นทางออกเองบันทึก `status='auto'`
→ ปุ่มดาวน์โหลดไม่ขึ้นทั้งที่ PDF สร้างสำเร็จ · `statusTh` ก็ไม่รู้จัก `auto`
แก้: `_done = (approved || auto)` · เพิ่ม `auto:'ออกอัตโนมัติ'` · ถ้า done แต่ไม่มี pdf_url → บอกให้กดสร้างใหม่ ไม่เงียบ
พร้อมแก้ข้อความ **"ขอ MA 10 ปี" → "รวม 10 ปี · ประกัน 3 ปี · คิดค่าบริการ 7 ปี"** (`ma_years` = ปีรวมทั้งประกัน ชื่อฟิลด์ชวนอ่านผิด)

### B. เลขที่เอกสารซ้ำ — เจอ SR-2026-0008 ซ้ำ 2 ใบ
`maNewRequestId` ใช้ `rows.length+1` → **พอลบแถวใดแถวหนึ่ง เลขจะซ้ำกับของเดิมทันที**
แก้: หาเลขสูงสุดที่มีอยู่ +1 (นับเฉพาะ prefix ปีปัจจุบัน)

### C. ตารางรายการ Manager → fixed layout แบบ SP Monitor
- `table-layout:fixed` + `<colgroup>` 9 คอลัมน์ · หัวตาราง sticky · ตัดข้อความยาวด้วย ellipsis + tooltip
- **แก้บั๊กคอลัมน์ไม่ตรงหัวข้อ** — เดิมหัวเขียน "เครื่อง" แต่ใส่เลขใบเสนอราคา/ผู้ขอ
  ตอนนี้แยกเป็น **เครื่อง** (ยี่ห้อ · N รุ่น · M เครื่อง) กับ **อ้างอิง / ผู้ขอ**
- backend `maApiListRequests` แนบ `_mcModels · _mcQty · _mcBrands` (นับจากชีท Machines ครั้งเดียว ไม่ยิงทีละใบ)
- ใบที่ไม่มีเครื่อง → ขึ้น "ไม่ระบุ" สีส้ม

### D. Manager ตรวจเฉพาะที่ "ไม่ใช่ค่ามาตรฐาน"
**ข้อความเอกสาร**
- เดิม vOpen ยัดข้อความ default ลงช่องทั้ง 7 ข้อ → Manager ต้องอ่านกำแพงข้อความทุกใบ
- ใหม่: ไม่มี override → **ช่องว่าง + ตารางยุบ** ขึ้นแถบเขียว *"✓ ใช้ข้อความมาตรฐานทั้ง 7 ข้อ — ไม่มีอะไรต้องตรวจ"*
- มี override → แถบส้ม *"⚠ ต้องอ่าน 2 ข้อ — ข้อ 2 · ข้อ 5"* ระบุเฉพาะข้อที่ต่างจริง
- ปุ่ม "กางดู / แก้ข้อความ" เปิดดูทั้งหมดได้
- **ช่องว่าง = ใช้ค่ามาตรฐานตอนออก PDF** (พฤติกรรมเดิมของ backend `pick(ovKey,defKey)`)

**ราคา**
- ช่องที่ระบบเติมจากกลุ่มราคา = สีจาง · ช่องที่คนแก้ = พื้นส้ม
- สรุปด้านบน: `ช่องราคา 21 · ✓ จากกลุ่มราคา 21 · ✎ แก้เอง 0 · ยังว่าง 0`
- พิมพ์กลับเป็นค่าเดิม → กลับไปนับเป็นอัตโนมัติ (เทียบกับ `data-autoval`)

### E. `maClearAllRequests()` — ล้างข้อมูลทดสอบ
รันเองใน Apps Script editor · ลบเฉพาะ **Requests / Machines / Prices** (เก็บแถวหัว)
**ไม่แตะ Settings / Templates / Models / Users** — เป็นค่าตั้งต้นที่ระบบใช้
ล้างแล้วเลขเอกสารเริ่ม `SR-2026-0001` ใหม่

---

## ทดสอบ
- `node --check` ผ่านทั้ง Code.js และ MA.html
- jsdom **44/44 ผ่าน** ด้วยข้อมูลจากภาพจริงของ Eak
  - ตาราง: fixed · sticky · ellipsis · colgroup 9 · ทุกแถวมี 9 td
  - คอลัมน์ตรงข้อมูล · หลายยี่ห้อ · ไม่มีเครื่อง · ปีว่างไม่เป็น NaN
  - เลขเอกสาร: ลบแถวกลางแล้วไม่ซ้ำ · ชีทว่าง→0001 · ข้ามเลขปีอื่น
  - ข้อความ: ไม่มี override→เขียว+ยุบ · มี 2 ข้อ→ส้มระบุตรงข้อ · ไม่ฟ้องข้อที่ไม่ได้แก้ · toggle ทำงาน
  - ราคา: อัตโนมัติ/แก้เอง/ยังว่าง นับถูก · แก้แล้วเปลี่ยนสี · พิมพ์กลับค่าเดิมกลับสถานะ
  - V630.1 ยังอยู่ (auto มีปุ่ม PDF · บรรทัดปีถูก)

---

## วิธีลง
```powershell
cd C:\bjh-dashboard
& .\patch_V631.ps1
```
guard: ฐานต้องเป็น V630.0

> ⚠ **แตะ Code.js → เช็คว่า Deploy ขึ้นเวอร์ชันใหม่จริง**
> ⚠ **ล้างข้อมูลทดสอบ:** เปิด Apps Script editor → เลือกฟังก์ชัน `maClearAllRequests` → Run
>    (ต้องรันหลังลง patch เพราะฟังก์ชันเพิ่งเพิ่ม)
> ℹ ถ้ายังไม่เคยรัน `maSetupSheets()` ตั้งแต่ V630 ให้รันด้วย (เติม 6 คอลัมน์ใหม่)

---

## Flow ที่ใช้อยู่ (ยืนยันแล้ว)
```
Admin คีย์  →  กดดูร่าง  →  ระบบตัดสินเส้นทาง
                            ├── ราคามาตรฐานครบ + ไม่ขอลด → ออก PDF เลย (status='auto')
                            └── มีรุ่นไม่มีราคา / ขอส่วนลด → Manager
Manager     →  เห็นเฉพาะที่ต้องตัดสิน (ข้อความที่แก้ + ราคาที่ยังว่าง/แก้เอง)  →  Approve → PDF
```

---

## ค้างอยู่ (MA)
- ⬜ ยังไม่ทดสอบเส้นทาง "ส่ง Manager" กับใบจริง
- ⬜ ยังไม่เปิด PDF จริงดูว่าตารางเครื่อง `{MACHINES}` มาครบไหม
- ไฟล์แนบยังเป็น UI เปล่า — `attach_urls` ส่ง `[]`
- ยังไม่ล็อกสิทธิ์แยก Admin/Manager (`tool_ma` เห็นได้ทั้ง admin/manager/sales)
- ราคาบันทึกเป็นยอดรวมทุกรุ่นต่อปี ไม่แยกรายรุ่น (Prices ไม่มี `row_no`)
- ยังไม่มีฟอร์มสาธารณะให้ Sales กรอกเอง (ตกลงว่า Admin คีย์แทนก่อน)

## ค้างอยู่ (อื่นๆ)
- แท็บ 4 อะไหล่: UI ตาม mockup v5 · ปุ่มรายงาน 1 หน้า A4 · **Battery ชื่อซ้ำ** (Yuasa 30 / ไม่ระบุยี่ห้อ 15 = ชิ้นเดียวกัน)
- Performance: sheets 4.7s + ETL 3.9s
- 6 mobile menus · Renewal.html · M3 SR (blocked) · M8 mobile black screen
- Hardware List ฝั่ง SP · ชิป Sale Order ถ้าขึ้น (0) → ไล่ `_spPartsInfo(qn).hasBill`
- `_PB_SVC_RX` มี 2 ชุด ควรยุบ

## คำขอถึง SmartFlow
1. ใบเสนอราคา — เพิ่ม `BRAND` / `MODEL` / `SN`
2. RAW_CONTRACT สัญญาเหมา — ระบุ SN เครื่องที่คลุม

## ดองไว้ตั้งใจ — ห้ามเสนอซ้ำ
- V610 "อะไหล่รอในคลัง" + "ติดตามสัญญา"
- % โอกาสเสียรายปี — ข้อมูลมีแค่ 2.6 ปี
- ใบเสนอราคาเข้าหน้าราคาอ้างอิง — รอ SmartFlow เพิ่มฟิลด์

---

## เกร็ดที่ได้รอบนี้
- **ID ที่สร้างจาก `rows.length+1` พังเสมอเมื่อมีการลบแถว** — ต้องใช้ max+1 หรือ counter แยก
- **หัวตารางกับข้อมูลต้องตรงกัน** — เขียนหัวว่า "เครื่อง" แล้วใส่เลขใบเสนอราคา คนอ่านเชื่อหัวตาราง แล้วเข้าใจข้อมูลผิดทั้งหน้า
- **"ให้ตรวจทุกอย่าง" = ไม่มีใครตรวจอะไรเลย** — ยัด default ลงช่องทั้ง 7 ข้อทำให้ Manager เลื่อนผ่าน · โชว์เฉพาะส่วนที่ต่างจากมาตรฐานถึงจะมีคนอ่านจริง
- **ต้องแยกให้ออกว่าค่าไหนระบบเติม ค่าไหนคนแก้** — ไม่งั้นตรวจไม่ได้ว่ามีใครแก้ราคาไปหรือเปล่า (`data-autoval` เทียบตรงๆ)
- **เพิ่มค่า status ใหม่ ต้องไล่ทุกที่ที่เทียบ status** (บทเรียนซ้ำจาก V630 — พลาดหน้าสถานะ)
