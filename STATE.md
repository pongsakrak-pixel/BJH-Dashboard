# STATE — BJH Revenue Dashboard
อัปเดต: 31 ก.ค. 2569 · เวอร์ชันล่าสุด **V627.0**

---

## กติกาการส่งงาน
ส่ง **ไฟล์เดียวต่อรอบ** = `patch_Vxxx.ps1` (ฝัง .html gzip+base64 · verify SHA · เขียน STATE.md ลง repo · push เอง)
ห้ามแนบ .html หรือ STATE.md แยก

---

## รอบนี้ทำอะไร (V627.0) — หน้าราคาอ้างอิง SC/SP · `SalesProspect.html`

### 1. dropdown ยี่ห้อ — top 10 ขึ้นก่อน
เดิมเรียง ก-ฮ ล้วน ต้องเลื่อนหา HOLOGIC ทุกครั้ง
→ นับจำนวนรุ่นที่มีจริงในตาราง แล้วเอา 10 อันดับแรกขึ้นบน · คั่นด้วยเส้น `──────` (option disabled เลือกไม่ได้) · ที่เหลือเรียง ก-ฮ ตามเดิม
**ไม่ hardcode ชื่อยี่ห้อ** — ต่างจาก `TOP_BRANDS_KEYS` ของหน้า Install Base ที่ fix ชื่อไว้ในโค้ด ถ้าสัดส่วนงานเปลี่ยน อันนี้ขยับตามเอง

### 2. ตัดช่วงกลาง P25–P75 ออก
- SC: ลบบรรทัดที่ 3 ในเซลล์ (เหลือ ราคา + "ซ้ำ 6/20 สัญญา")
- SP: ลบทั้งคอลัมน์ `ช่วงกลาง (P25–P75)`
- แก้คำอธิบายใต้ตารางทั้ง 2 ที่ ไม่พูดถึง "ตัดหัวท้าย 25%" อีก
- **CSV export ยังเก็บ p25/p75 ไว้** — ไม่กินที่บนจอ และเผื่อย้อนตรวจ

### 3. ราคาต้องซ้ำเกิน 3 ใบ ถึงแสดง
`_PB_MIN_MODE = 4` (ค่าเดียว คุมทั้ง SC และ SP)
- ไม่ถึงเกณฑ์ → แสดง `—` เหมือนช่องที่ไม่มีข้อมูล
- **ลบ ⚠ ทิ้งทั้งหมด** — ไม่มีของอ่อนเหลือให้เตือนแล้ว
- เดิม SC ใช้ `<3` · SP ใช้ `<2` คนละเกณฑ์กัน → รวมเป็นตัวเดียว
- SP: แถวที่ไม่ถึงเกณฑ์ **ข้ามไปเลย** ไม่แสดงแถว

> **ถ้าอยากได้ "ซ้ำ 3 ใบก็พอ" แก้ `_PB_MIN_MODE` เป็น 3 ที่เดียวจบ**
> ตอนนี้ตีความ "เกิน 3" = ต้องซ้ำ ≥ 4 ใบ

### 4. รวมใบที่ยกเลิกเสมอ
`_PB.cancel` ตั้งต้นเป็น `true` + ถอดปุ่ม `รวมสัญญาที่ยกเลิก` ออก
→ นับทุกใบที่ส่งลูกค้า ไม่มีตัวกรองสถานะอีกต่อไป ทั้ง SC และ SP
เงื่อนไข `if(!inclCancel && _pbDead(...))` **คงไว้ในโค้ด** (ไม่ทำงานแล้ว) เผื่ออยากกลับมาเปิดตัวเลือกภายหลัง

**ตัวกรองที่ยังอยู่ (ตั้งใจ):** `_PB_SKIP_CT` ตัดสัญญาซื้อขาย/เช่าออกจากสถิติ SC — ถ้าเอาออกด้วย ราคาขายเครื่องหลักล้านจะปนกับค่าบริการรายปี ตัวเลขพังทันที

### 5. SP ตัดงานบริการออก
`_PB_SVC_RX = /ค่าบริการ|ค่าแรง|ค่าเดินทาง|ค่าขนส่ง|ค่าตรวจเช็ค|SERVICE CHARGE|LABOR COST/i`
เดียวกับที่ใช้ในแท็บ 4 (V626) — "ค่าบริการสำหรับ Mammo" ไม่ใช่อะไหล่ ห้ามอยู่ในสถิติราคาอะไหล่

---

## ทดสอบ
- `node --check` ผ่านทุก block
- vm **34/34 ผ่าน** — รันโค้ดจริงที่ตัดจากไฟล์ที่ patch แล้ว
  - เกณฑ์: ซ้ำ 4 โชว์ · ซ้ำ 3/2/1 ไม่โชว์ · ไม่มีข้อมูล → `—`
  - เซลล์เหลือ 2 บรรทัด ไม่มี P25–P75 ไม่มี ⚠ · CSV ยังมี p25/p75
  - dropdown: ตัวคั่นเป็น option disabled ไม่มี value (เลือกไม่ได้) · เรียงตามจำนวนจริง · ตัดที่ 10
  - cancel ตั้งต้น true · ปุ่มหายจริง
  - regex: ตัดงานบริการ 5 แบบ · เก็บอะไหล่จริง 5 ตัวครบ
  - สแกนทั้งโซนหน้าราคา (ตัดคอมเมนต์ออก) ไม่มี ⚠ เหลือ
- diff SalesProspect 58 บรรทัด · overrides 4 · บรรทัดสุดท้ายเหมือนเดิม

---

## วิธีลง
```powershell
cd C:\bjh-dashboard
& .\patch_V627.ps1
```
guard: ฐานต้องเป็น V626.0 · SHA ไม่ตรง = revert อัตโนมัติ
ไม่แตะ Code.gs · ไม่แตะ classify → **ไม่ต้องกด Load Data สด**

**จะเห็นแถวหายไปเยอะ** เพราะเกณฑ์ซ้ำเข้มขึ้นจาก 2-3 เป็น 4 — ตั้งใจ ไม่ใช่บั๊ก

---

## ค้างอยู่
**หน้าราคา**
- ยังไม่ได้วัดว่าเกณฑ์ ≥4 ทำให้เหลือกี่แถว — ถ้าหายเยอะเกินไปให้ลดเป็น 3
- `_PB_SKIP_CT` ยังกรองประเภทสัญญาอยู่ (ตั้งใจ) — ถ้าอยากเอาออกบอกได้

**แท็บ 4 อะไหล่ (V626)**
- **ชื่ออะไหล่ซ้ำ** — `Battery 12V/9AH Yuasa (NPW53-12)` 30 เครื่อง กับ `Battery 12V/9Ah` 15 เครื่อง เป็นของชิ้นเดียวกัน สถิติโดนผ่าครึ่ง ต้องมี Config กลุ่มอะไหล่ให้คนจับคู่
- UI ตาม mockup v5 (Top 10 + แถบ % + รายงาน 1 หน้า A4) ยังไม่ทำ
- ปุ่ม "สร้างรายงาน 1 หน้า ส่งลูกค้า" (print/PDF) ยังไม่ทำ

**อื่นๆ**
- Performance: sheets 4.7s + ETL 3.9s
- 6 mobile menus · Renewal.html · M3 SR (blocked) · M8 mobile black screen
- Hardware List ฝั่ง SP (`renderSPDetail`)
- ชิป Sale Order ถ้าขึ้น `(0)` → ไล่ `_spPartsInfo(qn).hasBill`

## ดองไว้ตั้งใจ — ห้ามเสนอซ้ำ
- V610 "อะไหล่รอในคลัง" + "ติดตามสัญญา" — รอ Weekly Report ใช้จริง 2-3 สัปดาห์
- **% โอกาสเสียรายปี** — ข้อมูลมีแค่ 2.6 ปี ซอยรายปีแล้วเหลือ 1 เหตุการณ์/ช่อง · รอครบ 5+ ปี

---

## เกร็ดที่ได้รอบนี้
- **เกณฑ์เดียวกันควรอยู่ที่เดียว** — SC ใช้ `<3` SP ใช้ `<2` มานาน ไม่มีใครสังเกต เพราะกระจายอยู่ 4 จุด · รวมเป็น `_PB_MIN_MODE` แล้วแก้ทีเดียวจบ
- **ตัวคั่นใน `<select>`** ต้องเป็น `<option disabled>` ที่ไม่มี `value` ไม่งั้นผู้ใช้เลือกเส้นคั่นแล้วกรองว่าง
- **ถอดฟีเจอร์ให้ถอดคำอธิบายด้วย** — ลบ ⚠ ออกจากเซลล์แล้วแต่คำอธิบายใต้ตาราง SP ยังพูดถึงอยู่ เทสจับได้ · ครั้งหน้าค้นคำอธิบายทุกที่ก่อนถือว่าจบ
- **regex ตัดงานบริการใช้ร่วมกัน 2 ที่แล้ว** (แท็บ 4 + หน้าราคา) ถ้าเพิ่มคำใหม่ต้องแก้ 2 จุด — ควรยุบเป็นตัวเดียวรอบหน้า
