# STATE — BJH Revenue Dashboard
อัปเดต: 1 ส.ค. 2569 · เวอร์ชันล่าสุด **V630.1**

---

## กติกาการส่งงาน
ส่ง **ไฟล์เดียวต่อรอบ** = `patch_Vxxx.ps1` (ฝัง .html/.js gzip+base64 · verify SHA · เขียน STATE.md ลง repo · push เอง)

---

## รอบนี้ (V630.1) — HOTFIX หลังทดสอบใบจริง SR-2026-0008

**อาการ:** คีย์ใบแรกผ่านเส้นทาง "ราคามาตรฐาน → ออกเลย" สำเร็จ แต่หน้าสถานะขึ้น **"PDF ยังไม่พร้อม"** ทั้งที่ระบบสร้าง PDF ไปแล้ว

**ต้นเหตุ (บั๊กจาก V630.0):**
`loadResultList` เช็ค `r.status==='approved'` เท่านั้น
แต่ `maApiIssueDirect` บันทึกเป็น `status='auto'` → เงื่อนไขไม่ผ่าน ปุ่มดาวน์โหลดเลยไม่ขึ้น
พลอยทำให้ `statusTh('auto')` คืนค่าดิบ `auto` และ `badge auto` ไม่มี CSS

**แก้ 3 จุดใน `MA.html`:**
1. `statusTh` เพิ่ม `auto:'ออกอัตโนมัติ'`
2. หน้าสถานะ: `_done = (status==='approved' || status==='auto')` → ปุ่ม PDF ขึ้นถูก
   · badge ใช้คลาส `approved` (สีเขียว) เมื่อ status เป็น auto
   · ถ้า `_done` แต่ไม่มี `pdf_url` → ขึ้น "สร้าง PDF ไม่สำเร็จ — กดสร้างใหม่ที่หน้าตรวจสอบ" แทนที่จะเงียบ
3. **แก้ข้อความที่เขียนผิดความหมาย** — เดิม `"รับประกัน 3 ปี · ขอ MA 10 ปี"`
   แต่ `ma_years` = **ปีรวมทั้งประกัน** ไม่ใช่จำนวนปี MA
   ใหม่: `"รวม 10 ปี · ประกัน 3 ปี · คิดค่าบริการ 7 ปี"` + โชว์เลขที่ใบเสนอราคาอ้างอิง

---

## ทดสอบ
- `node --check` ผ่าน
- jsdom **17/17 ผ่าน** ด้วยข้อมูลจากใบจริง (SR-2026-0008 · ประกัน 3 · รวม 10)
  - auto + มี pdf_url → ปุ่มดาวน์โหลดขึ้น · approved ยังทำงานเหมือนเดิม · pending ยังขึ้น "ยังไม่พร้อม"
  - auto แต่ PDF ล้มเหลว → บอกให้กดสร้างใหม่ ไม่เงียบ
  - ป้าย "ออกอัตโนมัติ" · ไม่โชว์คำว่า `auto` ดิบ
  - บรรทัดปีคำนวณถูก 10−3=7 · ไม่มีเลขปี → ไม่ NaN
  - ของ V630.0 ไม่ถูกรื้อ (หน้าร่าง · ตาราง Manager · export ครบ)

---

## วิธีลง
```powershell
cd C:\bjh-dashboard
& .\patch_V630_1.ps1
```
guard: ฐานต้องเป็น V630.0 · **ไม่แตะ Code.js รอบนี้** → ไม่ต้อง Deploy New Version ไม่ต้องรัน maSetupSheets ซ้ำ

---

## สถานะ MA After Sales
✅ ทดสอบเส้นทาง "ราคามาตรฐาน → ออกเอง" กับใบจริงแล้ว (SR-2026-0008) — คีย์ผ่าน · PDF สร้างได้
⬜ ยังไม่ทดสอบเส้นทาง "ส่ง Manager" (รุ่นไม่มีราคา / ขอส่วนลด)
⬜ ยังไม่ตรวจว่า PDF ที่ออกมามีตารางเครื่อง `{MACHINES}` ครบถูกต้อง

**เลขที่เอกสารเป็น `SR-YYYY-NNNN`** (มาจาก `maNewRequestId` เดิม) ไม่ใช่ `MA-` ตามที่เคยเข้าใจ

---

## ค้างอยู่ (MA)
- ไฟล์แนบยังเป็น UI เปล่า — `attach_urls` ส่ง `[]`
- ยังไม่ล็อกสิทธิ์แยก Admin/Manager (`tool_ma` เห็นได้ทั้ง admin/manager/sales)
- ราคาที่บันทึกเป็นยอดรวมทุกรุ่นต่อปี ไม่แยกรายรุ่น (ตาราง Prices ไม่มี `row_no`)
- ยังไม่มีฟอร์มสาธารณะให้ Sales กรอกเอง (ตกลงว่า Admin คีย์แทนก่อน)

## ค้างอยู่ (อื่นๆ)
- แท็บ 4 อะไหล่: UI ตาม mockup v5 · ปุ่มรายงาน 1 หน้า A4 · **Battery ชื่อซ้ำ** (Yuasa 30 / ไม่ระบุยี่ห้อ 15 = ชิ้นเดียวกัน)
- Performance: sheets 4.7s + ETL 3.9s
- 6 mobile menus · Renewal.html · M3 SR (blocked) · M8 mobile black screen
- Hardware List ฝั่ง SP · ชิป Sale Order ถ้าขึ้น (0) → ไล่ `_spPartsInfo(qn).hasBill`
- `_PB_SVC_RX` มี 2 ชุด ควรยุบ

## คำขอถึง SmartFlow
1. ใบเสนอราคา — เพิ่ม `BRAND` / `MODEL` / `SN`
2. RAW_CONTRACT สัญญาเหมา — ระบุ SN เครื่องที่คลุม

## ดองไว้ตั้งใจ — ห้ามเสนอซ้ำ
- V610 "อะไหล่รอในคลัง" + "ติดตามสัญญา"
- % โอกาสเสียรายปี — ข้อมูลมีแค่ 2.6 ปี
- ใบเสนอราคาเข้าหน้าราคาอ้างอิง — รอ SmartFlow เพิ่มฟิลด์

---

## เกร็ดที่ได้รอบนี้
- **เพิ่มค่า status ใหม่ ต้องไล่ทุกที่ที่เทียบ status** — V630 เพิ่ม `'auto'` แต่ลืม 3 จุด (statusTh · เงื่อนไขปุ่ม PDF · CSS badge) · เทสรอบ V630 เช็คแต่หน้า Manager ไม่ได้เช็คหน้าสถานะ
- **บทเรียนซ้ำจาก V627/V628** — เทสต้องรันทุกหน้าที่แสดงค่านั้น ไม่ใช่แค่หน้าที่เพิ่งแก้
- **ชื่อฟิลด์ที่ความหมายไม่ตรงชื่อเป็นระเบิดเวลา** — `ma_years` แปลว่า "ปีรวมทั้งประกัน" แต่ชื่อชวนอ่านว่า "จำนวนปี MA" · หน้าเก่าจึงเขียน "ขอ MA 10 ปี" ผิดมานาน · เจอตรงไหนให้แก้ข้อความให้ชัดทันที
