# STATE — BJH Revenue Dashboard
อัปเดต: 2 ส.ค. 2569 · เวอร์ชันล่าสุด **V641.0**

> **patch นี้รวม V637–V640 ที่ยังไม่ได้ลงมาไว้ด้วยแล้ว** (repo ค้างที่ V636.0)
> ไม่ต้องหา patch เก่า รันตัวนี้ตัวเดียวจบ

---

## กติกาการส่งงาน
ส่ง **ไฟล์เดียวต่อรอบ** = `patch_Vxxx.ps1` · guard รับหลาย minor เพราะ patch เขียนไฟล์ทั้งไฟล์
**ถ้า patch เก่ายังไม่ได้ลง ให้ยุบรวมมาไว้ในตัวใหม่** — อย่าปล่อยค้างหลายไฟล์

---

## รอบนี้ (V641.0)

### 🆕 ตั้งราคาสัญญาบริการ (แบบกลุ่ม) — `SalesProspect.html`
**เดิม:** กล่อง 960px กลางจอ · ต้องเลือกแบรนด์ก่อนถึงเห็นอะไร · ไม่รู้ว่าทำไปแล้วกี่แบรนด์

**ใหม่**
- **เต็มจอ** `98vw × 94vh` (เดิม 960px) — ตารางรุ่น × อายุไม่ต้องเลื่อนซ้ายขวา
- **เปิดมาเจอตารางภาพรวมทุกแบรนด์** ว่าแบรนด์ไหนตั้งราคาแล้ว

```
แบรนด์   | เครื่องใน IB | รุ่นทั้งหมด | รุ่นที่มีราคา | กลุ่มราคา | สถานะ
GE       |     18      |      3     |      0       |    0     | ยังไม่ได้ตั้ง
HOLOGIC  |     30      |      4     |      3       |    2     | ตั้งบางส่วน
AGFA     |      9      |      2     |      2       |    1     | ครบทุกรุ่น
```
- **เรียงแบรนด์ที่ยังไม่ได้ตั้งขึ้นก่อน** แล้วเรียงตามจำนวนเครื่องมาก→น้อย — เห็นทันทีว่าควรทำอะไรต่อ
- กดแถว → เข้าจัดการกลุ่มราคาของแบรนด์นั้น · มีปุ่ม **← ทุกแบรนด์** กลับ
- หน้าแบรนด์: ตารางกลุ่มราคา + คอลัมน์ "ปีที่ตั้งราคา" (0 = สร้างกลุ่มแล้วแต่ยังไม่กรอกราคา)
  + กล่องเตือนท้ายตารางว่า **รุ่นไหนยังไม่มีราคา**
- **ค้นหา** แบรนด์ / รุ่น / ชื่อกลุ่ม
- **แถบสรุปมุมขวา** — ภาพรวม: `ตั้งราคาแล้ว 2 / 4 แบรนด์` · รายแบรนด์: `HOLOGIC · มีราคา 3 / 4 รุ่น`
- **dropdown แบรนด์: top 10 ขึ้นก่อน** เรียงตามจำนวนเครื่องใน Install Base · คั่นด้วยเส้น · โชว์จำนวนเครื่องท้ายชื่อ

### รวมของที่ค้างมาด้วย
| | |
|---|---|
| **V637** | ปี dropdown ว่าง (ต้นเหตุ: `loadRequestOptions` อ้าง `#r_ptypes` ที่ถอดไปตั้งแต่ V630) · ป้ายตรงแนว · **แนบไฟล์ขึ้น Drive ใช้ได้จริง** (แตะ Code.js) |
| **V638** | กด "ดู" ใบที่ออกแล้ว เห็นแค่ข้อ 1 ข้อมูลคำขอ + ข้อ 2 เครื่อง (ซ่อนข้อ 3 ราคา / ข้อ 4 ข้อความ) |
| **V639** | หน้าสถานะ & PDF เป็นตาราง + กรอง + ค้นหา |
| **V640** | ถอดช่องขอส่วนลด — เส้นทางเหลือเงื่อนไขเดียว (ทุกรุ่นมีราคาไหม) |

---

## ทดสอบ
- `node --check` ผ่านทั้ง SalesProspect.html · MA.html · Code.js
- jsdom **42/42 ผ่าน**
  - เต็มจอ 98vw/94vh · ไม่เหลือ 960px
  - นับเครื่องต่อแบรนด์ถูก · สรุปกลุ่ม/รุ่นที่ครอบคลุมถูก (HOLOGIC 2 กลุ่ม 3/4 รุ่น)
  - ตารางภาพรวม 4 แบรนด์ · ป้าย 3 สถานะ · ยังไม่ตั้งขึ้นก่อน · ในกลุ่มเดียวกันเรียงตามเครื่อง
  - ค้นหาได้ทั้ง 2 ระดับ · ปุ่มกลับซ่อน/โผล่ถูก
  - หน้าแบรนด์: 2 กลุ่ม · เตือนรุ่น M3 ที่ยังไม่มีราคา · AGFA ครบไม่ขึ้นเตือน · GE ว่างชวนให้เพิ่ม
  - dropdown: เรียงตามจำนวน · ตัดที่ 10 · มีตัวคั่น · โชว์จำนวนเครื่อง
  - ยืนยันว่า V637/638/639/640 ติดมาครบ

---

## วิธีลง
```powershell
cd C:\bjh-dashboard
& .\patch_V641.ps1
```
guard: รับ V636.x–V640.x

> ⚠ **แตะ Code.js (จาก V637) → เช็คว่า Deploy ขึ้นเวอร์ชันใหม่จริง** ไม่งั้นแนบไฟล์จะ error หาฟังก์ชันไม่เจอ

---

## Flow MA (ยืนยันแล้ว)
```
Admin คีย์ → ดูร่าง
   ├── ทุกรุ่นมีราคาในกลุ่มราคา → ออก PDF เลย (status='auto')  🔒 ล็อกทันที
   └── มีรุ่นไม่มีราคา          → Manager เคาะราคา → Approve → PDF  🔒 ล็อก
กด "ดู" ใบที่ออกแล้ว → เห็นแค่ข้อมูลคำขอ + ปุ่มดาวน์โหลด PDF
ต้องแก้ → สร้างคำขอใหม่ (เลขที่ใหม่ ตรวจย้อนได้)
```

---

## 🔴 งานใหญ่ที่ตกลงไว้แล้ว ยังไม่ได้ทำ — แยกราคารายเครื่อง

**Eak สั่ง:** แต่ละเครื่องคิดราคาแยก ไม่รวมยอด · เอกสารออกเป็น **ข้อ 7, 8, 9…** ตามจำนวนเครื่อง

**โครงจริงของกลุ่มราคา**
```
row.plain          → ไม่รวมอะไหล่
row.full           → รวมอะไหล่ (ทุกชิ้น)
row.excl[i]        → { type, label, parts[], price }   ← มีได้หลายชุด
     type==='exclude_except' → "ไม่รวมอะไหล่ยกเว้น <label>"
     อื่นๆ                    → "รวมอะไหล่ยกเว้น <label>"
```

**บั๊กที่ค้าง**
1. MA ผูกตาย 3 คอลัมน์ (`none/partial/full`) หยิบแค่ `excl[0]` → ชุดยกเว้นที่ 2, 3 หายเงียบ
2. หัวคอลัมน์ไม่ตรง — อาจเป็น "**ไม่**รวมอะไหล่ยกเว้น" ขึ้นกับ `type`
3. `รวมทุกชิ้น = 0` ในใบทดสอบ — **ยังไม่ยืนยันสาเหตุ** ต้องรันสคริปต์ดู `_PRICE_GROUPS` กลุ่ม Dimensions 3D
4. `parts[]` คือรายการยกเว้นที่ควรไหลเข้าเอกสารอัตโนมัติ

**ต้องรื้อ:** ชีท `Prices` เพิ่ม `row_no` + `type` เป็น `plain/full/excl0/excl1` · หน้า Manager ตารางแยกรายเครื่อง · PDF วนออกข้อ 7,8,9

**ค้างคำตอบ 1 ข้อ:** รุ่นเดียวกัน qty>1 → ก) ข้อเดียวยอดรวม ข) ข้อเดียว ×N ค) แยกคนละข้อ

---

## ค้างอยู่ (MA)
- ⬜ ยังไม่ทดสอบเส้นทาง "ส่ง Manager" กับใบจริง
- ⬜ ยังไม่ตรวจข้อความ default ในแท็บ Config
- ⬜ ยังไม่ได้ดู PDF ทั้งใบหลัง V633
- ยังไม่ล็อกสิทธิ์แยก Admin/Manager
- **ยังไม่มีปุ่ม "สร้างคำขอใหม่จากใบนี้"** — สำคัญขึ้นหลัง V640 เพราะเป็นทางเดียวที่เหลือในการแก้ราคา
- ไฟล์แนบยังไม่แสดงในหน้า Manager (เก็บ URL แล้ว แต่แถบไฟล์แนบยัง hardcode ชื่อตัวอย่าง)

## ค้างอยู่ (อื่นๆ)
- แท็บ 4 อะไหล่: UI ตาม mockup v5 · ปุ่มรายงาน 1 หน้า A4 · **Battery ชื่อซ้ำ**
- Performance: sheets 4.7s + ETL 3.9s
- 6 mobile menus · Renewal.html · M3 SR (blocked) · M8 mobile black screen
- Hardware List ฝั่ง SP · ชิป Sale Order ถ้าขึ้น (0) → ไล่ `_spPartsInfo(qn).hasBill`
- `_PB_SVC_RX` มี 2 ชุด ควรยุบ
- แท็ก `<div>` เกิน 1 ตัวใน MA.html — ไม่กระทบการแสดงผล

## คำขอถึง SmartFlow
1. ใบเสนอราคา — เพิ่ม `BRAND` / `MODEL` / `SN`
2. RAW_CONTRACT สัญญาเหมา — ระบุ SN เครื่องที่คลุม

## ดองไว้ตั้งใจ — ห้ามเสนอซ้ำ
- V610 "อะไหล่รอในคลัง" + "ติดตามสัญญา" · % โอกาสเสียรายปี · ใบเสนอราคาเข้าหน้าราคาอ้างอิง
- Template 5 Level (V635) · ช่องขอส่วนลดในฟอร์มคำขอ (V640)

---

## เกร็ดที่ได้รอบนี้
- **หน้าจัดการข้อมูลควรเปิดมาเห็นภาพรวมก่อน ไม่ใช่บังคับเลือกก่อน** — เดิมต้องเลือกแบรนด์ถึงเห็นอะไร เลยไม่มีทางรู้ว่าเหลืออีกกี่แบรนด์ต้องทำ
- **เรียง "ของที่ยังไม่ได้ทำ" ขึ้นก่อนเสมอ** ในหน้าที่มีไว้ให้ทำงานให้ครบ — เรียงตามตัวอักษรทำให้ต้องไล่หาเอง
- **ตัวนับความคืบหน้าเปลี่ยนความหมายตามบริบทได้** — ภาพรวมนับแบรนด์ · เข้าไปข้างในนับรุ่น
- **patch ที่ยังไม่ได้ลงต้องยุบรวมกับตัวถัดไป** — รอบนี้ค้าง 4 ตัว (V637-V640) เพราะผมลบไฟล์เก่าทิ้งตอนสร้างตัวใหม่ · ถ้าไม่ยุบรวมจะลงไม่ได้เลย
