# STATE — BJH Revenue Dashboard
อัปเดต: 1 ส.ค. 2569 · เวอร์ชันล่าสุด **V633.0**

---

## กติกาการส่งงาน
ส่ง **ไฟล์เดียวต่อรอบ** = `patch_Vxxx.ps1` · guard รับหลาย minor (`-like 'V63x*'`) เพราะ patch เขียนไฟล์ทั้งไฟล์

---

## รอบนี้ (V633.0) — PDF ข้อ 1

**เดิม (V630):** ตาราง `# / ยี่ห้อ / รุ่น / จำนวน` — ดูหนักเกินไปสำหรับ 2-3 เครื่อง

**ใหม่:** รายการข้อความ
```
1. รายละเอียดเครื่อง
   AAA ประกอบด้วย
     1) HOLOGIC 3Dimensions จำนวน 1 เครื่อง
     2) HOLOGIC 3Dimensions TOMO จำนวน 1 เครื่อง
   รวม 2 เครื่อง — ข้อเสนอนี้ครอบคลุมเฉพาะเครื่องตามรายการข้างต้นเท่านั้น
```

**บรรทัดสรุปท้ายคงไว้** — เป็นตัวจำกัดขอบเขตกรณี Admin คีย์เครื่องไม่ครบ (ตกลงกันตอนออกแบบ flow ว่า
Admin ไม่ใช่เจ้าของ spec จึงต้องให้เอกสารบอกขอบเขตชัดแทนการหาคนรับผิด)
ถ้าไม่ต้องการ → ลบ 2 บรรทัดที่ขึ้นต้นด้วย `machinesHtml += '<div style="font-size:11px;margin-top:2px">รวม '` ใน `maBuildPdfHtml`

แตะไฟล์เดียว: `Code.js` (`maBuildPdfHtml`) · ไม่แตะ MA.html

---

## ทดสอบ
- `node --check` ผ่าน
- vm **18/18 ผ่าน** — ตัดบล็อกสร้าง `machinesHtml` ของจริงมารัน
  - เคสจากภาพจริง 2 เครื่อง: ไม่มี `<table>` · " ประกอบด้วย" · `1)` `2)` · รวม 2 เครื่อง · ประโยคขอบเขตยังอยู่
  - 3 ยี่ห้อ 6 เครื่อง: เรียง 1..3 · คูณจำนวนถูก
  - เคสขอบ: ไม่มีเครื่อง / ไม่มี `machines` / มีแต่ยี่ห้อ / จำนวน 0 (ไม่เขียน "จำนวน 0 เครื่อง") / แถวว่างถูกกรอง / escape HTML
  - `{MACHINES}` ยังทำงาน · ไม่มีแท็ก → ต่อท้ายให้เอง

---

## วิธีลง
```powershell
cd C:\bjh-dashboard
& .\patch_V633.ps1
```
guard: รับ V632.x

> ⚠ **แตะ Code.js → เช็คว่า Deploy ขึ้นเวอร์ชันใหม่จริง**

---

## Flow MA (ยืนยันแล้ว)
```
Admin คีย์  →  ดูร่าง  →  ระบบตัดสิน
                        ├── ราคามาตรฐานครบ + ไม่ขอลด → ออก PDF เลย (status='auto')
                        └── มีรุ่นไม่มีราคา / ขอส่วนลด → Manager
Manager  →  เห็นเฉพาะที่ต้องตัดสิน  →  Approve → PDF
```

### Config 3 แท็บ — ยังต้องมีทั้งหมด
| แท็บ | ใช้ทำอะไร |
|---|---|
| **ข้อความเอกสาร (Default)** | **สำคัญสุด** — เป็นไม้บรรทัดวัดว่าอะไรคือ "มาตรฐาน" (`vFillClauses` เทียบกับค่านี้) · เก็บหัวเอกสาร โลโก้ เพดานปี ตัวเลือกปีประกัน รายการยกเว้น |
| ราคา Template 5 Level | สำรอง — ปุ่ม "↓ ดึงจาก Template" ตอนเจอรุ่นที่ไม่มีในกลุ่มราคา |
| ฐานข้อมูลรุ่นเครื่อง | เติม `spec_pulled` ลงชีท Machines · dropdown ยี่ห้อ/รุ่นดึงจากกลุ่มราคาแล้ว ไม่ใช่ตรงนี้ |

---

## ค้างอยู่ (MA)
- ⬜ ยังไม่ทดสอบเส้นทาง "ส่ง Manager" กับใบจริง (รุ่นไม่มีราคา / ขอส่วนลด)
- ⬜ ยังไม่ตรวจข้อความ default ในแท็บ Config ว่าตรงกับที่ใช้จริง (ทุกใบใช้ชุดนี้)
- ⬜ ยังไม่ได้ดู PDF หลัง V632/V633 ว่าหน้าตาโอเคทั้งใบ
- ไฟล์แนบยังเป็น UI เปล่า — `attach_urls` ส่ง `[]`
- ยังไม่ล็อกสิทธิ์แยก Admin/Manager (`tool_ma` เห็นได้ทั้ง admin/manager/sales)
- ราคาบันทึกเป็นยอดรวมทุกรุ่นต่อปี ไม่แยกรายรุ่น
- เสนอไว้ยังไม่ทำ: เรียงแท็บ Config ใหม่ให้ข้อความเอกสารขึ้นก่อน

## ค้างอยู่ (อื่นๆ)
- แท็บ 4 อะไหล่: UI ตาม mockup v5 · ปุ่มรายงาน 1 หน้า A4 · **Battery ชื่อซ้ำ** (Yuasa 30 / ไม่ระบุยี่ห้อ 15 = ชิ้นเดียวกัน)
- Performance: sheets 4.7s + ETL 3.9s
- 6 mobile menus · Renewal.html · M3 SR (blocked) · M8 mobile black screen
- Hardware List ฝั่ง SP · ชิป Sale Order ถ้าขึ้น (0) → ไล่ `_spPartsInfo(qn).hasBill`
- `_PB_SVC_RX` มี 2 ชุด ควรยุบ

## คำขอถึง SmartFlow
1. ใบเสนอราคา — เพิ่ม `BRAND` / `MODEL` / `SN`
2. RAW_CONTRACT สัญญาเหมา — ระบุ SN เครื่องที่คลุม

## ดองไว้ตั้งใจ — ห้ามเสนอซ้ำ
- V610 "อะไหล่รอในคลัง" + "ติดตามสัญญา"
- % โอกาสเสียรายปี — ข้อมูลมีแค่ 2.6 ปี
- ใบเสนอราคาเข้าหน้าราคาอ้างอิง — รอ SmartFlow เพิ่มฟิลด์

---

## เกร็ดที่ได้รอบนี้
- **ตารางไม่ได้ดีกว่าข้อความเสมอไป** — 2-3 รายการใช้ข้อความอ่านลื่นกว่า · ตารางเริ่มคุ้มตอน 4-5 แถวขึ้นไป หรือเมื่อต้องเทียบค่าข้ามคอลัมน์
- **แยกให้ออกระหว่าง "รูปแบบ" กับ "เนื้อหาที่ต้องมี"** — Eak ติดที่ตาราง ไม่ได้ติดที่การระบุเครื่อง · เปลี่ยนรูปแบบได้ แต่ประโยคจำกัดขอบเขตต้องอยู่เพราะเป็นเหตุผลที่ใส่รายการเครื่องตั้งแต่แรก
