# STATE — BJH Revenue Dashboard
อัปเดต: 3 ส.ค. 2569 · เวอร์ชันล่าสุด **V645.0**

---

## กติกาการส่งงาน
ส่ง **ไฟล์เดียวต่อรอบ** = `patch_Vxxx.ps1` · guard รับหลาย minor
**สะสมให้ครบ 3 เรื่องก่อนเขียนโค้ด** (รอบนี้ Eak สั่ง "จัดมาเลย" จึงยิงเดี่ยว)
patch ที่ยังไม่ได้ลง ให้ยุบรวมกับตัวถัดไป
**ไฟล์ .txt / .log ที่แนบมา มาถึงเป็นค่าว่างเสมอ** — ให้วางเป็นข้อความ หรือส่ง .csv / .xlsx / .pdf / .docx (พวกนี้อ่านได้ปกติ)

---

## รอบนี้ (V645.0) — คอลัมน์ BILL / DELIVERY ล้น

**เดิม** `High 🔄 เม.ย. 2026 → ส.ค. 2026` → กว้างเกินคอลัมน์ ตัวเลขเงินโดนดันตก
**ใหม่** `High 🔄 เม.ย. 2026`

**เหตุผลที่ตัดเดือนปลายทาง:** เป็นเดือนที่กรองอยู่ ทุกแถวซ้ำกันหมด ไม่ให้ข้อมูลเพิ่ม
ส่วนเดือนต้นทางเป็นตัวที่ต่างกันจริงในแต่ละแถว จึงเก็บไว้

- **ย้ายเดือนปลายทางไปอยู่ใน tooltip** — `เลื่อนอัตโนมัติ · เม.ย. 2026 → ส.ค. 2026` ข้อมูลไม่หาย
- tooltip ติดทั้งไอคอนและตัวเลข เอาเมาส์ชี้ตรงไหนก็เห็น
- **ไม่มีเดือนต้นทาง → แสดงเดือนปลายทางแทน** ไม่ปล่อยว่าง
- แถวที่ไม่ได้เลื่อนแสดงเดือนเดียวเหมือนเดิม ไม่กระทบ
- แก้ 2 บล็อกใน `script_main.html` — High/Medium และ Low
- **ไม่แตะ** ส่วนอื่นที่มีลูกศร (overdue · carry · SC backlog)

---

## ทดสอบ
- `node --check` ผ่าน
- vm **21/21 ผ่าน**
  - เลื่อนอัตโนมัติ → โชว์เดือนต้นทาง · ไม่มีลูกศร · ไอคอน 🔄 ยังอยู่ · tooltip มี 2 เดือน
  - ย้ายเอง → ไอคอน ✋ สีเขียว · tooltip บอก "ย้ายเอง"
  - ไม่มีเดือนต้นทาง → ถอยไปโชว์ปลายทาง
  - ไม่ได้เลื่อน → เหมือนเดิมทุกอย่าง (สี `--ac2`)
  - ไม่มีเดือนเลย → คืนค่าว่าง ไม่พัง
  - `_tip` / `_lTip` ประกาศก่อนใช้ (บทเรียนจาก V643 ที่ `var L` อ่าน `_lang` ก่อนประกาศ)

---

## วิธีลง
```powershell
cd C:\bjh-dashboard
& .\patch_V645.ps1
```
guard: รับ V644.x · **ไม่แตะ Code.js** → ไม่ต้อง Deploy New Version

---

## สถานะระบบ (หลัง V644 ลงแล้ว)
- **Tools → ตั้งราคาสัญญาบริการ**: ปุ่ม 📈 Analysis · ฝัง Incident 124 รุ่น · คอลัมน์เหลือ "ไม่รวมอะไหล่" + ปุ่ม + ยกเว้น · เรียงแบรนด์ที่ตั้งแล้วขึ้นบน
- **MA After Sales**: Admin คีย์ → ดูร่าง → ราคามาตรฐานออกเอง / ไม่ครบส่ง Manager · ใบที่ออกแล้วล็อก · เอกสาร 2 ภาษา · รายการอะไหล่ยกเว้นเป็น bullet ใน PDF

---

## ผลวิเคราะห์ราคา (Excel `service_pricing_model.xlsx`)
```
ราคา = Incident/ปี × ค่าแรงต่อ Incident ÷ (1 − margin)   ·   Incident = PM + CM
ต้นทุน/Incident 9,700 = ช่าง 2 วัน × 3,000 + ที่พัก 700 + เดินทาง 3,000
PM เฉลี่ย 1.9 (96% อยู่ 1–3) · CM เฉลี่ย 0.5 ครั้ง/เครื่อง/ปี
margin 50% → 42 รุ่น · 395 เครื่องต่ำกว่าฐาน → +8.1 ล้าน/ปี
```
**ราคาใหม่ = สูงกว่าระหว่าง [ราคาปัจจุบัน] กับ [ราคาฐาน] — ไม่ลดราคาใคร**
**ผู้บริหารเคาะ 2 ตัว** — ค่าแรงต่อ Incident · margin

---

## ค้างอยู่
**ตั้งราคากลุ่ม**
- ตั้งไปแล้ว **1 / 154 แบรนด์** — ทำ 18 กลุ่มที่คุมหลายรุ่นก่อน (350+ เครื่อง) แล้วต่อรุ่นเดี่ยวที่เครื่อง > 20
  (ชีท "กลุ่มที่แนะนำ" ใน Excel บอกวิธีจัดกลุ่มไว้แล้ว)
- ยังไม่มีปุ่มเลือกประเภทตอนเพิ่มคอลัมน์ (รวมยกเว้น / ไม่รวมยกเว้น + บังคับเลือก parts)

**MA After Sales**
- ⬜ ยังไม่ทดสอบเส้นทาง "ส่ง Manager" กับใบจริง
- ⬜ ยังไม่กรอกข้อความ EN ลง Config (แปลไว้ให้แล้วในแชท + ไฟล์ `MA_service_proposal_template.docx`)
- ⬜ ยังไม่ตรวจข้อความ default ใน Config
- หัวกระดาษยังใช้ `companyName` จาก Config — อยากได้ **BJCHealthcare** แก้ใน Config ได้เลย ไม่ต้อง patch
- ยังไม่ล็อกสิทธิ์แยก Admin/Manager · ยังไม่มีปุ่ม "สร้างคำขอใหม่จากใบนี้"
- ไฟล์แนบยังไม่แสดงในหน้า Manager
- **text layer ใน PDF เพี้ยน** — ก็อปข้อความออกมาได้ `ปีที่ปีที่` `เป็นป็` (หน้าจอแสดงถูก) จากการฝังฟอนต์ไทย

**อื่นๆ**
- แท็บ 4 อะไหล่: UI ตาม mockup v5 · ปุ่มรายงาน 1 หน้า A4 · Battery ชื่อซ้ำ
- Performance: sheets 4.7s + ETL 3.9s · 6 mobile menus · Renewal.html · M3 SR (blocked) · M8 mobile black screen

## คำขอถึง SmartFlow
1. ใบเสนอราคา — เพิ่ม `BRAND` / `MODEL` / `SN`
2. RAW_CONTRACT สัญญาเหมา — ระบุ SN เครื่องที่คลุม
3. จำนวน PM ที่ระบุในสัญญา — จะได้เทียบกับที่ไปจริง

## ดองไว้ตั้งใจ — ห้ามเสนอซ้ำ
- V610 "อะไหล่รอในคลัง" + "ติดตามสัญญา" · % โอกาสเสียรายปี · ใบเสนอราคาเข้าหน้าราคาอ้างอิง
- Template 5 Level (V635) · ช่องขอส่วนลดในฟอร์มคำขอ (V640) · คอลัมน์ "รวมอะไหล่" ในตัวแก้ไขกลุ่ม (V644)

---

## เกร็ดที่ได้รอบนี้
- **ข้อมูลที่ซ้ำกันทุกแถวไม่ต้องแสดง** — เดือนปลายทาง = เดือนที่กรองอยู่ ใส่ไปก็เปลืองที่เปล่า
- **ตัดของออกจากหน้าจอ ให้ย้ายไป tooltip ก่อนลบทิ้ง** — ข้อมูลไม่หาย คนที่ต้องการยังหาเจอ
- **fallback ทุกครั้งที่ตัดของ** — ถ้าไม่มีเดือนต้นทางแล้วลบปลายทางด้วย ช่องจะว่างเปล่า
