# STATE — BJH Revenue Dashboard
อัปเดต: 3 ส.ค. 2569 · เวอร์ชันล่าสุด **V644.0**

> **รวม V642 + V643 ที่ยังไม่ได้ลงมาด้วยแล้ว** (repo ค้างที่ V641.0) — รันตัวเดียวจบ

---

## กติกาการส่งงาน
ส่ง **ไฟล์เดียวต่อรอบ** = `patch_Vxxx.ps1` · guard รับหลาย minor
**สะสมให้ครบ 3 เรื่องก่อนเขียนโค้ด** · patch ที่ยังไม่ได้ลง ให้ยุบรวมกับตัวถัดไป
**ไฟล์ .txt / .log ที่แนบมา มาถึงเป็นค่าว่างเสมอ** — ให้วางเป็นข้อความ หรือส่ง .csv / .xlsx / .pdf / .docx แทน (พวกนี้อ่านได้ปกติ)

---

## รอบนี้ (V644.0)

### 1. ถอดคอลัมน์ "รวมอะไหล่" ออกจากตัวแก้ไขกลุ่มราคา
เสนอจริงไม่ได้ · ทุกกลุ่มเป็น 0 ทั้งแถว
- ตัดหัวตาราง + ช่องกรอกออก · ปีที่ 1 ไม่ต้อง `colspan=2` แล้ว
- **ค่า `full` เดิมที่บันทึกไว้ยังอยู่ในข้อมูล ไม่ได้ลบ** แค่ไม่แสดง/แก้ไม่ได้
- รูปแบบ "รวมอะไหล่" ให้ใช้ปุ่ม **+ ยกเว้น** แทน จะได้ระบุชิ้นที่ยกเว้นไปด้วย

### 2. PDF — รายการอะไหล่ยกเว้นเป็น bullet
เดิมต่อท้ายหัวข้อในวงเล็บ `แบบรวมอะไหล่ (ยกเว้น A, B, C)` → ยาว อ่านยาก
ใหม่:
```
7.2 แบบรวมอะไหล่
    รายการอะไหล่ที่ไม่รวมในสัญญา (คิดค่าใช้จ่ายตามจริงเมื่อต้องเปลี่ยน)
      • Generator
      • หลอดเอกซเรย์ (X-Ray Tube)
      • Detector
    ปีที่ 1   ในประกัน
    ปีที่ 2   ปีละ 1,650,000 บาท
```
รองรับ 2 ภาษา · ไม่มีรายการยกเว้น → ไม่แสดงอะไร

### 3. [V643] เอกสาร MA 2 ภาษา
- Config เพิ่มช่อง EN 9 ช่อง (`titleMain_en` · `clause1_en`–`clause6_en` · `notes_en` · `contact_en`)
- Admin เลือกภาษาตอนสร้างคำขอ (`r_lang`) · เก็บเป็น `doc_lang`
- PDF ใช้ `pickL()` — `ov_xxx` → `clauseN_en` → `clauseN` · **EN ว่างถอยไปใช้ไทย**
- ไม่ต้องแก้ฝั่ง server สำหรับคีย์ใหม่ (`maApiSaveSettings` เขียนทุกคีย์ที่ส่งมา)

### 4. [V642] Tools → ตั้งราคาสัญญาบริการ
- **ปุ่ม 📈 Analysis** — ค่าแรงต่อ Incident + margin 3 ระดับ · เทียบราคาที่ตั้งไว้กับราคาฐาน
- **ฝัง Incident จริง** 29 แบรนด์ · 124 รุ่น · **1 ส.ค. 2568 – 31 ก.ค. 2569** · 1,305 เครื่อง
  นับ SR ไม่ซ้ำต่อเครื่อง (ไม่นับแถว — 1 SR ≈ 2.1 แถวเพราะช่างหลายคน) · แยก SN ที่รวมกัน (4%)
  เฉลี่ยกลุ่ม = **ถ่วงน้ำหนักด้วยจำนวนเครื่อง ปัดขึ้น** · เตือนเมื่อ PM/CM ในกลุ่มต่างกันเกิน 2
- ไม่เก็บราคา 0 ตอนบันทึก (ยกเว้นปีที่ 1) · เรียงแบรนด์ที่ตั้งราคาแล้วขึ้นบน

---

## ทดสอบ
- `node --check` ผ่านทั้ง 3 ไฟล์
- **V644: 25/25** · V643 ภาษา: 28/28 · V642: 42/42
- บั๊กที่เทสจับได้ระหว่างทาง: `var L` (ป้าย 2 ภาษา) ถูกประกาศก่อน `_lang` → ตารางเครื่องเป็นไทยเสมอแม้เลือก EN · แก้ลำดับแล้ว

---

## วิธีลง
```powershell
cd C:\bjh-dashboard
& .\patch_V644.ps1
```
guard: รับ V641.x–V643.x

> ⚠ **แตะ Code.js → เช็คว่า Deploy ขึ้นเวอร์ชันใหม่จริง**

---

## เอกสาร Template (ไฟล์แยก outputs)
`MA_service_proposal_template.docx` — ไทย + English ตามรูปแบบจริง (BJCHealthcare)
โครง: หัวเรื่อง 3 บรรทัด → ข้อ 1–7 → ราคาเป็นรายการ 7.1/7.2 → หมายเหตุ → เบอร์โทร
**PDF ในระบบทำรูปแบบนี้อยู่แล้ว** (`prow`/`py`/`pv`) — V644 เพิ่มแค่ bullet รายการยกเว้น

**ยังไม่ได้ทำ:** หัวกระดาษยังใช้ค่าจาก Config (`companyName`) — ถ้าอยากได้ BJCHealthcare ให้แก้ใน Config เอง

---

## ผลวิเคราะห์ราคา (Excel แยกไฟล์ `service_pricing_model.xlsx`)
```
สูตร: ราคา = Incident/ปี × ค่าแรงต่อ Incident ÷ (1 − margin)   ·   Incident = PM + CM
ต้นทุน/Incident 9,700 = ช่าง 2 วัน × 3,000 + ที่พัก 700 + เดินทาง 3,000
PM เฉลี่ย 1.9 (96% อยู่ที่ 1–3) · CM เฉลี่ย 0.5 ครั้ง/เครื่อง/ปี
ที่ margin 50% → 42 รุ่น · 395 เครื่อง ต่ำกว่าฐาน → +8.1 ล้าน/ปี
```
**ราคาใหม่ = สูงกว่าระหว่าง [ราคาปัจจุบัน] กับ [ราคาฐาน] — ไม่ลดราคาใคร**
**ผู้บริหารต้องเคาะ 2 ตัว** — ค่าแรงต่อ Incident และ margin
(3,000 → ไม่ต้องขึ้นราคาใครเลย · 9,700 → ได้เพิ่ม 8.1 ล้าน)

---

## ค้างอยู่
**ตั้งราคากลุ่ม**
- ตั้งไปแล้ว **1 / 154 แบรนด์** — ทำ 18 กลุ่มที่คุมหลายรุ่นก่อน (350+ เครื่อง) แล้วต่อด้วยรุ่นเดี่ยวที่เครื่อง > 20
  (ชีท "กลุ่มที่แนะนำ" ใน Excel บอกไว้แล้วว่าจัดกลุ่มยังไง)
- ยังไม่มีปุ่มเลือกประเภทตอนเพิ่มคอลัมน์ (รวมอะไหล่ยกเว้น / ไม่รวมอะไหล่ยกเว้น + บังคับเลือก parts)

**MA After Sales**
- ⬜ ยังไม่ทดสอบเส้นทาง "ส่ง Manager" กับใบจริง
- ⬜ ยังไม่ได้กรอกข้อความ EN ลง Config (แปลไว้ให้แล้วในแชท)
- ⬜ ยังไม่ตรวจข้อความ default ใน Config
- ยังไม่ล็อกสิทธิ์แยก Admin/Manager · ยังไม่มีปุ่ม "สร้างคำขอใหม่จากใบนี้"
- ไฟล์แนบยังไม่แสดงในหน้า Manager
- **text layer ใน PDF เพี้ยน** — ก็อปข้อความออกมาได้ `ปีที่ปีที่` `เป็นป็` (หน้าจอแสดงถูก) เกิดจากการฝังฟอนต์ไทย

**อื่นๆ**
- แท็บ 4 อะไหล่: UI ตาม mockup v5 · ปุ่มรายงาน 1 หน้า A4 · Battery ชื่อซ้ำ
- Performance: sheets 4.7s + ETL 3.9s · 6 mobile menus · Renewal.html · M3 SR (blocked) · M8 mobile black screen

## คำขอถึง SmartFlow
1. ใบเสนอราคา — เพิ่ม `BRAND` / `MODEL` / `SN`
2. RAW_CONTRACT สัญญาเหมา — ระบุ SN เครื่องที่คลุม
3. จำนวน PM ที่ระบุในสัญญา — จะได้เทียบกับที่ไปจริง

## ดองไว้ตั้งใจ — ห้ามเสนอซ้ำ
- V610 "อะไหล่รอในคลัง" + "ติดตามสัญญา" · % โอกาสเสียรายปี · ใบเสนอราคาเข้าหน้าราคาอ้างอิง
- Template 5 Level (V635) · ช่องขอส่วนลดในฟอร์มคำขอ (V640)
- **คอลัมน์ "รวมอะไหล่" ในตัวแก้ไขกลุ่มราคา (V644)** — ใช้ "+ ยกเว้น" แทน

---

## เกร็ดที่ได้รอบนี้
- **ก่อนออกแบบใหม่ ให้ดูของจริงก่อนเสมอ** — ทำ mockup ราคาเป็นตาราง ทั้งที่ระบบทำเป็นรายการ 7.1/7.2 อยู่แล้วและตรงกับเอกสารจริง · เสียเวลาไปหนึ่งรอบเพราะไม่ได้ขอ PDF ตัวอย่างก่อน
- **ถอด UI แต่เก็บข้อมูลไว้** — ซ่อนคอลัมน์ `full` โดยไม่ลบค่าที่บันทึกไว้ กลับมาเปิดได้ถ้าคิดผิด
- **รายการยาวๆ ในเอกสารให้เป็น bullet ไม่ใช่วงเล็บ** — วงเล็บต่อท้ายหัวข้อยาวจนลูกค้ามองข้าม
