# STATE — BJH Revenue Dashboard
อัปเดต: 5 ส.ค. 2569 · เวอร์ชันล่าสุด **V670.3**

---

## กติกาการส่งงาน
ส่ง **ไฟล์เดียวต่อรอบ** = `patch_Vxxx.ps1` · guard รับหลาย minor
**สะสมให้ครบ 3 เรื่องก่อนเขียนโค้ด** · patch ที่ยังไม่ได้ลง ให้ยุบรวมกับตัวถัดไป
**STATE.md ไม่ต้องส่งเป็นไฟล์แยก** — ฝังใน `.ps1` เขียนทับ + `git push` ให้เอง

### 🔴 กฎที่ต้องเช็คทุกครั้ง
**`overrides.html` โหลดทีหลัง `script_main.html` จึงทับเสมอ**
**ก่อนแก้ฟังก์ชันใดๆ ให้ `grep` ชื่อฟังก์ชัน/ตัวแปรใน `overrides.html` ก่อน**

**ไฟล์ .txt / .log ที่แนบมา มาถึงเป็นค่าว่างเสมอ** — ให้วางเป็นข้อความ หรือส่ง .csv / .xlsx / .pdf / .docx

---

## รอบนี้ (V670.3) — permission ครบทุกแท็บ Sales Prospect

ตรวจแท็บจริง 13 อันเทียบกับระบบสิทธิ์ เจอว่า **`SR Commercial` ไม่เคยถูกคุมสิทธิ์เลย** ตกไป 3 จุดพร้อมกัน:

| จุด | อาการเดิม |
|---|---|
| `TABS` ใน `cfgTabPermInit` | ไม่มีรายการ -> แอดมินติ๊กไม่ได้ |
| `map` ใน `spApplyTabPermissions` | มีใน `DEF` แต่ตกจาก `map` -> **ปุ่มไม่เคยถูกซ่อน ทุกคนเห็นหมด** |
| `_TAB_PERM_KEYS` | ไม่มี -> เซฟลงชีต `_config_kv` ไม่ได้ |
| `_TAB_PERM_DEFAULT` / `NEWKEYS` | ไม่มี -> ถ้าเพิ่ม key เฉยๆ ทุกคนจะมองไม่เห็นแท็บทันทีตอนอัป |

เติมครบทั้ง 4 จุด default `all-true` (เท่ากับพฤติกรรมเดิมที่ทุกคนเห็น — เปลี่ยนทีหลังได้ที่ Config)

**แก้ชื่อใน Config ให้ตรงกับชื่อบนแท็บ** (เดิมคนละชื่อ อ่านแล้วไม่รู้ว่าคุมอันไหน)
```
ติดตามสั่ง SP    -> SP Monitor
ติดตามเข้า SC    -> SC Monitor
ติดตามเปิดบิล    -> Bill Monitor
Installation Base -> Install Base
```
และเรียงลำดับใน Config ให้ตรงกับลำดับแท็บจริงในหน้า

### ทดสอบ
`node --check` 3 ไฟล์ · jsdom **18/18**
**เทียบอัตโนมัติ**: ดึง `id="sp-tab-*"` ทั้งหมดจากไฟล์จริง แล้วยืนยันว่าทุกตัวมีทั้งใน `map` และใน `TABS` ของ Config — เทสนี้จะจับได้เองถ้าเพิ่มแท็บใหม่แล้วลืมคุมสิทธิ์อีก
เรนเดอร์จริง: ปิด `sp_srcom` -> ปุ่มซ่อน · เปิด -> โชว์ · อยู่แท็บที่ถูกปิด -> เด้งไปแท็บแรกที่เห็นได้ · แยกตาม role (manager เห็น / sales ไม่เห็น)

---

## รอบก่อน (V670.1 + V670.2) — ป้าย (Px) และ Nชิ้น ย้ายมาอยู่หลังเลขที่ใบเสนอราคา

รวม 2 เวอร์ชันย่อยเป็นแพตช์เดียว (V670.1 ยังไม่ได้ลง)

### V670.1 — ย้าย (Px) จากท้ายยี่ห้อ ไปหลัง QN
เดิม `HOLOGIC P9` อ่านเหมือน `P9` เป็นรุ่นหรือส่วนหนึ่งของยี่ห้อ
และไม่เข้าชุดกับป้าย `Nชิ้น` ของ QSP ที่อยู่หลัง QN อยู่แล้ว
ย้ายพร้อมกัน **ตารางหลัก** (`renderTbl`) + **SC Monitor** — ตารางหลักเก็บค่าไว้ใน `_pkgB` ตอนสร้างเซลล์ยี่ห้อ แล้วไปแปะที่เซลล์ QN
เกณฑ์ไม่เปลี่ยน: `_pkgBadge` ขึ้นเฉพาะ `pkg > 1` และยังกดเปิดรายการเครื่องได้

### V670.2 — เติม Nชิ้น ให้ 3 หน้า monitor
`Nชิ้น` (จำนวนอะไหล่ในใบ) เดิมมีแค่ตารางหลัก ทั้งที่ **SP / SC / Bill Monitor** ใช้ `window.D` ชุดเดียวกันซึ่งมี `r.nc` อยู่แล้ว (`nc = parts.length`)

เซลล์ QN ตอนนี้เป็นชุดเดียวกันทุกหน้า:
```
เลขใบ (ลิงก์ SmartFlow) + (Px) ถ้าเป็น package + Nชิ้น ถ้ามีอะไหล่
```
เซลล์ QN ทุกหน้าเป็น `nowrap` กันป้ายตกบรรทัด

### ทดสอบ
`node --check` 3 ไฟล์ · jsdom **15/15 + 11/11**
`P9` อยู่หลัง QN และกดได้ (`selRow`) · QN ยังเป็นลิงก์ · `pkg=1` ไม่ขึ้นป้าย · `bm` ว่างไม่ throw ·
`nc=1` ขึ้นป้าย · `nc=0`/`nc=null` ไม่ขึ้นและไม่ throw · เคสมีทั้งคู่ต้องเรียง `QN -> P9 -> 2ชิ้น` ·
ไม่มีป้ายค้างที่เซลล์ยี่ห้อทั้ง 2 หน้า · ทั้ง 3 จุดมี guard `Number(r.nc||0)>0` เหมือนกัน

⚠️ เทสรอบแรกตกเพราะ regex `/P\d/` ไป match `P2511` ใน `QSP2511-014853` — ต้องเช็ค tooltip `Package` แทน

---

## รอบก่อน (V670.0) — Weekly Report: PO อะไหล่ + สถานะสัญญา

ผู้ใช้ขอ 5 ตัวเลขราย sales ราย week — 3 ตัวมีอยู่แล้ว (New QSC/QSP · เปิดบิล · สัญญาใหม่) เพิ่มใหม่ 2 ตัว

### 1. 🛒 PO อะไหล่ — ทำได้ทันที ไม่ต้องพึ่ง snapshot
นิยาม (ยืนยันกับผู้ใช้แล้ว): **ใบ QSP ที่ลูกค้าออก PO ให้ในสัปดาห์นั้น**
`STATUS_NAME = 'PO Completed'` + เกาะวัน `CUSTOMER_ACCEPT_DATETIME`

ตรวจกับข้อมูลจริงก่อนเขียน: QSP `PO Completed` 2,322 แถว มี `CUSTOMER_ACCEPT_DATETIME` **ครบ 100%**
(ฝั่ง QSC ไม่ครบ — 1,108 แถวมี accept แค่ 1,070 จึงยังไม่ทำ)

⚠️ **ต้อง dedupe ตาม `DOC_NUMBER`** — `_jsupQuo` มีหลายแถวต่อ 1 ใบ (`QSP2605-016186` โผล่ 3 แถวติดกัน)
ถ้าไม่ตัดจะนับเกินราว 3 เท่าและยอดเงินคูณตาม

### 2. 📄 สถานะสัญญา — ต้องทำ snapshot ใหม่
`RAW_CONTRACT` **ไม่มีวันที่เปลี่ยนสถานะเลย** มีแค่ `CONTRACT_START_DATE` / `CONTRACT_END_DATE`
-> ตอบได้แค่ "ตอนนี้ค้างกี่งาน" ซึ่งเลขเท่าเดิมทุกสัปดาห์ ตอบ "สัปดาห์นี้ปิดไปกี่งาน" ไม่ได้

เพิ่ม 2 ชีตคู่กับของเดิม เก็บวันละครั้ง (โครงเดียวกับ `snap_state` / `daily_change` เป๊ะ):
```
contract_state   1 แถว = 1 CON_ID  — สถานะปัจจุบัน · sales · มูลค่า · วันเข้าสถานะนี้ · เห็นล่าสุด
contract_change  1 แถว = 1 การขยับ — วัน · kind(new|moved|gone) · จาก -> เป็น · sales · มูลค่า
```
`saveContractSnapshot()` + `getContractChanges()` ใน `Code.js` · ตัวยิงคือ `window.bjhContractSnapshot()`
หน่วง 6 วิ (ตัวเดิม 3 วิ) กันยิงชนกัน · ล้มแล้วไม่กระทบ snapshot ตัวแรก

**นิยาม "ปิดงาน" (ยืนยันกับผู้ใช้แล้ว):** `Contract Completed` **หรือ** `PO Completed` เท่านั้น
`รับคู่สัญญาเรียบร้อย` และ `รอลงงาน` = **ยังค้าง** · `ยกเลิกสัญญา` = ไม่นับทั้งสองฝั่ง

sales ของสัญญา: `SALES_NAME_DT` (95%) -> ว่างค่อย fallback หาจาก QN ในใบเสนอราคา

### ตารางใน Weekly เพิ่ม 2 แถว
```
🛒 PO อะไหล่     ลูกค้าออก PO ให้ (ใบ QSP)              จำนวนใบ + มูลค่า
📄 สถานะสัญญา    ขยับสถานะ · ปิดได้ N งาน · ค้างอยู่ M
```
ก่อนมีข้อมูลเทียบ (วันแรก) แถวสัญญาจะแสดง **ยอด ณ ปัจจุบัน** พร้อมข้อความ "เริ่มเก็บแล้ว ยังไม่มีวันเทียบ" แทนที่จะโชว์ 0

### ข้อจำกัดที่ยอมรับแล้ว
- ย้อนหลังก่อนวันติดตั้งไม่ได้เลย · ต้องเก็บอย่างน้อย 2 วันถึงเริ่มมีรายการ "ขยับ"
- **ต้องรัน `repairSnapshotDates()` จาก V669.0 ก่อน** ไม่งั้นระบบ snapshot ทั้งชุดยังอ่านไม่ออก

### ทดสอบ
`node --check` 4 ไฟล์ · jsdom + vm **31/31**
จำลอง Sheets ทั้ง object แล้วรัน `saveContractSnapshot` จริง 5 วัน-เคส: วันแรกทุกอย่างเป็น `new` · วันสองแยก `moved` กับ `new` ถูก · สัญญาที่ไม่ขยับต้อง**คง `sinceDate` เดิม** ไม่รีเซ็ต · `daysInPrev` · เปิดหน้าเว็บซ้ำวันเดิมต้อง `skipped` · **`lastSeen` เป็น Date object ก็ยัง skip ถูก** (บั๊ก V669 ต้องไม่เกิดซ้ำ) · สัญญาหายไป -> `gone`
ฝั่งนับ: dedupe QN 3 แถว = 1 ใบ และยอดเงินไม่คูณ 3 · ตัดใบนอกสัปดาห์/ไม่ใช่ PO/QSC · `รับคู่สัญญาเรียบร้อย`+`รอลงงาน` ต้องเป็นค้าง · `ยกเลิกสัญญา` ไม่โผล่ทั้งสองฝั่ง

---

## รอบก่อน (V669.2) — ลิงก์ SmartFlow ที่ SC Monitor + Bill Monitor

V666 เติมลิงก์ให้หลายหน้าแต่ **ตก 2 ตาราง** — SC Monitor และ Bill Monitor ยังโชว์เลขใบเสนอราคาเป็นข้อความเปล่า
ทั้งสองหน้าใช้ `window.D` ชุดเดียวกับที่หน้าอื่นใช้ (มี `r.url` ครบอยู่แล้ว) แค่ไม่ได้ห่อด้วย `_sfLink`

ตอนนี้ทุกตารางที่โชว์ QN ใช้ตัวกลางเดียวกันครบ **5 จุด**: Contract Monitor · SP Ordering · SP Monitor · SC Monitor · Bill Monitor
ใบที่ไม่มี URL ยังแสดงเลขเฉยๆ ไม่ทำลิงก์ตาย

**หมายเหตุที่เคยเข้าใจผิด:** เส้นใต้ที่เห็นบางแถวใน SC Monitor ไม่ใช่ลิงก์ — เป็นเส้นของ `_qnCellHtml` (แถวที่มีโน้ต) จึงดูเหมือนบางใบลิงก์ได้บางใบไม่ได้

### ทดสอบ
`node --check` ผ่าน · jsdom **12/12**
เรนเดอร์ `_sfLink` จริงแล้วเช็ค: มี url -> `<a target=_blank rel=noopener` + `stopPropagation` (กันเปิด modal แถวพร้อมกัน) · ไม่มี url -> ไม่มี `<a>` · url ที่ไม่ใช่ http(s) เช่น `javascript:` -> ไม่ทำลิงก์ · escape ข้อความ · `qn` ว่างไม่ throw
เช็คทั้งไฟล์ว่าไม่เหลือ QN ที่ยังไม่ผ่าน `_sfLink` และทุกจุดมี fallback `_esc`

---

## รอบก่อน (V669.1) — เก็บตกสิทธิ์ Order Tracking หลังย้ายเข้า Summary

สิทธิ์ `nav_track` มีมาตั้งแต่ V661 และยังคุมได้ปกติ (ปุ่มใหม่ใน Summary คง `id="nb-track"` ไว้)
แต่การย้ายทำให้เหลือของตกค้าง 3 จุด:

1. **Config แสดงเป็นแท็บระดับบน** -> ย้ายเป็นลูกของ Summary (`└ 📋 Order Tracking`, group `sub`) ให้ตรงกับที่อยู่จริง
2. **`visibleNav` ยังมี `'track'`** -> ถ้าผู้ใช้ถูกปิดแท็บที่กำลังเปิดอยู่ `_autoSwitchIfHidden()` อาจเลือก `sv('track')` เป็นปลายทาง
   ซึ่งตอนนี้ **ไม่มีทางกลับ** เพราะหน้าอยู่ในโมดัลและไม่มีปุ่มบน nav แล้ว -> ไม่ push `'track'` เข้า `visibleNav`
3. **`'track'` ไม่ได้อยู่ใน `_navExempt`** -> ถ้าบังเอิญค้างอยู่ที่ `v-track` ตอน landing จะถูกเตะออก -> เพิ่มเข้าไป

`nav_track` ยังอยู่ใน `navMap` เหมือนเดิม — คุมการแสดงปุ่มได้ครบ แค่ไม่เป็น "ปลายทาง" ของ auto-switch

### ทดสอบ
`node --check` ผ่าน · jsdom **14/14**
เทสด้วยชุดสิทธิ์จริง 5 แบบ: มีสิทธิ์ -> ปุ่มโชว์ · ไม่มีสิทธิ์ -> ปุ่มซ่อน · เหลือสิทธิ์แค่ track -> ต้องไม่เด้งไป `sv('track')` · ค้างที่ `v-track` ตอน landing -> ต้องไม่ถูกเตะออก · หน้าปัจจุบันถูกปิด -> เด้งไป `se` ไม่ใช่ `track`

---

## รอบก่อน (V669.0) — SP Forecast กฎใหม่ + ซ่อม snapshot

### 1. การ์ด SP FORECAST — เลิกคิดหัก On Hand แบบเหมารวม
อาการ: เป้า 25.00M · On Hand 36.85M (เกินเป้าแล้ว) · แต่ยังบอก To Find 21.97M — ขัดกันเอง
เหตุ: **3 ตัวเลขคนละสโคป** — เป้ารวมเฉพาะเดือนที่ตั้ง config · On Hand รวมทั้ง 12 เดือน · To Find คิดรายเดือน `max(0, เป้า-มี)` เดือนที่เกินไม่หักลบเดือนที่ขาด

**กฎใหม่ (รวมศูนย์ที่ `window._dbSpFcShown(st, lo, hi, yr)`)**
```
ทั้งปี / H1-H2 / รายไตรมาส   -> ผลรวม config เฉพาะ "เดือนอนาคต" ในช่วงนั้น ไม่หักอะไร
รายเดือน + เดือนอนาคต        -> config ของเดือนนั้น
รายเดือน + เดือนปัจจุบัน/อดีต -> เป้า - On Hand = To Find
```
เดือนปัจจุบัน **ไม่ใช่** อนาคต (ส.ค. 2569 -> ทั้งปีได้ ก.ย.-ธ.ค. = 20.00M)
เลิกใช้ `Math.max(config, prospect)` — การ์ดเป็นยอด config ล้วน · ⭐ ท้ายการ์ดจึงเป็น SP Prospect เสมอ

**เรียกจาก 3 จุดให้ตรงกันทั้งระบบ** (ตัดสินใจร่วมกับผู้ใช้ว่าให้ขยับทั้งระบบ ไม่ใช่แค่การ์ด)
`dbRenderStory` (การ์ด + PIPELINE + Forecast YE + CHALLENGE) · `_dbtsCalc` (team scene) · `_pptStoryCalc` (PPT/Export to FAD)
ผลข้างเคียงที่ตั้งใจ: **Forecast YE ลดราว 5M · CHALLENGE เพิ่มเท่ากัน** เทียบกับก่อน V669

### 2. การ์ดใช้เลข "ก่อน FAD override" ค้างมาตั้งแต่ V661.2
`getStats` คิด `monthlySpHave` เสร็จ **ก่อน** `_dbForceActualFromSheet()` เขียนทับ `monthlyActualSP` ด้วย ACTUAL_FAD
การ์ดจึงโชว์ SP 36.85M ขณะที่หัวกลุ่ม ON HAND โชว์ 32.51M — ต่างกัน 4.34M คือยอดที่ FAD ปรับเดือนอดีตลง
แก้: `_dbSpFcShown` อ่าน `st.monthlyActualSP / monthlyBacklogSP` **สดตอนเรียก** = ผ่าน override แล้วเสมอ · `monthlySpHave` เลิกใช้
(ยืนยันด้วยข้อมูลจริงจาก console: actual SP 29.53M + backlog SP 2.98M = 32.51M ตรงกับหัว ON HAND เป๊ะ)

### 3. Order Tracking ขึ้น "ยังไม่มีข้อมูล snapshot" ทั้งที่เก็บไว้ครบ
`setValues('2026-08-05')` -> Sheets แปลงเป็น**เซลล์วันที่** ให้เอง -> อ่านกลับได้ `Date`
`String(Date).slice(0,10)` = `'Mon Aug 05'` -> ตัวกรอง `d <= t` ใน `getSnapshotRange` เป็นเท็จเสมอ = คืน 0 แถว
ตัวกันซ้ำใน `saveDailySnapshot` เทียบพลาดแบบเดียวกัน -> **เขียนซ้ำทุกครั้งที่มีคนเปิดหน้าเว็บ ตั้งแต่ V660**

แก้ 2 ชั้น:
- `_snapDayStr(v)` — `Date` -> `yyyy-MM-dd` ตาม timezone สคริปต์ · ใช้ทุกจุดที่อ่านวันที่ (กรอง · กันซ้ำ · `since`) · ส่ง `date` กลับ client เป็นข้อความเสมอ
- `setNumberFormat('@')` ก่อน `setValues` ทุกจุดที่เขียนวันที่ — กันไม่ให้เกิดใหม่
- `repairSnapshotDates()` — **ต้องรันมือครั้งเดียว** จาก Apps Script editor: เขียนวันที่เดิมกลับเป็นข้อความ + ยุบแถวซ้ำใน `daily_summary` เหลือชุดล่าสุดต่อ (วัน, สถานะ)

### ทดสอบ
`node --check` (Dashboard/overrides/Code.js) · jsdom **32/32**
กฎ SP Forecast เทสครบทุกช่วง: ทั้งปี · H1/H2 · Q3 คร่อมเดือนปัจจุบัน · Q4 · รายเดือนอนาคต/ปัจจุบัน/อดีต · On Hand เกินเป้าต้องไม่ติดลบ · ปีหน้า/ปีที่แล้ว · ไม่มี config · ตั้งเป้าบางเดือน
`_snapDayStr`: Date · string · ISO · ค่าว่าง · Invalid Date

---

## รอบก่อน (V668.1) — Sales กลับมาเป็นคอลัมน์ของตัวเอง

V667 ยุบ `Sales` เข้าไปอยู่ในเซลล์เดียวกับปุ่ม `Send to FC` ตาม mockup — **ผู้ใช้ขอแยกคืน**
เหตุ: หัวตารางเหลือคำว่า `FC` คำเดียวแต่ข้างล่างมีทั้งชื่อคนและปุ่ม อ่านแล้วไม่รู้ว่าชื่อนั้นคืออะไร

```
SC Quotation  10 -> 11   ... สถานะ · Sales · FC · โน้ต
SP Quotation   8 ->  9   ... สถานะ QN · Sales · FC · โน้ต
```
เซลล์ FC กลับไป `text-align:center` (มีแต่ปุ่ม) · `Sales` เป็นคอลัมน์ซ้าย สีจาง `--mu` เหมือน Contract Monitor

**บทเรียน:** ยุบ 2 อย่างเข้าเซลล์เดียวกัน = หัวตารางอธิบายของข้างล่างไม่ได้อีกต่อไป
ประหยัดคอลัมน์ได้ก็จริง แต่แลกกับการที่ผู้ใช้ต้องเดาว่าชื่อคนในช่อง `FC` หมายถึงอะไร — **ถ้าหัวเดียวอธิบาย 2 ค่าไม่ได้ แปลว่าต้องแยกคอลัมน์**

### ทดสอบ
`node --check` ผ่าน · jsdom **38/38** (เพิ่มการนับ `cols` เทียบ `<td>` ทุกหน้า และยืนยันว่าชื่อ sales ไม่อยู่ในเซลล์เดียวกับปุ่มแล้ว)

---

## รอบก่อน (V668.0) — Order Tracking เป็น modal + จัดหัวตาราง

### 1. Order Tracking ย้ายจาก nav บน → ปุ่มในแถบ Summary (เปิดเป็น modal)
เหตุผล: แท็บบนกินที่และไม่ได้ใช้ทุกวัน · ต้องการดูระหว่าง present Summary โดยไม่ออกจากหน้า

**วิธีทำ — ย้าย element ไม่ใช่ copy โค้ด**
`otOpenModal()` **ย้าย `#v-track` ตัวจริง** เข้า `#ot-modal-slot` แล้วย้ายกลับที่เดิมตอนปิด
→ ฟังก์ชัน `otXxx` และ id ภายในหน้าใช้ได้เหมือนเดิมทุกตัว ไม่ต้องแก้ `OrderTracking.html` เลยสักบรรทัด
- จำที่อยู่เดิมไว้ใน `window._otHome = {parent, next}` **ตั้งแต่ครั้งแรกที่เปิด** — ถ้าไม่จำ ปิดแล้วหน้าหายถาวร
- ตอนคืนที่เช็ค `h.next.parentNode===h.parent` ก่อน (ถ้า sibling เดิมถูกลบไปแล้ว `insertBefore` จะ throw)
- ปิดได้ 3 ทาง: ปุ่ม ✕ · คลิกพื้นหลัง · ESC
- ปุ่มยังใช้ **`id="nb-track"` เดิม** เพราะ `_applyNavPermissions()` คุมสิทธิ์ `nav_track` ด้วย id นี้ — เปลี่ยน id เมื่อไรสิทธิ์หลุดทันที
- เพิ่ม `#nb-track` เข้าลิสต์ CSS ที่ซ่อนตอน Present (กฎมี `!important` จึงชนะ inline style ที่ permission ตั้งไว้)

### 2. หัวตารางไม่ตรงกับเนื้อ — `_spTbl` รับ `aligns`
`_spTbl` ตั้ง `text-align:left` ให้ `<th>` ทุกช่องตายตัวมาตั้งแต่ v276 แต่ `<td>` บางช่องเป็น right/center
→ หัว **มูลค่า** ลอยไปคนละที่กับตัวเลข เห็นชัดเมื่อคอลัมน์น้อยลงหลัง V667

`_spTbl(cols, rows_html, aligns)` — **ไม่ส่ง `aligns` = left เหมือนเดิม** ตารางอื่นอีก 6 จุดในไฟล์จึงไม่กระทบ
ส่งให้ 3 หน้านี้: มูลค่า = right · ชนิด/โน้ต = center · ที่เหลือ left

### 3. ช่อง FC — ชื่อ sales กับปุ่ม Send to FC ไม่ตรงกัน
เดิม `text-align:center` ชื่อ sales ยาวไม่เท่ากัน (จีรนันท์ / พงษ์ศักดิ์ / เอกชล) เลยดันปุ่มเยื้องทุกแถว
แก้: เซลล์เป็น `left` + ชื่อเป็น `inline-block; min-width:64px; text-align:right` → ปุ่มเริ่มที่ตำแหน่งเดียวกันทั้งคอลัมน์

### ทดสอบ
`node --check` 3 ไฟล์ผ่าน · jsdom **34/34**
โมดัล: เปิด/ปิด/เปิดซ้ำ · ย้ายกลับที่เดิมถูกตำแหน่ง (เช็ค `nextElementSibling`) · ไม่สร้างโมดัลซ้ำ · ESC · `otInit()` ถูกเรียกครั้งเดียวต่อการเปิด
align: เรนเดอร์จริงแล้วอ่าน `text-align` ของ `<th>` ทีละช่อง + ยืนยันว่าเรียกแบบไม่ส่ง `aligns` ยังได้ left ครบ

---

## รอบก่อน (V667.0) — จัดคอลัมน์ 3 ตาราง (mockup ผ่านแล้ว)

### หลักที่ใช้ทั้ง 3 หน้า
1. คอลัมน์ที่เป็น `—` ทุกแถว → **ตัดทิ้ง** ไม่เก็บไว้กินที่
2. วันเริ่ม + วันสิ้นสุด → รวมเป็น **ช่วงสัญญา** ช่องเดียว (ต้นเหตุที่ `2569-09-01 *` ตกบรรทัด)
3. QN · ลูกค้า · วันที่ · ชนิด → `white-space:nowrap` **ห้ามตัดคำ** · ปล่อยให้ยืดได้เฉพาะช่องรายละเอียด
4. `Sales` ย้ายไปอยู่**หน้าปุ่ม Send to FC** (SC/SP) — เป็นข้อมูลประกอบการตัดสินใจ ไม่ใช่คอลัมน์ที่ต้องกวาดตาหา
   Contract Monitor ไม่มีปุ่ม จึงคง `Sales` เป็นคอลัมน์ (ย้ายไปท้ายตาราง)

### ผลลัพธ์
```
SC Quotation      13 -> 10   QN · ลูกค้า · รายละเอียด · ชนิด · ประเภทสัญญา · ช่วงสัญญา · มูลค่า · สถานะ · FC · โน้ต
SP Quotation       9 ->  8   QN · ลูกค้า · Brand · มูลค่า · สถานะเครื่อง · สถานะ QN · FC · โน้ต
Contract Monitor  13 -> 10   Stage · ลูกค้า · เลขที่ใบเสนอราคา · ยี่ห้อ · รุ่น · ช่วงสัญญา · สถานะใบเสนอราคา · มูลค่า · Sales · โน้ต
```
Contract Monitor ตัด **ผู้รับผิดชอบ** + **Log** — ทั้งสองคอลัมน์ hardcode `—` ทุกแถว (ไม่ใช่ข้อมูลว่าง แต่ไม่เคยมีแหล่งข้อมูลเลย)
SP **ไม่ได้ตัด `สถานะเครื่อง`** ตามที่ mockup วาดไว้ — mockup วาดจากภาพหน้าจอที่ไม่มีคอลัมน์นั้น แต่ของจริงมีและใช้งานอยู่ (v397)

### ไม่ต้องแตะตัวเรียง
ทั้ง 3 ตารางสร้างด้วย `_spTbl(cols, body)` ตัวเดียวกัน → `_spSortTable` เรียงตาม **index คอลัมน์** อัตโนมัติ
เปลี่ยน `cols` แล้วหัวคลิกเรียงได้เองทุกช่องโดยไม่ต้องแก้อะไรเพิ่ม
ช่อง **ช่วงสัญญา** เรียงตาม**วันเริ่ม** (regex `YYYY-MM-DD` ตัวแรกใน cell) — ถ้าอยากเรียงตามวันสิ้นสุดต้องแก้ `val()`

### จุดที่ต้องระวังเวลาแก้ตารางกลุ่มนี้
- `cols.length` ต้องเท่ากับจำนวน `<td>` **ทุก branch** — เทสนับให้แล้วทั้ง 3 หน้า
- ของที่ห้ามหลุดตอนจัดคอลัมน์: `_sfLink` (ลิงก์ SmartFlow V666) · `_fcCellHtml` (โหมด SP ต้องส่ง `true`) ·
  `_qnCellHtml` · `_scqDateCell` (ดาว * ส้ม = วันที่มาจากใบเสนอราคา) · `_qnYearCell` · `event.stopPropagation()` ที่ช่องโน้ต Contract

### ทดสอบ
`node --check` ผ่าน · jsdom **23/23**
เรนเดอร์ `_spTbl` ด้วยคอลัมน์ใหม่จริง แล้วกดเรียง 4 คอลัมน์: ช่วงสัญญา (asc/desc) · มูลค่า (ต้องอ่านเป็นเลข ไม่ใช่สตริง) · QN

---

## รอบก่อน (V666.1) — ลิงก์ SmartFlow ครบทุกหน้า

### `URL` มาครบ 100% ทุกตาราง และชี้คนละ endpoint
```
RAW_SC_BILL      -> EDIT_SC            4504/4504
RAW_SpareParts   -> EDIT_SPARE_PART    3419/3419
RAW_Quotation    -> EDIT_QUOTATION     5842/5842
RAW_Quotation_SP -> EDIT_QUOTATION     5284/5284
RAW_SERVICE_JOB  -> EDITJOB             161/161
```
**ต้องใช้ URL ของแถวนั้นเสมอ ห้ามยืมของตารางอื่น** — endpoint ต่างกัน

### ทำเป็นฟังก์ชันกลาง `window._sfLink(txt, url)`
ไล่เติมทีละหน้าแล้วลืมอีกหน้า พลาดมา 2 รอบแล้วกับ `_pkgBadge` → รอบนี้ทำตัวกลางตั้งแต่แรก
- เช็ค `^https?://` ก่อนเสมอ · ไม่มี URL → คืนข้อความเปล่า **ไม่ทำลิงก์ตาย**
- escape ทั้งข้อความและ url · `stopPropagation` กันเปิด modal พร้อมกัน
- guard `typeof === 'function'` ทุกจุดที่เรียก

### [V666.1] แก้: 3 หน้าที่ต่อผิดที่
V666.0 ผมเติมลิงก์ตามบรรทัดที่ `grep` เจอ **โดยไม่ยืนยันว่าตรงกับหน้าที่ผู้ใช้เปิดจริง**
หน้า SC Quotation / SP Quotation / Contract Monitor เรนเดอร์เข้า `sp-scquote-body` (6764) · `sp-spquote-body` (6955) · `sp-contract-body` (7674) — คนละที่กับที่ผมแก้
**วิธีหาที่ถูก:** ไล่จากปุ่มแท็บ → `sp-pane-xxx` → `getElementById('sp-xxx-body')` → ฟังก์ชัน render
Contract Monitor บังเอิญถูกอยู่แล้ว เหลือ SC/SP Quotation 2 จุด (ใช้สตริงเดียวกันเป๊ะ)

### จุดที่ต่อแล้ว
- ตารางหลัก Service Commercial Orders — **แถว SP ชี้ `EDIT_SPARE_PART`** (เติม `url` ให้ record SP 4 จุดที่สร้างจาก `RAW_SpareParts`)
- SC / SP Quotation · Contract Monitor · อีก 2 ตารางใน SalesProspect (รวม 5 จุด)

### ข้อมูล RAW_SERVICE_JOB ที่สำรวจได้ (161 รายการ · ทีม 1002131 · ก.พ.–ก.ย. 69)
| ฟิลด์ | ครบ |
|---|---|
| `job_sr` · `URL` | **100%** |
| `job_problem_detail` (อาการ) | 97% |
| `job_serial_no` | 87% |
| `job_contact_dep` (แผนก) | 64% |
| `job_contact_name` | 60% |
| **`job_contact_tel`** | **49%** |
| `job_due_date` | **14%** |

**ปัญหาที่ต้องแก้ก่อนทำหน้า SR Commercial**
1. **เบอร์โทรอยู่ผิดช่อง** — ตัวอย่างจริง `job_contact_name = "คุณศิริรำไพ บุญตา (081-4719226)"` แต่ `job_contact_tel = ''` → ต้องดึงเบอร์จาก 2 ที่ แล้ว fallback ไป `RAW_CUSTOMER`
2. **`job_status` เป็นรหัสตัวเลข** `2333`(124) `2327`(18) `2328`(8) `2330`(5) `2331`(3) — **ยังไม่รู้ว่าแปลว่าอะไร** ต้องขอตารางแปลจาก SmartFlow ไม่งั้นบอกไม่ได้ว่าใบไหนปิดแล้ว
3. **`job_commercial_state` ว่างทั้ง 161 รายการ** — มีครบชุด (`_by` `_dt` `_remark` `_case`) น่าจะเป็นช่องที่ทีม commercial ใช้บันทึกการติดต่อ แต่ยังไม่มีใครใช้
4. `job_due_date` มีแค่ 14% → เรียงตาม SLA ไม่ได้ ต้องเรียงตาม `job_add_date_time`
5. prefix SR หลากหลาย: `DC`(131) `RP`(23) `SC`(3) `PM`(2) — **ไม่เกี่ยวกับ SC_BILL** (งานส่งต่อจากช่างให้ commercial ติดต่อลูกค้า ไม่ใช่งานบิล)

### ทดสอบ
`node --check` 13 บล็อกผ่าน · jsdom/vm **24/24** (V666) + ชุดเก่าทั้งหมดผ่าน
รัน `_sfLink` 9 เคส: มี/ไม่มี url · url อันตราย (`javascript:`) · escape ข้อความและ url

---

## รอบก่อน (V665.5) — ดึง RAW_TEAMS + RAW_SERVICE_JOB ครบตามคู่มือ API

### เดิมดึงแค่ 8 จาก 10 dataset
`RAW_TEAMS` และ `RAW_SERVICE_JOB` ไม่เคยถูกดึงเลย (ยืนยันจาก `meta.selected_datasets` ในไฟล์จริงที่ผู้ใช้ส่งมา 5 ส.ค. 69)

### วิธีทำ — แยกไฟล์ที่ 3 ห้ามให้ของเดิมพัง
- **`RAW_TEAMS`** ไม่ต้องมีพารามิเตอร์ → ต่อท้าย `SMARTFLOW_MASTER_DATASETS` ได้เลย
- **`RAW_SERVICE_JOB`** บังคับ `team` + `date_start` + `date_end` **ถ้าขาด API ตอบ 400 ทั้ง request**
  → ถ้ายัดเข้า `SMARTFLOW_TX_DATASETS` เฉยๆ **5 dataset เดิมจะพังไปด้วย ข้อมูลทั้งระบบหาย**
  → จึงเป็นการเรียกแยก + ไฟล์แยก `bjh_job.json.gz` + ปุ่มแยก

**`smartflowJobSyncToDrive()`**
- `_bjhJobTeamIds_()` อ่าน `TEAM_ID` จาก `RAW_TEAMS` ในไฟล์ master ที่ดึงมาแล้ว **ไม่ hardcode**
  กรองเฉพาะเลขบวก (ตามคู่มือ) · ตัดซ้ำ · รองรับทั้ง `TEAM_ID` และ `team_id`
- `_bjhJobDateRange_()` ย้อนหลัง 6 เดือน → สิ้นเดือนหน้า (เผื่องานนัดล่วงหน้า)
- **ยังไม่มี `RAW_TEAMS` → คืน error บอกให้กด Load SF ก่อน ไม่ยิง API เปล่า**
- **HTTP ไม่ใช่ 200 → ไม่เขียนไฟล์ทับของเดิม** และ error บอกว่าส่ง team/วันอะไรไป (400 จะได้ไม่ต้องเดา)
- เช็ค `BJH_URL_MAX` ก่อนยิง — ถ้าทีมเยอะจน URL เกิน 2048 จะบอกให้แบ่งดึง

**ปุ่ม `🛠 Load SR Job`** ใน Config → API Configuration (ข้าง Load SF) · แสดงจำนวนทีม/ช่วงวัน/ขนาด/เวลาที่ใช้
`getServiceJobData()` เตรียมไว้ให้หน้าจอเรียกอ่าน

### ลำดับการใช้งาน
1. `🛠 Load SR Job` → **ดึง master ให้เองถ้ายังไม่มี `RAW_TEAMS`** แล้วดึง SR ต่อ
2. `Load Data`

**[V665.3] แก้: ปุ่ม `Load SF` ดึงแค่ TX ไม่ได้ดึง master**
`RAW_TEAMS` อยู่ใน `SMARTFLOW_MASTER_DATASETS` ซึ่งดึงโดย `smartflowDailySyncToDrive()` — **มีแต่ trigger ตี 0 เท่านั้นที่เรียก ไม่มีปุ่มในระบบเลย**
ผมบอกผู้ใช้ว่า "กด Load SF ก่อน" ซึ่ง**ไม่มีวันได้ `RAW_TEAMS`** → ขึ้น error วนอยู่อย่างนั้น
แก้: `smartflowJobSyncToDrive()` เรียก `smartflowDailySyncToDrive()` ให้เองเมื่อยังไม่มีทีม แล้วอ่านซ้ำ
- ดึงแล้วยังไม่มี → บอกให้เช็คว่า SmartFlow เปิดสิทธิ์ dataset นี้ให้ client แล้วหรือยัง **ไม่ยิง API เปล่า**
- ดึง master พัง → คืน error ไม่ throw
- มีทีมอยู่แล้ว → ไม่ดึงซ้ำ

### ทดสอบ
- `node --check` 13 บล็อกผ่าน · jsdom/vm **46/46** (V665) + ชุดเก่า 10 ชุดผ่านหมด
- รัน `smartflowJobSyncToDrive()` ตัวจริงด้วย `UrlFetchApp` จำลอง: URL ถูกต้องครบพารามิเตอร์ · ไม่มีทีม → ไม่ยิง · 400 → ไม่เขียนไฟล์
- เช็คว่าฟังก์ชันช่วยที่เรียกมีอยู่จริงทุกตัว (เคยเขียน `_bjhDriveFolder_` ที่ไม่มีอยู่ แล้วเทสจับได้)

### ข้อมูลจริงที่สำรวจได้ (จากไฟล์ที่ผู้ใช้ส่ง)
- `SC_BILL` มี `SR` อยู่แล้ว **4,066 / 4,504 (90%)** · SR ไม่ซ้ำ 4,060
  `JOB_STATUS`: Completed 3,859 · Canceled 97 · Delegated 54 · Pending 42 · Pending (งานเอกสาร) 13
  → **งานค้าง 109 ใบ** ทำหน้า SR Commercial เฟส 1 ได้เลยโดยไม่ต้องรอ API
- `TEAM_BILL` เป็นเลข 1/2/3/4 และ **มีแบบผสม** `1 , 4` (133) · `1 , 2 , 4` (126) → **1 ใบมีได้หลายทีม ~350 ใบ** ต้องตัดสินใจว่านับให้ใคร
- ยังไม่รู้ว่า `TEAM_BILL` กับ `RAW_TEAMS.TEAM_ID` เป็นเลขชุดเดียวกันไหม — **ห้ามเดา** ต้องดู RAW_TEAMS จริงก่อน

### [V665.1] ลิงก์ SmartFlow กดไม่ติด — ตัวคัดลอกกินคลิก
V664 ทำลิงก์ไว้แล้วแต่**กดไม่เปิด** เพราะ `[C8] คลิกเลข → คัดลอก` (`overrides.html:2271`) ดักที่ `document`
ด้วย **capture phase + `stopPropagation()`** → กินคลิกก่อน `<a>` เสมอ
แก้ที่ `_leafText()` — เจอ `el.closest('a[href]')` ให้คืนค่าว่าง = ไม่ถือเป็นเลขที่คัดลอกได้
**เลขที่ไม่มีลิงก์ยังคัดลอกได้เหมือนเดิม** · tooltip สั้นลงเหลือ `เปิดใน SmartFlow` (เดิมมีเลขซ้ำ ยาวจนบังตาราง) และ `<span>` แม่ไม่ใส่ title ซ้ำเมื่อมีลิงก์แล้ว

### [V665.2] บั๊กในตัวสร้าง patch เอง — ไม่ใช่บั๊กแดชบอร์ด
`patch_V665.ps1` / `_1` ที่ส่งไป **พิมพ์ `DONE : V663.0` และไม่มี guard ตรวจเวอร์ชันเลย**
เหตุ: ตอนประกอบไฟล์ผมใช้ regex ตัดคอมเมนต์เก่าออก
```python
re.sub(r"#  NEW in V664.*?# ====...\n", "", head, flags=re.S)
```
`.*?` กินยาวข้ามบล็อกคอมเมนต์ไปลบส่วนหัวที่มี `$NEWVER` `$STAMP` `$MSG` และ guard `notmatch` ทิ้งด้วย
**payload กับ SHA ยังถูกต้อง** ไฟล์ที่เขียนลงเครื่องไม่เสียหาย แต่เสี่ยงเพราะลงทับฐานผิดเวอร์ชันได้โดยไม่มีใครห้าม

V665.2 ประกอบใหม่จากต้นแบบที่หัวไฟล์ครบ + เพิ่มขั้นตรวจหลังสร้างทุกครั้ง:
เช็คว่ามี `$NEWVER` / `$STAMP` / `$MSG` / guard / `Write-Payload` / `Check-Same` ครบ และเรียงลำดับถูก **ก่อนส่งให้ผู้ใช้**

### [V665.4] ดึงเฉพาะทีม Service Commercial
เดิมดึงทั้ง 44 ทีม → URL เสี่ยงเกิน 2048 · ช้า · ส่วนใหญ่ไม่เกี่ยวกับงาน
`SMARTFLOW_JOB_TEAM_NAMES = ['Service Commercial']` — **ตั้งเป็นชื่อทีม ไม่ใช่เลข** ถ้า SmartFlow เปลี่ยน ID ยังหาเจอ
อยากได้ทั้ง BU ให้เติมชื่อทีมอื่นในลิสต์

### RAW_TEAMS ของจริง (5 ส.ค. 69 · 44 ทีม)
```
1002131  Service Commercial   ← ทีมเป้าหมาย  (BU 1000246 · manager 2392)
```
ทีมอื่นใน BU `1000246`: Service-3 · NH : Non Hospital · MIT4 · Product Group IV · Service Contract · Service1 G2-1 · Service Project

**⚠️ `TEAM_ID` กับ `TEAM_BILL` เป็นคนละระบบเลข**
```
RAW_TEAMS.TEAM_ID  = 1002131      (7 หลัก)
SC_BILL.TEAM_BILL  = 1/2/3/4 หรือ "1 , 4"
```
**เชื่อมกันตรงๆ ไม่ได้** — ต้องรอดูว่า `RAW_SERVICE_JOB` มีฟิลด์อะไรให้ join (น่าจะเป็นเลข SR ซึ่ง `SC_BILL` มีอยู่แล้ว 90%)

### หน้า SR Commercial ที่ผู้ใช้ต้องการ (ยังไม่ได้ทำ)
ส่งให้ **sales เจ้าของตามโรงพยาบาล** · แสดง: เลข SR · ชื่อ รพ. · ยี่ห้อ · รุ่น · SN · อาการ · ผู้ติดต่อ · เบอร์โทร
**เบอร์โทรต้องกดโทรออกได้บนมือถือ** (`tel:`)
→ รอไฟล์ตัวอย่าง `RAW_SERVICE_JOB` ก่อน ยังไม่รู้ว่ามีฟิลด์ผู้ติดต่อ/เบอร์/อาการ จริงไหม

### [V665.5] อ่าน RAW_TEAMS ไม่เจอ ทั้งที่ข้อมูลมีอยู่จริง
`_bjhReadDriveCached()` คืน **"ข้อความดิบ" (base64 ของ gzip) ไม่ใช่ object ที่ parse แล้ว**
ฝั่ง server ไม่เคยแตะข้อมูลข้างในเลย — ส่งดิบให้ client ไปคลายเอง (`Code.js:1310` ส่ง `content` ตรงๆ)
V665.4 ผมเขียน `m.data.RAW_TEAMS` โดยคิดเองว่าเป็น object → ได้ `[]` **เงียบๆ ทุกครั้ง** เพราะครอบ try/catch ไว้

แก้: เพิ่ม `_bjhMasterJson_()` คลายเอง — `Utilities.base64Decode` → `newBlob` → `ungzip` → `JSON.parse`
รองรับ 3 รูปแบบ: base64+gzip (ของจริง) · JSON ตรงๆ · object (เผื่ออนาคตเปลี่ยน)

**เกร็ด:** ในฟังก์ชันห้ามเขียน `'{'` เป็นสตริง เพราะตัวตัดโค้ดในเทส (นับปีกกา) จะพัง → ใช้ `charCodeAt(0) === 123` แทน

### ยังไม่ได้ทำ
- หน้า SR Commercial (แท็บยังเป็น Coming Soon) — รอเห็นฟิลด์จริงของ `RAW_SERVICE_JOB` ก่อน
- ป้าย (Px) เฉพาะใบที่มีใน Install Base — Install ครอบคลุมใบใน SC_BILL แค่ **918/1,612 (56%)** ที่เหลือนับจาก SC_BILL ซึ่งได้เลขต่ำกว่าจริง **รอผู้ใช้เลือกว่าจะใส่ป้ายทุกใบหรือเฉพาะที่แน่ใจ**

---

## รอบก่อน (V664.3) — Brand/Model จาก SC_BILL + (Px) + ลิงก์ SmartFlow

### SF เพิ่มฟิลด์ให้แล้ว (5 ส.ค. 69) — ไม่ต้องแก้ ETL
`SMARTFLOW_TX_DATASETS` ดึงทั้ง dataset ไม่ได้ระบุคอลัมน์ → ฟิลด์ใหม่ไหลมาเอง
**ต้องกด `🔌 Load SF` (Config → API Configuration) ก่อน แล้วค่อย `Load Data`** — กด Load Data เฉยๆ จะได้ไฟล์เดิมใน Drive

ตรวจจากข้อมูลจริง: `_jsupSC` 4,504 แถว · `URL` `SN` `BRAND_NAME` มาครบ · ทุกตาราง (Quo/QuoSP/Contract) ก็มี `URL`

### 1. Brand/Model — Layer 0 จาก RAW_SC_BILL
V619 เคยเขียนไว้ว่า *"RAW_SC_BILL ไม่มีฟิลด์ผูกเครื่องต่องวด"* → **ข้อจำกัดนั้นหมดไปแล้ว**
`resolveBM()` เพิ่ม **Layer 0** อ่าน `BRAND_NAME`/`MODEL_NAME` ของแถวนั้นตรงๆ เชื่อก่อนทุกชั้น (เป็นฟิลด์จริง ไม่ใช่การเดา)
record SC รับเพิ่ม: `scbr` `scml` `scsn` `url` — **ใช้ชื่อ `scsn` เพราะ `sn` ถูกใช้เก็บ `JOB_STATUS` อยู่แล้ว** (ชื่อชนกันมาแต่เดิม ห้ามแตะ)

### 2. ป้าย `(Px)` — P = Package · x = จำนวนเครื่องในสัญญา
**[V664.1] นับจาก Installation Base ก่อน ตกมาที่ SC_BILL**
V664.0 นับจาก SN ใน SC_BILL อย่างเดียว → **ผิด** เพราะ SC_BILL เก็บเฉพาะเครื่องที่**ออกบิลไปแล้ว** ไม่ใช่ทุกเครื่องในสัญญา
เคสจริง `QSC230525-5955`: Install Base **5 เครื่อง** แต่ SC_BILL มีแค่ **2 SN** → ขึ้น `P2` ผิด
Install Base ครอบคลุม SC **2,021/3,058 (66%)** และ ≥ SC_BILL ทุกเคสที่ตรวจ · ที่เหลือใช้ SC_BILL ต่อ ดีกว่าไม่มีป้ายเลย

**[V664.1] ป้ายต้องอยู่ใน `renderTbl` ไม่ใช่ `extractBrandModelHTML`**
V664.0 ใส่ป้ายไว้ใน `extractBrandModelHTML()` แต่ **ตารางหลักสร้าง `_brHTML` เอง ไม่ได้เรียกฟังก์ชันนั้น** → ป้ายไม่ขึ้นสักใบ
ชิป `Package` เดิมก็เติมจำนวนให้ด้วย → `Package P5`

เดิม (V664.0): นับ SN ไม่ซ้ำใน `RAW_SC_BILL` — ยังใช้เป็น fallback

**[V664.2] ยี่ห้อจริงต้องชนะชิป `Package` เสมอ**
ชิป `Package` (V622) มีไว้ตอนที่**ยังไม่รู้ยี่ห้อรายเครื่อง** — พูดความจริงว่า "หลายยี่ห้อ" ดีกว่าเดาให้ผิด
พอ `BRAND_NAME` มาแล้ว ชิปนั้นหมดความจำเป็น แต่ V664.1 ยังตกไปเข้าเงื่อนไขเก่า เพราะ Layer 0 นับ `nBr` จาก Install list ด้วย
เคสจริง `QSC2602-15510`: ได้ยี่ห้อจริงแล้ว แต่ `nBr=9` เลยขึ้น `Package P9` ทับ
แก้: เติม `&& _bm.src!=='sc_bill'` — ใบที่ไม่มี `BRAND_NAME` ยังใช้ชิปเดิมต่อไป

**[V664.3] ป้าย (Px) เป็นฟังก์ชันกลาง `window._pkgBadge(bm, rid)`**
ไล่ใส่ป้ายผิดที่มา 2 รอบ: V664.0 ใส่ที่ `extractBrandModelHTML` (ตารางหลักไม่ได้เรียก) · V664.1 ย้ายเข้า `renderTbl` แล้วหน้า **SC Monitor** ก็ยังไม่มี
→ ทำเป็นตัวกลางตั้งแต่แรก ทุกหน้าที่โชว์ยี่ห้อเรียกตัวเดียวกัน
- `rid` ไม่ส่ง = ไม่ผูก `onclick` (สำหรับหน้าที่ไม่มี `selRow()`)
- **มี `typeof === 'function'` guard ทุกจุดที่เรียก** — เทสชุด V655 จับได้ว่าถ้า `_pkgBadge` ยังไม่โหลด `renderTbl` จะ throw แล้ว**ตารางว่างทั้งหน้า**
- SC Monitor เรียก `resolveBM()` ตัวเดียวกับตารางหลักอยู่แล้ว → ยี่ห้อถูกเองตั้งแต่ V664.2
ตรวจข้อมูลจริง `QSC2401-08586`: contract มี **1 แถว** (เก็บแค่เครื่องหลัก) แต่ SC_BILL มี **42 แถว = 5 SN × หลายงวด**
→ `CON_ID` ซ้ำสูงสุด 1 (unique รายเครื่อง) · `CONTRACT_NO` ซ้ำ 57 แต่ค่าเป็น `-` (ค่าว่างมารวมกัน) · **`QUOTATION_NO` ซ้ำสูงสุดแค่ 2** ใช้ไม่ได้ทั้งหมด
ผลจริง: ใบมีหลายเครื่อง 39 ใบ จาก 1,612 · แถวไม่มี SN 19 จาก 4,504 (0.4%) → ตกไปใช้ `resolveBM` เดิม
มี cache (`_pkgCache`) + `window._pkgReset()` เรียกตอนโหลดข้อมูลใหม่

**ยี่ห้อแสดงตามแถวตรงๆ ไม่เลือก ไม่เดา** — `(Px)` เป็นแค่ป้ายบอกว่าเป็น Package ไม่เกี่ยวกับยี่ห้อ
(ผมตีความผิด 2 รอบว่าต้องเลือก "ยี่ห้อที่มีมากสุด" — ผู้ใช้ยืนยันว่าไม่ใช่)

### 3. กดเลขใบเปิด SmartFlow
คอลัมน์ Quotation ในตารางหลัก → `<a href=URL target=_blank>` · `event.stopPropagation()` กันเปิด modal พร้อมกัน
**เช็ค `^https?://` ก่อนเสมอ** ใบไม่มี URL แสดงเลขเฉยๆ ไม่ทำลิงก์ตาย
เส้นใต้แบบจุด (dotted) บอกว่ากดได้โดยไม่รบกวนสายตาบนตารางที่แน่นอยู่แล้ว

### ทดสอบ
- `node --check` 13 บล็อกผ่าน · jsdom/vm **33/33** (V664) + ชุดเก่าทั้งหมดผ่าน
- `_pkgCount` 6 เคส: 5 SN × 8 งวด → 5 (ไม่ใช่ 40) · เครื่องเดียวหลายงวด → 1 · ไม่มี SN → 0 · ข้อมูลว่างไม่ throw
- `extractBrandModelHTML` 7 เคส: ป้าย P5 ขึ้นเมื่อ >1 · เครื่องเดียวไม่ขึ้น · ยี่ห้อแสดงตามจริงไม่เลือกมากสุด

### ยังไม่ทำ
- ลิงก์ SmartFlow ในหน้า Contract Monitor / SC Quotation / SP Quotation / Install Base (2️⃣ ประวัติสัญญา · 6️⃣ ประวัติเสนอราคา) — รอบนี้ทำแค่ตารางหลักก่อน ให้ดูว่าใช้งานจริงโอเคไหม
- **SR Job** (`RAW_SERVICE_JOB`) — รอ `TEAM_ID` จาก `RAW_TEAMS` + วัตถุประสงค์ · ต้องแก้ `Code.js` เพราะ dataset นี้บังคับส่ง `team`/`date_start`/`date_end` ถ้าต่อท้าย `SMARTFLOW_TX_DATASETS` เฉยๆ **ทั้งชุดจะ 400**

---

## รอบก่อน (V663.0) — 4 เรื่อง

### กติกาใหม่ของ patch (เริ่มรอบนี้)
**ฝัง payload เฉพาะไฟล์ที่แก้จริง** ไฟล์ที่ไม่ได้แก้จะเช็ค SHA บนเครื่องเท่านั้น
ถ้า SHA ไม่ตรง = ฐานไม่ตรงกับที่ผมคิด → **หยุดทันที** แล้วขอ patch เต็ม (กันพังเงียบ)
รอบนี้แก้ 3 ไฟล์ จาก 14 → ขนาดลดจาก ~2.9MB เหลือ ~1.5MB
`clasp push` + `create-deployment` คงเดิม (ผู้ใช้บอกว่าเวลาปกติ)

### 1. Focus modal ยังหาใบ Prospect ไม่เจอ — V662 แก้ไม่ครบ
**V662 ผมเดาผิด** ว่า `r.st` ของ `QSP2511-014743` คือ `SP Ordered (Pending)`
ค่าจริงจาก console คือ **`st: 'Prospect'`** (SP ที่ลูกค้าตอบรับแต่ยังไม่มี PO — v369/v370)

มี **3 สถานะ** ที่ระบบแสดงผลเป็น "Prospect" — ลิสต์เต็มมีอยู่แล้วที่ `overrides.html:1615` ผมลอกมาแค่ตัวเดียว
```js
window._stBucket = function(r){
  if(st==='Offer') return 'Offer';
  if(st==='Prospect') return 'Offer';                                  // ← V663 เพิ่ม
  if(st==='SP Ordered (Pending)' && _hasHMStage(r)) return 'Offer';
  return st;
};
```
**เจอ `_stBucket` ซ้ำ 2 นิยาม** (บรรทัด 2821 กับ 2841) ตัวแรกเป็นของเก่าไม่มี `Prospect` — ลบทิ้งแล้ว มีเทสล็อกว่าต้องเหลือ 1 นิยาม
ซ้ำรอย V547 (กล่อง "ค่าเดิมก่อนแก้" ซ้ำ 2 บล็อก) — **บั๊กแบบนี้เจอรอบที่ 2 แล้ว**

### 2. `🔒 CONFIRMED` → `🔒 ON HAND`
`CONFIRMED` แปลว่า "ผ่านขั้นยืนยันแล้ว" แต่คนอ่านตีความว่า "ได้เงินแน่" ทั้งที่ Sale Order / Sales Confirmed ยังหลุดได้
`ON HAND` = อยู่ในมือแล้ว ตรงตามข้อเท็จจริง ไม่สัญญาอะไร · เข้ากับคอลัมน์ `Order on hand (Admin)` ในชีต Export ที่ทีมคุ้นอยู่แล้ว
จุดที่ผู้ใช้เห็นมีที่เดียว (`Dashboard.html:400`) ที่เหลือเป็นคอมเมนต์

### 3. ชื่อ Sale Order / Sales Confirmed อ่านจาก Config
เดิมชื่อเดียวกันเขียนไว้ **4 ที่ ไม่ตรงกัน** — chip เขียน `Sales Order` แต่หัวป๊อปอัพเขียน `Backlog` / `BL Confirmed`
เพิ่ม `_qfNm(pill, fb)` อ่านจาก `_QF_CFG` แล้วใช้ทั้ง 4 จุด: การ์ด (`db-nm-backlog`/`db-nm-backlogc`) · ตัวเลือก status · หัวป๊อปอัพ Breakdown · chip (เดิมอ่านอยู่แล้ว)
`_qfApplyNames()` เรียกทุกครั้งที่ render เพราะ config มาทีหลัง

### 4. การ์ด SP Forecast: `Confirm` → `On Hand`
ยอดที่หักออกมาจากกลุ่ม `ON HAND` พอดี — ใช้คำเดียวกันคนอ่านโยงได้ทันทีว่าหักมาจากไหน
```
🔧 SP FORECAST
฿5.00M
On Hand ฿2.25M
To Find ฿2.75M   12.1%
```

### ทดสอบ
- `node --check` 13 บล็อกผ่าน · jsdom/vm **33/33** (V663) + 26 + 62 + 121 + 26 + 82 + 62 + 34 + 76
- รัน `cbRenderList()` จริงด้วยข้อมูล `QSP2511-014743` (`st='Prospect'`): ทั้งหมด/Prospect/ค้นเลขใบ เจอครบ · SP Pending ไม่เจอ (อยู่กลุ่มเดียว)
- `_qfNm` 4 เคส: ไม่ตั้ง config / ตั้ง / ค่าว่าง / ไม่มี element

---

## รอบก่อน (V662.0) — Focus modal หาใบ Prospect ไม่เจอ (ด่วน)

### อาการ
`QSP2511-014743` แสดงในตาราง Orders เป็น **Prospect** แต่เปิดโมดัล Focus เลือก Status = `Prospect` แล้วขึ้น **0 รายการ**

### ต้นเหตุ — ตรรกะเดียวกันเขียนไว้ 2 ที่ แล้วไม่ตรงกัน
ใบนี้ `r.st` จริงคือ **`SP Ordered (Pending)`** ไม่ใช่ `Offer`
`renderTbl` มี special case (`_spIsProspect`) ว่า **SP Ordered (Pending) + มี HM stage → แสดงเป็น Prospect**
(ใบนี้ `qs='Customer Accept'` ซึ่ง map เป็น stage H)

แต่ `cbRenderList()` ในโมดัลกรองด้วย `r.st` ตรงๆ เทียบกับ `_CB_ALLOW_ST` ที่มี `Offer` → ใบนี้ตกทั้งกอง
**ตารางบอก Prospect · โมดัลบอก SP Pending · ใบเดียวกัน** คนใช้งานไม่มีทางเดาถูก

### แก้ — ทำกติกาจัดกลุ่มตัวกลาง
```js
window._stBucket = function(r){
  if(r.st==='SP Ordered (Pending)' && _hasHMStage(r)) return 'Offer';
  return r.st || '';
};
```
- `cbRenderList()` กรองด้วย `_stBucket(r)` แทน `r.st` ทั้งตอนเช็ค `_CB_ALLOW_ST` และเทียบ dropdown
- `_cbFillIp()` (ตัวสร้าง dropdown IP) ใช้กติกาเดียวกัน ไม่งั้นตัวเลือก IP ไม่ตรงกับรายการที่กรองได้
- `renderTbl` เปลี่ยนมาใช้ `_stBucket` ด้วย พร้อม fallback เดิม → **กติกาอยู่ที่เดียวจริง ทั้ง 2 หน้า**
- ผลลัพธ์: ใบนี้อยู่ใต้ `Prospect` เท่านั้น **หายจาก `SP Pending`** — 1 ใบ 1 กลุ่ม ไม่ซ้ำ ไม่หาย

### ทดสอบ
- `node --check` 13 บล็อกผ่าน · jsdom/vm **26/26** (V662) + 62 + 121 + 26 + 82 + 62 + 34 + 76
- รัน `_stBucket` 8 เคส: Customer Accept / M stage → Offer · ไม่มี HM stage และ L stage → คงเดิม · สถานะอื่นไม่โดนแตะ · `r` ว่างไม่ throw
- รัน `cbRenderList()` ตัวจริงด้วยข้อมูลใบ `QSP2511-014743`: เลือก Prospect เจอ 1 · เลือก SP Pending ได้ 0 · เลือกทั้งหมดเจอ 1 · ใบ SP Pending ที่ไม่มี HM stage ยังอยู่กลุ่มเดิม

---

## รอบก่อน (V661.3) — 2 เรื่อง · ไฟล์ใหม่ `OrderTracking.html`

### 1. หน้า `📋 Order Tracking` — แท็บบนสุดต่อจาก Sales Prospect
อ่านจาก snapshot ที่เก็บมาตั้งแต่ V660 (`getSnapshotRange`) **ไม่คำนวณจาก `window.D` สด** เพราะต้องเทียบภาพ 2 วัน

**4 บล็อก**
1. แถบเลือกวัน `A → B` (date picker 2 ช่อง) + ปุ่มลัด เมื่อวาน→วันนี้ / วันแรกของเดือน / 7 วัน · ใส่กลับด้านสลับให้เอง
2. การ์ด 6 สถานะ ณ สิ้นวันปลายทาง + ส่วนต่างเทียบวันตั้งต้น (▲▼)
3. ตารางความเคลื่อนไหว `จาก → เป็น` นับจาก `daily_change` — **เห็นทุกก้าวที่ขยับ ไม่ใช่แค่ผลสุทธิ** (ต่างจากที่กังวลไว้ตอนทำม็อกอัพ เพราะเก็บ change รายวันไว้แล้ว)
4. รายใบ 5 แท็บ (ทั้งหมด / เข้าใหม่ / เปลี่ยนสถานะ / ออกบิลแล้ว / หายไป) + คอลัมน์ **ค้างมากี่วัน**

**กติกาที่ยึด**
- ความเคลื่อนไหวนับ `date > dFrom && date <= dTo` — ตัดวันตั้งต้นออก (เป็นจุดอ้างอิง ไม่ใช่ช่วงที่วัด)
- **วันที่เลือกไม่มี snapshot → เตือนบนจอว่าใช้วันไหนแทน** ไม่แอบใช้วันข้างเคียงเงียบๆ
- ยังไม่มีข้อมูลเลย → บอกตรงๆ ว่าเริ่มเก็บตั้งแต่ V660 และเก็บวันละครั้งตอนเปิดหน้าเว็บ
- สิทธิ์: key `nav_track` (default เปิดทุก role) · อยู่ใน `_TAB_PERM_NEWKEYS_DEF` จึงไม่โดนปิดตอนอัป

**2 บั๊กที่เทสจับได้ก่อนส่ง**
- เรียก `otRender()` แบบ bare — ในเบราว์เซอร์ทำงานได้เพราะ `window.x=` สร้าง global แต่เปราะ → เปลี่ยนเป็น `window.otRender()` ให้ชัด
- **`otTabs()` รันก่อน `otRows()` ซึ่งเป็นคนตั้ง `_otF/_otT`** → ตัวนับบนแท็บอ่าน `undefined` ได้ 0 ทุกแท็บในรอบแรก · ย้ายการตั้งช่วงวันขึ้นไปต้น `otRender()`

### 2. SP Forecast หักใบที่มีจริงแล้ว (การ์ด PIPELINE)
เดิม `PIPELINE 5.00M` เต็มก้อนจาก config บวกกับ `CONFIRMED` ที่มี SP 2.19M อยู่แล้ว → **Forecast YE นับซ้ำ 2.19M**
เป้า 5 ล้าน = ยอด SP ทั้งเดือน ไม่ใช่ "ที่จะได้เพิ่มจากที่มีอยู่แล้ว"
ระบบเคยกันไว้บางส่วน (`hasFcspCfg` ตัดใบ `is_sp_fix`) แต่**ไม่ได้ตัดใบ QSP จริง**

**[V661.1] ย้ายการหักไปทำใน `getStats()` เป็นรายเดือน — V661.0 ผิด**
V661.0 หักใน `dbRender` โดยกรองเดือนเองด้วย `_lo/_hi` → **กวาดทั้งปีมาหัก** (ขึ้น `Confirm ฿9.67M` แทนที่จะเป็น ฿2.19M) เหลือ ฿0
และยังเป็นคนละที่กับแถว **Monthly Forecast** ที่อ่าน `monthlyFCSP[]` ตรงๆ → 2 หน้าไม่ตรงกัน

**[V661.2] นิยาม "ที่หัก" ผิดด้วย — ต้องเป็น SP ในการ์ด CONFIRMED**
V661.0/.1 ผมเขียนลูปนับ SP เองใหม่ ซึ่ง**กวาดสถานะเกิน** (SP Ordered / Processing / Carry) และไม่ตรงกับที่ผู้ใช้ต้องการ
ผู้ใช้ระบุชัด: `CONFIRMED ฿4.20M · SC ฿1.97M · SP ฿2.23M` → **ต้องหัก ฿2.23M ตัวนี้**

แก้: เลิกนับเอง ใช้ array ที่ระบบคำนวณไว้แล้ว
```js
monthlySpHave[m] = monthlyActualSP[m] + monthlyBacklogSP[m]   // ตัวเดียวกับ confSP ที่การ์ดโชว์
monthlyFCSP[m]   = max(0, config[m]/_fcspDiv − monthlySpHave[m])
```
**เลขบนการ์ด PIPELINE กับ CONFIRMED มาจากแหล่งเดียวกัน ไม่มีทางไม่ตรง**
ต้องวางไว้**หลังลูปหลัก** เพราะ 2 array นี้เพิ่งถูกเติมเสร็จ (`cnt.fcsp` / `hasFcspCfg` ยังนับที่เดิม)
**ทุกที่ที่ใช้ `monthlyFCSP` ถูกพร้อมกันหมด** — แถว Monthly Forecast · Quarterly Forecast · การ์ด PIPELINE · Forecast YE · Export · ปุ่ม `Forecast SP` ในตัวเลือก status
`dbRender` เลิกคำนวณเอง ใช้ `_sumR(stats.monthlySpHave)` ซึ่งใช้ช่วงเดือนเดียวกับการ์ดอื่นทุกใบ **ไม่มีทางเหลื่อม**
ส่งค่ากลับเพิ่ม 2 ตัว: `monthlyFCSPRaw` (เป้าเต็ม) · `monthlySpHave` (ที่หักไป)

**หน้าตาการ์ด 3 บรรทัด [V661.3]**
```
🔧 SP FORECAST
฿5.00M
Confirm ฿2.25M
To Find ฿2.75M   12.1%
```
`To Find` ใช้คำเดียวกับการ์ด **CHALLENGE** (`to find`) — บอกว่าเป็นยอดที่ยังต้องปิดให้ได้ ไม่ใช่แค่เศษที่เหลือ · คนอ่านเชื่อมโยงได้ทันที
ไม่มีการหัก → โชว์แค่ตัวเดียวเหมือนเดิม ไม่รกจอ · ตรงกับ 3 คอลัมน์ในชีต Pivot ของ Export to FAD

### ทดสอบ
- `node --check` 13 บล็อกผ่าน · jsdom/vm **60/60** (V661) + 121 + 26 + 82 + 62 + 34 + 76
- รันหน้า Order Tracking จริงผ่าน `google.script.run` จำลอง: ไม่มีข้อมูล / มี 2 วัน / เลือกวันที่ไม่มีข้อมูล / กรองแท็บ / ใส่วันกลับด้าน / ไม่มี google.script
- รันสูตร `_spHave` จริง 6 เคส: หักใบจริง · ไม่หัก prospect/is_sp_fix/SC · เกินเป้าตัดที่ 0 · นอกเดือน/คนละปีไม่หัก

---

## รอบก่อน (V660.6) — 4 เรื่อง · แตะ 6 ไฟล์ · **ต้อง Deploy → New Version**

### 1. ปุ่ม `✅ Confirm Billing` → `✅ Focus` + แก้ชื่อเองได้
เดิม hardcode ที่ `body_app_top.html:644` · ย้ายเข้าระบบเดียวกับ Quick Filter chips (key = `confirm`)
- `_QF_KEYS` +`confirm` → ได้ทั้ง `qf_confirm_label` / `qf_confirm_show` ฟรีจากกลไกเดิม
- `window._cbApplyBtn()` ต้องเรียก **2 จังหวะ**: ตอน apply role และตอน config โหลดเสร็จ (มาคนละเวลา ถ้าเรียกจังหวะเดียวจะได้ค่าเก่า)
- **สิทธิ์ยังชนะ config เสมอ** — role ไม่ผ่านก็ซ่อน แม้ config สั่งแสดง

**[V660.3] หัวโมดัลกับ default filter**
- หัวโมดัลเดิม hardcode `✅ Confirm Billing —` ที่ `confirm_billing.html:4` → เปลี่ยนเป็น `<span id="cb-title-lbl">` แล้วให้ `_cbApplyBtn()` เซ็ตชื่อเดียวกับปุ่ม (แก้ที่ Config ที่เดียว เปลี่ยนทั้งปุ่มและหัวโมดัล)
- เรียก `_cbApplyBtn()` ตอนเปิดโมดัลด้วย เพราะ config อาจถูกแก้ระหว่างทาง
- **default filter = `Backlog (Confirmed)`** (ป้ายที่ผู้ใช้ตั้งชื่อว่า `Sales Focus`) — เดิมเปิดมาเป็น "ทั้งหมด" ต้องเลือกเองทุกครั้ง
  ตั้งค่าที่ `confirm_billing.html:83` · ลำดับสำคัญ: `_cbFillStatus()` เติม option ก่อน แล้ว `openConfirmBilling` เซ็ต value แล้ว `_cbFillStatus()` รอบสองไม่รีเซ็ตเพราะ early-return ตาม signature

### 2. Export to FAD — SP Forecast ในชีต Pivot
`FCSP_FIXED` (5 ล้าน/เดือน) เป็น **config ไม่ใช่ใบจริง** ส่วน `dbExportToFAD()` วนจาก `window.D` ล้วน → ค่านี้ไม่เคยโผล่ใน Excel
แถมมีกับดัก: ถ้าตั้ง `FCSP_FIXED` ไว้ ระบบจะตัดใบ `is_sp_fix` ออกจาก pipeline กันนับซ้ำ → **หายทั้งคู่**

เพิ่ม 3 คอลัมน์ในชีต Pivot: `SP Forecast (ตั้งไว้)` · `SP เปิดบิลแล้ว` · `SP Forecast คงเหลือ`
- **ค่า config เป็น "บาท" อยู่แล้ว — ห้าม ×1000** (แก้ที่ V660.4 · V660.0–.3 ผิด ออกมาเป็น 5 พันล้าน)
  `BUD` เก็บพันบาทและมี `*1000` แต่ `FCSP_FIXED` ไม่ใช่ — `getStats:936` อ่าน `fcspCfg` ตรงๆ ไม่คูณ
- **[V660.6] หักด้วย "ใบ QSP ทุกใบที่อยู่ในชีตนี้" ไม่ใช่แค่ที่เปิดบิลแล้ว**
  เดิมหักเฉพาะ `st='Billed'` → ใบ QSP ที่ยัง Backlog/Ordered ก็อยู่ในตารางด้วย กลายเป็น**นับซ้ำ**
  ผู้ใช้จับได้จากของจริง: `forecast 4.99 + QSP 2.19 = 7.18` ล้าน ทั้งที่เป้าคือ 5 ล้าน
  ใช้ `orderedRecs` ตัวเดียวกับที่สร้างชีต → หักตรงกับสิ่งที่อยู่ในไฟล์เป๊ะ (prospect อยู่คนละกอง · ใบ `is_sp_fix` ถูกตัดไปแล้วเมื่อมี FCSP config)
  หัวคอลัมน์เปลี่ยนเป็น `SP ที่มีใบแล้ว (QSP)` · ป้ายในแถวเป็น `SP Forecast (หัก QSP ในเดือนแล้ว)`
- คงเหลือ = `max(0, ตั้งไว้ − บิลแล้ว)` **ตัดที่ 0 ไม่ติดลบ** แต่ยังโชว์ยอดบิลจริงไว้ตรวจย้อนได้

**[V660.1] เพิ่มแถวในชีต `Ordered` ด้วย** — ผู้ใช้ต้องการเห็นเป็น**บรรทัดในตาราง** ไม่ใช่แค่คอลัมน์สรุป
1 แถว/เดือนที่มียอดคงเหลือ > 0 · `PO/Contract number` = `SP-FORECAST-Aug-26` · Hospital = `SP Forecast (หักที่เปิดบิลแล้ว)` · Period = `Aug-26` · Quick Filter = `SP Forecast`
**ใส่ที่ `oRows` เท่านั้น ไม่ยัดเข้า `orderedRecs`** — ไม่งั้นชีต Pivot จะนับซ้ำ เพราะ Pivot คำนวณจาก `orderedRecs`
ต้องย้ายบล็อกคำนวณ `fcspRaw`/`spBilled` ขึ้นมาก่อน Sheet 1 (เดิมอยู่ในช่วง Pivot ซึ่งอยู่ทีหลัง)

**[V660.5] แถวรวม + เรียงเดือน ในชีต `Ordered`**
- **แถวรวมอยู่บนสุด (แถว 1)** · header เลื่อนเป็นแถว 2 · ข้อมูลเริ่มแถว 3 → ตรงกับสูตร `SUBTOTAL(9,L3:L9999)` / `M3:M9999` ที่ผู้ใช้ให้มา
- `SUBTOTAL(9)` นับเฉพาะแถวที่ผ่านตัวกรอง Excel → เปิด `!autofilter` ที่ `A2:V<n>` ด้วย ไม่งั้นไม่ต่างจาก `SUM`
- ตั้ง `wsO['L1'] = {t:'n', f:'...', z:'#,##0'}` โดยตรง เพราะ `aoa_to_sheet` ใส่สูตรจาก array ไม่ได้
- **เรียงเดือน: เดือนปัจจุบันขึ้นก่อน** แล้วไล่ไปข้างหน้า (ส.ค. → ก.ย. → ธ.ค.) จบด้วยเดือนที่ผ่านมาแล้ว (ม.ค.–ก.ค.)
  อ่านเดือนจากคอลัมน์ Period (`'Aug-26'`) · แถวไม่มีเดือนไปท้ายสุด · ใช้ index กำกับให้ sort เสถียร ลำดับเดิมในเดือนเดียวกันไม่สลับ · เรียงเลข NO ใหม่หลัง sort

**หมายเหตุ:** ชีต `Pipeline` ที่คอมเมนต์เก่าบอกว่ามี **ไม่มีอยู่จริง** — `pipelineRecs` ถูกเก็บแต่ไม่เคย `book_append_sheet` เลย (dead code) จึงใส่ไว้ในชีต Pivot แทน

### 3. Daily Snapshot — เริ่มเก็บข้อมูลสำหรับหน้า Order Tracking
**เดิมวางแผนให้ trigger ตี 0 ถ่ายที่ server — ทำไม่ได้** เพราะ `Code.js` ไม่มีตรรกะจัดสถานะเลยสักบรรทัด (`grep Backlog Code.js` = 0)
ทั้งหมดอยู่ที่ `normalizeStatusV51()` ในเบราว์เซอร์ · ถ้ายกไป server ต้องเขียนซ้ำ แล้วเลขในรายงานจะไม่ตรงกับหน้าจอ

→ **ถ่ายตอนเปิดหน้าแทน** (`window.bjhDailySnapshot()` ยิงหลัง render เสร็จ หน่วง 3 วิ)
- snapshot = สิ่งที่คนเห็นบนจอจริงๆ ใช้ตรรกะตัวเดียวกันเป๊ะ
- แลกกับ: วันไหนไม่มีใครเปิดเลย = ไม่มีข้อมูลวันนั้น (เสาร์-อาทิตย์)
- ติดตาม 6 สถานะ: `Billed` `Backlog` `Backlog (Confirmed)` `SC Ordered` `SC Carry` `SC Carry (Pending)` เฉพาะปีปัจจุบัน

**3 ชีตใหม่**

| ชีต | เก็บอะไร | โต |
|---|---|---|
| `snap_state` | ภาพล่าสุด 1 แถว = 1 ใบ + `sinceDate` (วันที่เข้าสถานะนี้) | เขียนทับทุกวัน ไม่โต |
| `daily_summary` | วัน × สถานะ: จำนวน · มูลค่า · แยก SC/SP · จำนวนที่ข้ามเดือนมา | +4-6 แถว/วัน |
| `daily_change` | เฉพาะใบที่ขยับ: `new` / `moved` / `billed` / `gone` + `daysInPrev` | ตามจริง |

**ลำดับเขียนสำคัญ: change → summary → snap_state**
ถ้าพังกลางทาง `snap_state` ยังเป็นภาพเมื่อวาน วันถัดไปจับส่วนต่าง 2 วันรวด — ดีกว่าเขียน state ก่อนแล้วส่วนต่างหายถาวร
`LockService` กันยิงชนกัน · เช็ควันซ้ำก่อนเขียนเสมอ ยิงกี่ครั้งก็ได้
`getSnapshotRange(from,to)` เตรียมไว้แล้วสำหรับหน้าจอรอบหน้า

### 4. Save Budget / Forecast แล้วมีผลทันที
`_cfgSaveToSheet()` เดิม**ขึ้นข้อความว่าบันทึกแล้วอย่างเดียว** ไม่อัปเดตตัวแปรในหน่วยความจำ ไม่สั่ง render → ต้อง refresh
เพิ่ม callback ตัวที่ 4 · `window._cfgReapply(partial)` เรียก `applyConfig(partial)` แล้ว render ซ้ำ
`applyConfig` มี `if(cfg.X)` คุมทุกส่วน → ส่งเฉพาะ key ที่ต้องการ apply ได้ ไม่กระทบส่วนอื่น
ไม่ต้องโหลดข้อมูลใหม่ เพราะ budget/forecast มาจาก config ไม่ได้มาจาก ETL

**[V660.2] แก้บั๊กที่ V660.0 ยังไม่หาย — แถบ Quarterly/Monthly Budget ยังค้าง**
`cfgSaveBudgetToSheet()` สร้าง payload เป็น **flat `{team:{SC,SP}}` ไม่มีชั้นปี** (backend เติมปีปัจจุบันให้เองที่ `Code.js:1998` legacy branch)
แต่ `applyConfig` อ่าน `cfg.budget[ปี][team][type]` → ส่ง flat เข้าไปตรงๆ ได้ `BUD_YEARS['Service 1']` = ขยะ → `curBud` ว่าง → `BUD` ไม่เปลี่ยน → แถบค้างเหมือนเดิม
แก้: ห่อปีปัจจุบันก่อนส่ง + merge ปีอื่นไว้ไม่ให้หาย
`buildSummary` ที่ render แถบนี้อยู่ใน `overrides.html:386` (ทับตัวใน `script_main`) — `_cfgReapply` เรียกผ่าน global จึงได้ตัวที่ถูก
ต่อ callback ให้ `fcsp` / `fcfad` / `sa_bud` / `fad` ด้วย (ตั้งค่า window ไว้อยู่แล้ว ขาดแค่ render ซ้ำ)

### ทดสอบ
- `node --check` 9 บล็อกผ่าน · jsdom/vm **121/121** (V660) + 26 + 82 + 62 + 34 + 76
- จำลอง Sheet แล้วรัน `saveDailySnapshot()` ตัวจริง 3 วันติด: วันแรก new หมด · ยิงซ้ำวันเดิมข้าม · วันที่ 2 จับ `billed`+`new` ได้ ใบที่ไม่ขยับคง `sinceDate` เดิม · วันที่ 3 จับ `gone`
- รันสูตร SP Forecast จริง: หักเฉพาะ SP `Billed` ปีเดียวกัน · บิลเกินเป้าตัดที่ 0 · ไม่ตั้ง config = 0
- `_cbApplyBtn` 6 เคส (default / ตั้งชื่อ / ชื่อเว้นวรรค / สั่งซ่อน / role ไม่ผ่าน / ไม่มีปุ่ม)

### หลังลง
1. `Ctrl+Shift+R` → กด **Load Data**
2. เปิดหน้าทิ้งไว้สัก 5 วินาที → เช็คว่ามีชีต `snap_state` / `daily_summary` / `daily_change` เกิดขึ้น
3. **snapshot จะเริ่มนับวันที่ 1 จากวันที่ลง** — ยิ่งลงเร็ว ข้อมูลยิ่งเดินเร็ว
4. หน้า `Order Tracking` ทำรอบหน้า (V661) — วางเป็นแท็บบนสุดต่อจาก Sales Prospect

---

## รอบก่อน (V659.0) — ปิดการแก้ Status ด้วยมือ

### เหตุผล
ปุ่มแก้ Status ด้วยมือทำให้ไล่ตัวเลขไม่จบ — ใบที่ถูกแก้จะหลุดจากตรรกะปกติทั้งหมด
(carry-forward ไม่ทำงาน · `periodLabel` ขึ้น manual shift · ยอดในการ์ดขยับ) และ**จากหน้าจอดูไม่ออกว่าเลขไหนมาจากของจริง เลขไหนมาจากการแก้มือ**
ผู้ใช้กดคืนค่าเดิมออกหมดแล้ว จะดูสักพักว่ากระทบอะไรไหม ถ้าไม่มีค่อยลบถาวร

### สิ่งที่ทำ
1. **ตัดทางเข้าอย่างเดียว** — `script_main.html` `var _stClick=''` (เดิมผูก `onclick="editStatus(id)"` ไว้ที่ badge Status)
   บรรทัดเดิมเก็บไว้เป็นคอมเมนต์ใต้บรรทัดใหม่ **เปิดกลับได้ทันที**
   ทางเข้ามีที่เดียวจริง — grep แล้วไม่มีปุ่มที่อื่น
2. **ไม่ลบฟังก์ชัน** — `editStatus` / `confirmEditStatus` / `rollbackEditStatus` / `cancelManualEdit` อยู่ครบ พร้อม `window.*` เดิม
3. **หน้า Config → 📝 ประวัติแก้ Status เพิ่มสรุป** — "ยังมีผลอยู่ N ใบ" (เหลือง) หรือ "ไม่มีใบที่ยังถูกแก้มือค้างอยู่" (เขียว)
   นับโดยเอา**แถวล่าสุดต่อ `rowKey`** แล้วดูว่า Cancelled หรือยัง — ใช้ยืนยันว่ากดคืนออกหมดจริง

### ไม่ได้แตะ
- ระบบ**ยังอ่าน `sales_manual_edit` เหมือนเดิม** → **ยอดทุกการ์ดไม่ขยับสักบาท**
- ปุ่ม `↩ กลับไป Status เดิม` ยังใช้ได้ปกติ อยู่ในกล่องรายละเอียดใบ (`overrides.html:1054` / `:1343`) คนละที่กับโมดัลแก้ — โชคดีที่ไม่ได้อยู่ในโมดัล ไม่งั้นตัดทางเข้าแล้วจะ undo ไม่ได้เลย
- แถวที่เคยแก้ยังไฮไลต์ `.edited` อยู่

### ขั้นต่อไป (รอผู้ใช้ดูสักพัก)
ถ้าไม่กระทบอะไร → เลิกอ่าน `sales_manual_edit` ถาวร + ลบฟังก์ชันทิ้ง
**ก่อนทำต้องเช็คตัวเลข "ยังมีผลอยู่ N ใบ" ให้เป็น 0 ก่อน** ไม่งั้นยอดจะเด้งกลับโดยไม่รู้ว่าเท่าไร

### ทดสอบ
- `node --check` 7 บล็อกผ่าน · jsdom/vm **26/26** (V659) + 82/82 + 62/62 + 34/34 + 76/76
- รัน `renderTbl()` ตัวจริง — badge Status ไม่มี `onclick` ไม่มี tooltip ชวนกด แต่ `.edited` ยังไฮไลต์
- รัน `cfgMeditInit()` ตัวจริงผ่าน google.script.run จำลอง 4 เคส: มีค้าง 2 ใบ · ใบเดียวแก้ 2 ครั้งแล้วยกเลิก = 0 · ไม่มีประวัติเลย

---

## รอบก่อน (V658.2) — 5 เรื่อง · แตะ 5 ไฟล์ · **ต้อง Deploy → New Version** (แตะ `Code.js`)

### 1. IP ตามสถานะงานจริง — `SC Pending` / `RP Pending`
เดิมใบที่ยังไม่มีเลขบิลถูกเหมาเป็น `SC Billing` / `SP Billing` ทั้งหมด **ไม่ว่างานเสร็จหรือยัง**
→ ตาราง Orders เต็มไปด้วย `Sale Order` + `SC Billing` ทั้งที่ JOB STATUS เป็น `Delegated`

กฎใหม่ (`overrides.html:114` SC · `:150` SP):
- **มีเลขบิลแล้ว → `SC Billed` / `SP Billed` จบ** ไม่ดูสถานะงาน
- ยังไม่มีเลขบิล + งานจบ → `SC Billing` / `SP Billing`
- ยังไม่มีเลขบิล + งานไม่จบ → **`SC Pending` / `RP Pending`**
- `Canceled` → `Cancel PM` (เหมือนฝั่ง SC Ordered เดิม)

**แก้เฉพาะ `ip` ไม่แตะ `st`** — ทุกใบยังเป็น `Backlog` เหมือนเดิม **ยอดเงินไม่ขยับสักบาท** เปลี่ยนแค่ป้าย
ต้องเติม 2 ค่าใหม่เข้า 5 จุดที่ hardcode `SC Billing`/`SP Billing` ไว้ (`_isBL` · `moBilling` · `billingRows` · chip map · ปุ่ม pill) ไม่งั้นยอด Billing หาย
`ipCls()` เพิ่มให้ 2 ค่าใหม่ใช้สี `bip-sc-wait-pm` (เหลือง = งานยังไม่จบ)

**⚠️ ชื่อชนของเดิม:** `overrides.html:93` มี `r.ip='SC Pending'` อยู่ก่อนแล้ว (สาย `_hasHMStage` + มีเลขบิล → `st='Billed'`) — **ไม่กระทบ** เพราะทั้ง 3 จุดที่เติมมี `st==='Backlog'` คุมอยู่ และ chip map เช็ค `st==='Billed'` ก่อน · มีเทสคุมไว้แล้ว แต่ถ้าจะแตะจุดนี้อีกต้องระวัง

### 2. แท็บ Config `🏁 สถานะงาน` — ลิสต์ "ถือว่าจบงาน" แก้เองได้
`r.sn` (JOB_STATUS) **ใช้คำต่างกันระหว่าง SC กับ SP** — SC = `Completed` · SP = `PO Completed` · สัญญา = `Contract Completed`
ถ้าเขียน `sn==='Completed'` ตรงๆ **ฝั่ง SP จะกลายเป็น `RP Pending` ทั้งหมด** ยอด SP Billing หายเกลี้ยง

แก้: `window._jsClass(sn)` อ่านจากตารางแมป (`_JS_DEFAULT` + config section `job_status`)
- **`PO Completed` = `pending` ไม่ใช่ `done`** (แก้ V658.1 ตามที่ผู้ใช้ยืนยัน) — PO เสร็จหมายถึงสั่งซื้อจบ **อะไหล่ยังไม่ถึงมือลูกค้า** ยังบิลไม่ได้
- **ผลคือฝั่ง SP ไม่มีค่า default ตัวไหนเป็น `done` เลย** → SP ที่ยังไม่มีเลขบิลจะเป็น `RP Pending` ทั้งหมด ถ้ามีสถานะที่ถือว่างานเสร็จจริง ให้ไปติ๊กในแท็บ Config (ไม่ต้องรอ patch)
- ค่า: `done` / `pending` / `cancel` · **ค่าที่ไม่รู้จัก = `pending` เสมอ** (ปลอดภัยกว่า — กันนับเงินเข้า Billing ทั้งที่งานยังไม่เสร็จ)
- UI: รายการสถานะงานที่**พบจริงในข้อมูล** + จำนวนใบ เรียงจากมากไปน้อย · ค่าที่ยังไม่ได้ตั้งติดป้าย **"ใหม่"** สีส้ม + แจ้งจำนวนด้านล่าง
- โครงเดียวกับ 🔧 สถานะอะไหล่ (`parts_status`, V562) · อยู่กลุ่ม `status`
- **แก้แล้วต้องกด Load Data สด** — cache ไม่ re-classify

### 3. Quick Filter — chip ที่กดอยู่ต้องเห็นชัด
โหมดมืด `.on` ต่างจากปกติแค่ `rgba(...,.08)` → `.12` กับ border 1→1.5px **มองไม่ออกว่ากดอะไรอยู่**
โหมดสว่างทำถูกมาตั้งแต่ V479.27b (`:1333-1336`) — โหมดมืดโดนลืม
แก้: พื้นทึบสีเต็ม + ตัวอักษรขาว + วงแหวน + ยกขึ้น 1px · **สีที่ทึบยังต่างกันรายตัว** จึงบอกได้ว่ากรองอะไรอยู่ ไม่ใช่แค่ว่ามีอะไรถูกกด

**กับดักที่เทสจับได้:** วางกฎใหม่ไว้ตำแหน่ง `.ip-pill.on` (บรรทัด 561) ซึ่งอยู่**ก่อน** `.ip-backlog.on` เดิม (569) → specificity เท่ากัน ตัวหลังชนะ กฎใหม่ไม่ทำงานเลย · ต้องย้ายไปวางหลังกฎเดิมทั้งหมด

### 4. ปุ่ม `📋 Budget & Forecast` → `📋 Budget & Actual`
แก้ 3 จุด — `Dashboard.html` 2 ปุ่ม (แท็บบน + ปุ่มลัด) และป้ายใน Tab Permissions (`script_main.html:1342`)

### 5. ตัวเตือนกรอง status — ลดความเด่น (แก้ของ V656)
V656 ทำปุ่ม ⚙ เป็น `⚙ กรอง 4/10` สีส้มพื้นส้ม — **เด่นเกินไปสำหรับหน้าที่ใช้ present**
เหลือแค่ `⚙` สีเดิม (จางลงนิด `opacity .85`) ส่วนจำนวนกับคำเตือนย้ายไปอยู่ใน **tooltip** แทน
ยังกันกับดักเดิมได้ (เอาเมาส์ชี้ก็รู้ว่ากรองอยู่) แต่ไม่รบกวนตอนนำเสนอ

### ทดสอบ
- `node --check` 7 บล็อกผ่าน · jsdom/vm **82/82** (V658) + 62/62 + 33/33 + 76/76
- รัน `normalizeStatusV51()` ตัวจริง 17 เคส: SC/SP × จบ/ไม่จบ/ยกเลิก × มี-ไม่มีเลขบิล × `sb=รอดำเนินการออกบิล`
  ยืนยัน `st` และเดือนไม่เปลี่ยนตามสถานะงาน (แบบ A)
- โหลด `style.html` เข้า jsdom แล้ววัด `getComputedStyle` ของ chip — พื้นทึบจริง ตัวอักษรขาวจริง แต่ละสียังต่างกัน
- แท็บ Config: render/เรียงลำดับ/ป้าย "ใหม่"/save/ไม่มี element ไม่ throw

---

## รอบก่อน (V657.0) — 3 เรื่อง · แตะ 5 ไฟล์ · **ต้อง Deploy → New Version** (แตะ `Code.js`)

### 1. Dropdown ปี — เอาแค่ 2026 เป็นต้นไป
`dbPopulateFilters()` ดูด `BUD_YEARS` / `ACTUAL_FAD_YEARS` เข้ามาหมด → 2023/2024/2025 โผล่ทั้งที่ไม่มีประโยชน์
แก้: `filter(y => y >= 2026)` · **ใช้ค่าคงที่ `_YR_MIN = 2026` ไม่ใช้ `CUR_YR`** — ถ้าใช้ `CUR_YR` พอขึ้นปี 2027 ระบบจะซ่อน 2026 ทิ้งเอง
ปีอนาคต (2027 วางแผน) ยังเข้ามาปกติ · **ไม่แตะ dropdown ปีใน Budget modal** (`md-bud-year`) เผื่อต้องแก้งบปีเก่า

### 2. `sales_manual_edit` เก็บประวัติครบ (append-only)
เดิม `saveManualEdit()` **ทับแถวเดิมตาม `rowKey`** → ใบเดียวแก้ 3 ครั้ง เห็นแค่คนสุดท้าย และ `OldStatus` กลายเป็นค่าที่คนก่อนตั้งไว้ ไม่ใช่ค่าจริงจาก SmartFlow
- `saveManualEdit()` → `appendRow` ทุกครั้ง
- `getManualEdits()` → dedupe เอาแถวล่างสุดต่อ `rowKey` **ก่อน** กรอง Cancelled (ถ้ากรองก่อน แถวเก่าที่ยัง active จะโผล่มาแทนใบที่เพิ่งยกเลิก)
- `cancelManualEdit()` → mark เฉพาะแถวล่าสุด และ**ไม่ทับ `EditedAt` อีกต่อไป** (ของเดิมเขียนทับ = ประวัติหาย) · เก็บเป็น `Cancelled <iso> by <ชื่อ>` ในคอลัมน์ 12
- **พฤติกรรมการคำนวณเดิมทุกอย่าง** — แค่ประวัติไม่หาย

### 3. หน้า "📝 ประวัติแก้ Status" ใน Config (admin)
`getManualEditLog(limit)` ใหม่ — คืนทุกแถวรวมที่ยกเลิกแล้ว เรียงล่าสุดขึ้นก่อน (default 300 · เพดาน 1000)
UI: เวลา · ใครแก้ · ใบ/ลูกค้า · จาก → เป็น · เดือน/ปี · สถานะ (ใช้งานอยู่ / คืนค่าเดิมแล้ว) + ช่องค้นหา + ปุ่มรีเฟรช
อยู่ในกลุ่ม `sys` แถว admin → **admin เท่านั้น** · ไม่ต้องเปิด Google Sheet ดูเองอีก

### ทดสอบ
- `node --check` 7 บล็อกผ่าน · jsdom/vm **62/62** (V657) + 33/33 (V656) + 76/76 (V655)
- จำลอง Sheet แล้วรัน `saveManualEdit` / `getManualEdits` / `cancelManualEdit` / `getManualEditLog` ตัวจริง
  แก้ 3 ครั้ง → 3 แถว · dedupe ได้ค่าล่าสุด · ยกเลิกแล้วแถวเก่าไม่โดน mark และ `EditedAt` ไม่ถูกทับ
  แก้ → ยกเลิก → แก้ใหม่ กลับมา active · หลายใบไม่ปนกัน · ชีตว่างไม่พัง · limit/เพี้ยน
- `dbPopulateFilters()` ตัวจริง 5 เคส · ตาราง log render + ค้นหา + ไม่มี element ไม่ throw

---

## รอบก่อน (V656.0) — 3 เรื่อง

### 1–2. CA (SP) และ High (SC) ไม่ถูกดันไป ธ.ค. อีกต่อไป
`_applyNoBillDiv4()` ใน `overrides.html` ดันทุกอย่างที่ `st='Offer'` ไปเดือน ธ.ค. ถ้าเข้าเงื่อนไข
`_toDec = _isNb || (r.t==='SP') || r._isLow` → **SP Offer ทุกใบ + SC ที่ไม่มีงวดบิล กองอยู่ ธ.ค. หมด**
→ การ์ด SP FORECAST / SC PROSPECT ขึ้น **฿0 ตลอด 11 เดือน** เห็นของเฉพาะตอนเลือก ธ.ค.

แก้: เพิ่ม `_isHotProspect(r)` ยกเว้น 2 กลุ่มที่ใกล้ปิดที่สุด ให้อยู่เดือนของตัวเอง
- **SP**: `qs` / `sc` / `status` = `Customer Accept` (ลูกค้าตอบรับแล้ว รอ PO)
- **SC**: HM stage = `H`
- `_isLow` ยังไป ธ.ค. เสมอ แม้ stage เป็น H
- **ไม่แตะ `px÷4` และ `_isEstimate`** — เปลี่ยนแค่เดือนที่ไปลง (SC High ยังหาร 4 ติดธง `~` เหมือนเดิม)
- Medium / Low / Other ยังกอง ธ.ค. เหมือนเดิม

### 3. ตัวเตือนว่ากำลังกรอง status อยู่
`Dashboard.html:1387-1399` ถ้าตัวกรอง ⚙ เปิดอยู่ `_sfOn()` จะบังคับยอดเป็น **0 เงียบๆ**
เสียเวลาไล่หาบั๊กไปหลายรอบเพราะการ์ด SP ORDERED ขึ้น ฿0 ทั้งที่มีของ 26 ใบ ฿2.50M — ที่จริงคือลืมติ๊ก status
แก้: ปุ่ม ⚙ เปลี่ยนเป็น **`⚙ กรอง n/N` สีส้ม** + tooltip "ยอดที่เห็นไม่ใช่ทั้งหมด" เมื่อกรองอยู่ · ไม่กรองก็กลับเป็น `⚙` ปกติ

### ทดสอบ
- `node --check` 6 บล็อกผ่าน · jsdom/vm **33/33** (V656) + **76/76** (ของ V655 ที่รวมมา)
- รัน `_applyNoBillDiv4()` ตัวจริง 11 เคส: CA อยู่เดือนเดิม · SP ธรรมดาไป ธ.ค. · SC High อยู่เดือนเดิมแต่ยังหาร 4 · SC Medium ไป ธ.ค. · Low ไป ธ.ค. แม้ stage H · เรียกซ้ำไม่หารซ้ำ · ไม่มี HM map ก็ไม่พัง
- ปุ่ม ⚙ 3 สถานะ + กรณีไม่มีปุ่มในหน้า

---

## รอบก่อน (V655.x) — 6 เรื่อง · แตะ 5 ไฟล์ + `.gitignore` ใหม่ · **ไม่แตะ `Code.js`**

### 1. ฟอนต์ในตารางหลักไม่เท่ากัน + คอลัมน์ Bill ล้น
ตารางเดียวมี **5 ขนาดปนกัน** เพราะ `.dtbl td` ไม่เคยตั้ง `font-size` เลย → รับ 14px จาก `body` ขณะที่ badge ตั้ง inline ไว้ 9 / 10 / 11 / 12.5px

| ส่วน | เดิม | ใหม่ |
|---|---|---|
| ทุกช่องในตาราง | 14px (สืบทอด) | **12px** (`.dtbl td`) |
| `.bdg` (Status / IP) · Job Status · ชิป "ชิ้น" · Type | 9 / 9 / 9 / 10 | **10px ทั้งหมด** |
| In.VAT · ชิป Package | 11 / 12.5 | ลบ inline → รับ 12px |

- `.bdg` แก้แบบ **scope `.dtbl`** เท่านั้น — `.bdg` ที่ใช้นอกตารางยังเป็น 9px เหมือนเดิม
- คอลัมน์ Bill / Delivery: V621 ตั้ง `overflow:visible;text-overflow:clip` ไว้กันวันที่โดนตัด → ที่ 14px มันเลยล้นไปทับ In.VAT · **คืนเป็น `overflow:hidden` + ellipsis + `title` ข้อความเต็ม** (12px พอดีคอลัมน์แล้ว)
- โหมดสว่างเดิมบังคับ `td{font-size:12.5px !important}` (บรรทัด 138) ทำให้ดำ 14 / สว่าง 12.5 ไม่ตรงกันมาตั้งแต่ต้น → เพิ่ม override เฉพาะ `.dtbl` ให้เป็น 12px และ badge 11px **ไม่แตะบรรทัด 138 เดิม**

### 2. กล่อง "ค่าเดิมก่อนแก้" ซ้อนกัน 2 กล่องในโมดัลแก้ Status
`editStatus()` มีบล็อก `r._orig_st` **2 บล็อกในสตริงเดียวกัน** (บรรทัด 5321 กับ 5342) ติดคอมเมนต์ `[V547]` เหมือนกันทั้งคู่ = แปะซ้ำ 2 รอบตอน V547
ไม่มีใครเห็นมานาน เพราะต้องเคยกดแก้ Status ใบนั้นมาก่อนถึงจะโผล่

**ลบกล่องที่ 2** เก็บกล่องแรก — กล่องแรกแปลงเดือนไทยถูก (`Forecast เดิม: ส.ค. 2026`) ส่วนกล่อง 2 ใช้ `_origWhen()` ออกมาดิบเป็น `· 8 2026`
`_origWhen()` ยังไม่ลบทิ้ง (เป็น dead code แล้ว แต่ปล่อยไว้กันของอื่นเรียก)

### 3. ถอดการ์ด "ทีม Service 1 / 2 / 3" ออกจาก Daily Board
เลขในการ์ดขัดกันเอง — Service 1: `ยังไม่จบ 67 + จบ 17 + ยกเลิก 0 = 84` แต่มุมขวาเขียน `SC 94 · SP 2 = 96` · Service 2 ก็ `54 vs 57`
บนล่างมาคนละ filter → ลบทั้งบล็อกออก (`hb-sech` + `div#hb-svc-row`)
JS ที่ render มี `if(svcEl)` guard อยู่แล้ว **ไม่ต้องแก้ JS** · CSS `.hb-svc-*` ปล่อยไว้เฉยๆ
**[V655.2] ถอดการ์ด `📌 งานค้างเดือนนี้ — Pending` (4 ใบ) ออกด้วย** ตามคำขอ
ตัวเซ็ตค่าทั้งหมดมี guard อยู่แล้ว (`_set` / `_urgSet` เช็ค `if(e)`) → **ไม่ต้องแก้ JS สักบรรทัด**
popup `#hb-modal` + `hbShowPending()` คงไว้ — Critical Down ใช้ร่วมกัน
subtitle หัวหน้าเพจแก้จาก `Daily Sales · งานค้างเดือนนี้ที่ต้องตาม (Pending)` → `Daily Sales`
**Daily Board ตอนนี้เหลือ: Daily Sales KPI + Critical Down** (บล็อก "คอขวด" ถูกลบไปแล้วตั้งแต่ C16)

### 4. ชิป Medical Device Status ใช้คำเดียวกับการ์ด Critical Down
- `🔴 Critical Down` → **ใช้ไม่ได้** · `🟡 Partial Down` → **ตรวจสอบ** · `🟣 Monitoring` → **เฝ้าระวัง** · `⚫ EOL` / `✅ Clear` คงเดิม
- ชิปแรก `Units` ผิดมาตลอด — ใช้ `rows.length` = **ยอดหลังกรอง** พอกด Critical Down เลยขึ้น `7 Units` เท่ากับ Critical Down พอดี → เปลี่ยนเป็น `rowsAll.length` + ป้าย **"เครื่องทั้งหมด"**
- แก้ 2 จุด (`kpiEl` และ `kpiPills` เป็นสตริงเดียวกันซ้ำ 2 ที่)
- **[V655.1] badge ในแถวตารางเปลี่ยนเป็นไทยด้วย** — `GB{}` และข้อความ issue ใน Weekly Report (`_wkIssues`) ใช้คำชุดเดียวกันหมดแล้ว: ชิปด้านบน = badge ในแถว = การ์ด Critical Down ใน Daily Board = Weekly Report
- dropdown `sp-p-machine` ยังเป็น `Critical Down` — **ไม่แตะ** เพราะเป็นค่าข้อมูลที่ใช้ filter ไม่ใช่ป้ายแสดงผล

### 5. `.gitignore` — ไฟล์สำรองเลิกขึ้น repo
`git add -A` ของ patch script ดูด `.bak_*` / `.broken_*` ขึ้น repo ไปด้วยทุกรอบ (สะสมแล้ว 13 ไฟล์ รวม SalesProspect 3 ชุด ~2 MB)
เพิ่ม `.gitignore` (`*.bak_*` `*.broken_*` `last.log` `node_modules/`) + สคริปต์ `git rm --cached` ถอดของเก่าออกจาก index
**ไฟล์ในเครื่องไม่ถูกลบ** — แค่เลิก track

---

## ทดสอบ
- `node --check` 5 บล็อกผ่านหมด
- jsdom/vm **76/76 ผ่าน** — ตัดโค้ดจากไฟล์ที่ patch แล้วมารันจริง
  - `renderTbl()` รันจริงพร้อมโหลด `style.html` เข้า jsdom แล้ว **วัด `getComputedStyle` ทุกช่อง** → td ทั้ง 13 = 12px · badge ทุกตัว = 10px · ไม่มี inline font-size ค่าอื่นหลงเหลือ
  - Bill/Delivery = `overflow:hidden` + ellipsis + `title` ข้อความเต็ม ไม่มีแท็ก HTML / `&nbsp;` ปนใน tooltip
  - `editStatus()` รันจริง 2 เคส — ใบที่เคยแก้ได้กล่องเดียว + แสดง `ส.ค. 2026` · ใบที่ไม่เคยแก้ไม่มีกล่อง
  - `svcEl = null` ไม่ throw · `_set`/`_urgSet` เรียกกับ element ที่ถูกลบแล้วไม่ throw · Critical Down + popup ยังอยู่
  - ชิปสถานะเครื่องครบ 2 จุด · badge ในแถว + Weekly Report เป็นไทยตรงกัน · dropdown ค่าข้อมูลไม่โดนแตะ
  - `overrides.html` ไม่มีตัวทับ `renderTbl` / `editStatus` / `periodLabel`

## วิธีลง — ส่งบรรทัดเดียวพอ
ผู้ใช้เปิด `cd C:\bjh-dashboard` + `.\start.ps1` ค้างไว้อยู่แล้ว **ไม่ต้องส่งซ้ำ**
ไฟล์ patch โหลดไว้ที่ `C:\bjh-dashboard\Deploy`

```powershell
& .\Deploy\patch_Vxxx.ps1
```

**ห้ามให้ก๊อปเนื้อไฟล์ไปวางใน console** — base64 หลายพันบรรทัดจะถูก parse+echo ทีละบรรทัด ช้ากว่ารันเป็นไฟล์หลายสิบเท่า

## วิธีลง
```powershell
cd C:\bjh-dashboard
& .\Deploy\patch_V666.ps1
```
guard: รับ **V654.x–V658.x** (ยิงทับได้ทุกกรณี รวมรันซ้ำเพื่อทำงานที่ค้างให้จบ) · **ไม่แตะ `Code.js`** แต่ยัง create-deployment เพราะ deployment พินไว้ต้องมี version ใหม่ถึงจะเห็น HTML ที่แก้
หลังลง: Ctrl+Shift+R

---

## สถานะระบบ
- **Tools → ตั้งราคาสัญญาบริการ**: ปุ่ม 📈 Analysis · ฝัง Incident 124 รุ่น (1 ส.ค. 68 – 31 ก.ค. 69) · เลือกประเภท 3 แบบ
- **MA After Sales**: Admin คีย์ → ดูร่าง → ราคามาตรฐานออกเอง / ไม่ครบส่ง Manager · ใบที่ออกแล้วล็อก · เอกสาร 2 ภาษา · [V654] คุมสิทธิ์หน้าตรวจสอบ/Config + เห็นไฟล์แนบแล้ว
- **Alert**: [V654] ปุ่ม Clear จำค่าข้ามการโหลดได้แล้ว (ผูกกับเลข SR ไม่ใช่ SN)
- **Daily Board**: [V655] เหลือ Critical Down + Pending 4 ใบ (ถอดการ์ดทีม Service ออก)

---

## ผลวิเคราะห์ราคา (Excel `service_pricing_model.xlsx`)
```
ราคา = Incident/ปี × ค่าแรงต่อ Incident ÷ (1 − margin)   ·   Incident = PM + CM
ต้นทุน/Incident 9,700 = ช่าง 2 วัน × 3,000 + ที่พัก 700 + เดินทาง 3,000
PM เฉลี่ย 1.9 (96% อยู่ 1–3) · CM เฉลี่ย 0.5 ครั้ง/เครื่อง/ปี
margin 50% → 42 รุ่น · 395 เครื่องต่ำกว่าฐาน → +8.1 ล้าน/ปี
```
**ราคาใหม่ = สูงกว่าระหว่าง [ราคาปัจจุบัน] กับ [ราคาฐาน] — ไม่ลดราคาใคร**
**ผู้บริหารเคาะ 2 ตัว** — ค่าแรงต่อ Incident · margin

---

## ค้างอยู่
**ตั้งราคากลุ่ม**
- ตั้งไปแล้ว **1 / 154 แบรนด์** — ทำ 18 กลุ่มที่คุมหลายรุ่นก่อน (350+ เครื่อง) แล้วต่อรุ่นเดี่ยวที่เครื่อง > 20

**MA After Sales**
- ⬜ ยังไม่ทดสอบเส้นทาง "ส่ง Manager" กับใบจริง — V654 แก้ไฟล์แนบ + สิทธิ์แล้ว รอบนี้เทสได้ครบเส้น
- ⬜ ยังไม่กรอกข้อความ EN ลง Config (แปลไว้แล้วในแชท + `MA_service_proposal_template.docx`)
- ⬜ ยังไม่ตรวจข้อความ default ใน Config
- หัวกระดาษใช้ `companyName` จาก Config — อยากได้ **BJCHealthcare** แก้ใน Config ได้เลย
- ยังไม่มีปุ่ม "สร้างคำขอใหม่จากใบนี้"
- ชื่อไฟล์แนบ — ต้องแก้ format `attach_urls` (กระทบใบเก่า) แยกรอบ
- **text layer ใน PDF เพี้ยน** — ก็อปได้ `ปีที่ปีที่` `เป็นป็` (หน้าจอถูก) จากการฝังฟอนต์ไทย

**อื่นๆ**
- แท็บ 4 อะไหล่: UI ตาม mockup v5 · ปุ่มรายงาน 1 หน้า A4 · Battery ชื่อซ้ำ
- Performance: sheets 4.7s + ETL 3.9s · 6 mobile menus · Renewal.html · M3 SR (blocked) · M8 mobile black screen

## คำขอถึง SmartFlow
1. ใบเสนอราคา — เพิ่ม `BRAND` / `MODEL` / `SN`
2. RAW_CONTRACT สัญญาเหมา — ระบุ SN เครื่องที่คลุม
3. จำนวน PM ที่ระบุในสัญญา

## ดองไว้ตั้งใจ — ห้ามเสนอซ้ำ
- V610 "อะไหล่รอในคลัง" + "ติดตามสัญญา" · % โอกาสเสียรายปี · ใบเสนอราคาเข้าหน้าราคาอ้างอิง
- Template 5 Level (V635) · ช่องขอส่วนลด (V640) · คอลัมน์ "รวมอะไหล่" ในตัวแก้ไขกลุ่ม (V644)
- การ์ดทีม Service 1/2/3 + การ์ด Pending 4 ใบ ใน Daily Board (V655 — ถอดออกแล้ว)
- B20 hospital→sales mapping · SP reconciliation · MDCollection.html · ServiceMix.html · AR Monitor · NOTE_LOG SmartFlow parameter

---

## เกร็ดที่ได้รอบนี้
- **[V670.3] สิทธิ์แท็บมี 4 ที่ที่ต้องตรงกัน** — `TABS` (Config UI) · `map` (ตัวซ่อนปุ่มจริง) · `_TAB_PERM_KEYS` (เซฟลงชีต) · `_TAB_PERM_DEFAULT`+`NEWKEYS` (ค่าตั้งต้น) · ขาดที่ไหนอาการต่างกันหมด และที่อันตรายสุดคือขาดใน `map` เพราะ **ไม่มี error ปุ่มแค่ไม่เคยถูกซ่อน**
- **เทสที่ derive จากไฟล์จริงดีกว่าเทสที่ hardcode รายชื่อ** — ดึง `id="sp-tab-*"` มาเทียบกับ `map`/`TABS` แทนที่จะเขียนลิสต์ไว้เอง · เพิ่มแท็บใหม่แล้วลืมคุมสิทธิ์ เทสจะจับได้เองโดยไม่ต้องแก้เทส
- **ชื่อใน Config ต้องเป็นชื่อเดียวกับที่ผู้ใช้เห็นบนหน้าจอ** — `ติดตามสั่ง SP` กับ `SP Monitor` คืออันเดียวกัน แต่แอดมินที่ไปติ๊กไม่มีทางรู้
- **[V670.1] ป้ายที่ต่อท้ายค่าอื่น จะถูกอ่านว่าเป็นส่วนหนึ่งของค่านั้น** — `HOLOGIC P9` อ่านเหมือนรุ่น · ป้ายที่อธิบาย 'ใบ' ต้องอยู่ติดกับ 'เลขใบ' ไม่ใช่ติดยี่ห้อ
- **[V670.2] ย้ายป้ายหนึ่ง มักตามด้วยคำถามว่า 'แล้วป้ายอื่นล่ะ'** — พอ `(Px)` กับ `Nชิ้น` มาอยู่ที่เดียวกันในตารางหลัก ความไม่ครบของหน้าอื่นก็เห็นทันที · **แตะป้ายในตารางไหน ให้ไล่ดูทุกตารางที่มีคอลัมน์เดียวกันในรอบเดียว**
- **regex ในเทสก็มี false positive ได้** — `/P\d/` match `P2511` ใน `QSP2511-014853` · เช็ค element หรือ tooltip ที่เจาะจง ดีกว่าจับ pattern สั้นในข้อความรวม
- **[V670] ก่อนรับปากว่าทำรายสัปดาห์ได้ ต้องถามก่อนว่า 'เกาะวันที่อะไร'** — สถานะปัจจุบันมีทุกตาราง แต่ **วันที่เปลี่ยนสถานะ** มักไม่มี · ถ้าไม่มี ตัวเลขจะเท่าเดิมทุกสัปดาห์และไม่มีใครอ่านหลังสัปดาห์ที่สอง · ให้ dump ฟิลด์ที่ชื่อเข้าข่ายวันที่ + fill rate จากข้อมูลจริงก่อนออกแบบเสมอ
- **`_jsupQuo` ซ้ำหลายแถวต่อ 1 ใบ — ทุกการนับต้อง dedupe ก่อน** เจอซ้ำอีกครั้งในรอบนี้ (`QSP2605-016186` 3 แถว) · ถ้าลืม จำนวนใบเกิน 3 เท่าและยอดเงินคูณตาม
- **snapshot เพิ่มชุดใหม่ ให้ลอกโครงเดิมทั้งชุด ไม่ใช่แค่ตารางใหม่** — `_snapSheet` / `_snapDayStr` / `_snapDayDiff` / `setNumberFormat('@')` ต้องใช้ครบ ไม่งั้นบั๊กวันที่กลายเป็น Date object (V669) จะเกิดซ้ำกับชีตใหม่
- **ฟีเจอร์ที่ต้องรอข้อมูลสะสม ต้องออกแบบ 'หน้าตาก่อนมีข้อมูล' ด้วย** — วันแรกยังไม่มีวันเทียบ ให้แสดงยอด ณ ปัจจุบันพร้อมบอกว่ากำลังเริ่มเก็บ ดีกว่าโชว์ 0 ซึ่งอ่านเหมือนไม่มีผลงาน
- **[V669.2] ทำ 'ตัวกลาง' แล้วต้องเช็คว่าเรียกครบทุกที่จริง** — V666 สร้าง `_sfLink` เป็นฟังก์ชันกลางเพื่อไม่ต้องไล่เติมทีละหน้า แต่ก็ยังตก 2 ตารางอยู่ดี · **วิธีกันคือ grep หารูปแบบเก่าให้เหลือศูนย์** (`_esc(r.qn)` ที่ไม่ผ่าน `_sfLink`) ไม่ใช่ไล่ดูทีละหน้าด้วยตา
- **เส้นใต้ 2 แบบในคอลัมน์เดียวกันทำให้ตรวจด้วยตาไม่ได้** — `_qnCellHtml` (มีโน้ต) กับ `_sfLink` (ลิงก์) ขีดเส้นใต้เหมือนกัน เลยดูเหมือนลิงก์ทำงานบางแถว
- **[V669.1] ย้ายที่อยู่ของหน้า ต้องไล่ดูทุกที่ที่ 'ชื่อหน้า' ไปโผล่** — ไม่ใช่แค่ปุ่มกับสิทธิ์ · `visibleNav` / `_autoSwitchIfHidden` / `_navExempt` ล้วนอ้างถึง `'track'` อยู่เงียบๆ · หน้าที่เปิดได้ทางเดียว (โมดัล) ห้ามเป็นปลายทางของ auto-switch เพราะจะไม่มีทางกลับ
- **สิทธิ์กับการนำทางเป็นคนละเรื่อง** — `navMap` ใช้ 2 หน้าที่พร้อมกัน (คุมการแสดงปุ่ม + สร้างรายชื่อหน้าปลายทาง) พอ Order Tracking เลิกเป็นแท็บ ต้องแยก 2 หน้าที่นี้ออกจากกัน
- **[V669] เลขที่คิดไว้ล่วงหน้าจะค้าง ถ้ามีขั้นตอน override ตามหลัง** — `monthlySpHave` ถูกคิดใน `getStats` แล้ว `_dbForceActualFromSheet` มาเขียนทับ `monthlyActualSP` ทีหลัง · **อะไรที่ขึ้นกับค่าที่ยังถูกแก้ได้ ต้องคิดตอน render ไม่ใช่ตอน getStats** (แพตเทิร์นเดียวกับ `_jsupSC`)
- **เอาเลข 3 ตัวมาวางเรียงกันบนการ์ดเดียว = สัญญาว่ามันเทียบกันได้** — เป้า/On Hand/To Find คนละสโคปกัน คนอ่านเลยได้ข้อสรุปที่ขัดกันเอง · ถ้าสโคปไม่ตรง อย่าวางไว้ด้วยกัน
- **`setValues` กับสตริงที่หน้าตาเหมือนวันที่ = Sheets แปลงให้เอง** — ต้อง `setNumberFormat('@')` ก่อนเขียน และอ่านกลับต้องผ่านตัวแปลงเสมอ · บั๊กนี้เงียบมาก เพราะ *เขียน* สำเร็จทุกวัน แค่ *อ่าน* ไม่เจอ
- **ตัวกรองที่เทียบสตริงวันที่ ถ้าฝั่งหนึ่งไม่ใช่สตริงจะไม่ error แต่คืนผลผิดเงียบๆ** — `'Mon Aug 05' <= '2026-08-05'` เป็น false ไม่มี exception ให้จับ
- **แก้เลขที่ VP เห็น ต้องถามก่อนเสมอ** — `_spForecastShown` ไหลไป PIPELINE / Forecast YE / CHALLENGE / Export ไม่ใช่แค่การ์ด · เสนอทางเลือก 'ทั้งระบบ' vs 'เฉพาะการ์ด' แล้วให้ผู้ใช้ตัดสิน
- **[V668.1] mockup ที่ประหยัดคอลัมน์ ต้องผ่านตาผู้ใช้จริงก่อนเสมอ** — ยุบ Sales เข้าเซลล์ FC ดูสะอาดในภาพ mockup แต่พอใช้จริงหัวตารางอธิบายของข้างล่างไม่ได้ · **ถ้าหัวเดียวอธิบาย 2 ค่าไม่ได้ = ต้องแยกคอลัมน์**
- **[V668] ย้ายหน้าเข้าโมดัล = ย้าย DOM ไม่ใช่ copy markup** — `appendChild` ย้าย element จริงเข้าไป ฟังก์ชัน/handler/id เดิมทำงานต่อได้หมด · ถ้า copy `innerHTML` จะได้ id ซ้ำ 2 ชุดทันที แล้ว `getElementById` ไปเจอตัวที่ซ่อนอยู่
- **ย้าย element ต้องจำ "ที่กลับ" ก่อนย้าย** — เก็บ `{parent, next}` ครั้งแรกครั้งเดียว · ถ้าเก็บทุกครั้งที่เปิด รอบที่สองจะจำที่อยู่ในโมดัลแทน แล้วปิดไปหน้าจะค้างอยู่ในโมดัลที่ซ่อน = หายถาวร
- **id เป็นสัญญากับระบบอื่น ไม่ใช่ชื่อเล่น** — `nb-track` ถูก `_applyNavPermissions()` อ้างอยู่ · ย้ายปุ่มไปคนละที่ได้ แต่เปลี่ยน id ไม่ได้ ไม่งั้นสิทธิ์หลุดเงียบ
- **ค่าที่ตั้งตายตัวใน "ตัวสร้างกลาง" คือหนี้ที่รอวันโผล่** — `_spTbl` บังคับ `<th>` เป็น left มาตั้งแต่ v276 ไม่มีใครรู้สึกเพราะคอลัมน์เยอะจนมองไม่ออก · พอตัดคอลัมน์ทิ้งครึ่งนึงใน V667 ความเบี้ยวก็เด่นขึ้นมาทันที
- **[V667] mockup ที่วาดจากภาพหน้าจอ อาจตกคอลัมน์ที่ภาพนั้นไม่ได้เปิดอยู่** — mockup SP บอกให้ตัด `ประเภทสัญญา / ช่วงสัญญา / NEW-RENEW` ซึ่ง**ของจริงไม่มีอยู่แล้ว** และไม่มี `สถานะเครื่อง` ซึ่งของจริงมี · **ก่อนลงมือต้องเทียบ `cols` ในโค้ดจริงกับ mockup ทีละช่อง** ไม่ใช่ทำตาม mockup ตรงๆ
- **ตารางที่ผ่านตัวสร้างกลาง แก้ที่เดียวได้ทุกอย่าง** — `_spTbl` ทำให้เปลี่ยนคอลัมน์แล้วการเรียงตามมาเอง · ถ้าเป็นตารางที่เขียน `<thead>` เองแต่ละหน้า จะต้องไล่แก้ตัวเรียงอีกรอบและลืมบางหน้าแน่นอน
- **⚠️ ผู้ใช้รัน patch โดย "ก๊อปเนื้อไฟล์ไปวางใน PowerShell" มาตลอด** — เห็นร่องรอย `>>` และ `PS C:\bjh-dashboard>` ทุกบรรทัดใน log ตั้งแต่ V657 แล้วแต่ผมไม่ได้สังเกต · base64 หลายพันบรรทัดถูก parse+echo ทีละบรรทัด ช้ากว่ารันเป็นไฟล์หลายสิบเท่า · **"ส่งเฉพาะไฟล์ที่แก้" ที่ทำไปจึงแทบไม่ช่วยเลย เพราะคอขวดอยู่ที่วิธีรัน ไม่ใช่ขนาดไฟล์**
- **วิธีที่ถูก:** โหลดไฟล์ไว้ `C:\bjh-dashboard\Deploy` แล้วรัน `& .\Deploy\patch_Vxxx.ps1` — ส่งคำสั่งให้ผู้ใช้พร้อมทุกครั้ง
- **เจอปัญหาเรื่องความเร็ว ให้ถามว่า "รันยังไง" ก่อนไปปรับขนาดไฟล์** — ผมปรับ patch ให้เล็กลง 3 เท่าแล้วผู้ใช้ยังไม่รู้สึกต่าง เพราะแก้ผิดจุดตั้งแต่แรก
- **ผู้ใช้ส่งไฟล์ `bjh_tx/master.json.gz` (base64+gzip) มาให้ได้** — คลายแล้วอ่านข้อมูลจริงทั้งชุดได้เลย · **ดีกว่าให้ผู้ใช้รัน console ทีละคำถามมาก** (วันนี้ขอไป 5-6 รอบกว่าจะได้ครบ) · รอบหน้าขอไฟล์ตั้งแต่แรก
- **dataset ที่ต้องมีพารามิเตอร์ ห้ามรวมกับชุดที่ไม่ต้องมี** — `RAW_SERVICE_JOB` ขาด `team` แล้ว API ตอบ 400 **ทั้ง request** ไม่ใช่แค่ตัวมันเอง · ถ้าเผลอต่อท้ายรายการเดิม ข้อมูลทั้งระบบหายทันทีโดยไม่มีอะไรเตือน
- **เขียนโค้ดเรียกฟังก์ชันที่คิดว่ามี** — ผมเรียก `_bjhDriveFolder_()` ซึ่งไม่มีอยู่จริง เทสที่เช็ค "ฟังก์ชันช่วยมีอยู่ไหม" จับได้ก่อนส่ง · **เทสควรเช็คว่า dependency มีจริง ไม่ใช่แค่ว่า logic ถูก**
- **ข้อจำกัดที่เขียนไว้ในโค้ดมีวันหมดอายุ** — V619 เขียนว่า "RAW_SC_BILL ไม่มีฟิลด์ผูกเครื่องต่องวด" แล้วออกแบบทั้งระบบรอบข้อจำกัดนั้น · พอ SF เพิ่มฟิลด์ให้ ข้อจำกัดหายไปแต่โค้ดยังอ้อมอยู่ · **เจอคอมเมนต์ที่ขึ้นต้นว่า "ทำไม่ได้เพราะ..." ให้เช็คก่อนว่ายังจริงอยู่ไหม**
- **ถามให้ตรงคำถามสำคัญกว่าถามเยอะ** — ผมถาม "ยี่ห้อไหนควรโชว์เมื่อมีหลายยี่ห้อ" ไป 2 รอบ ทั้งที่คำตอบคือ **ไม่ต้องเลือกเลย** เพราะแต่ละแถวมียี่ห้อของตัวเองอยู่แล้ว · การตั้งคำถามผิดทำให้ได้คำตอบที่ตอบไม่ตรงปัญหา
- **ขอค่าจริงก่อนออกแบบ ประหยัดกว่าออกแบบแล้วแก้** — รอบนี้ขอ console 4 ครั้งก่อนเขียนโค้ดสักบรรทัด และทุกครั้งได้ข้อมูลที่ล้มสมมติฐานเดิม (`CON_ID` ใช้ไม่ได้ · `CONTRACT_NO` เป็น `-` · contract มีแค่ 1 แถว) · ถ้าเขียนตามที่คิดไว้ตั้งแต่แรกคงต้องแก้ 3 รอบ
- **⚠️ ผมเดา `r.st` จากโค้ดแล้วผิด 2 รอบติด (V662, V663)** — รอบแรกฟันธงว่าเป็น `SP Ordered (Pending)` เพราะเห็น `_spIsProspect` ใน renderTbl · ค่าจริงคือ `'Prospect'` ซึ่งรู้ได้ใน 10 วินาทีถ้าขอ console มาตั้งแต่แรก · **บทเรียนนี้จดไว้ตั้งแต่ V656 แล้วยังทำซ้ำ — เรื่องข้อมูลต้องขอค่าจริงเสมอ ห้ามอนุมานจากโค้ด**
- **ลิสต์ที่รวมครบมักมีอยู่แล้วที่ไหนสักแห่ง** — `overrides.html:1615` มีเงื่อนไข Prospect ครบทั้ง 3 สถานะมานานแล้ว ผมลอกมาแค่ตัวเดียว · ก่อนเขียนเงื่อนไขใหม่ ให้ `grep` หาว่ามีใครเขียนไว้ครบแล้วหรือยัง
- **นิยามซ้ำ 2 อันในไฟล์เดียว = บั๊กที่หาไม่เจอด้วยการอ่าน** — `_stBucket` ซ้ำเหมือน V547 ที่กล่องซ้ำ · ตัวหลังชนะตอน runtime แต่เทสที่ slice ตัวแรกจะได้ผลตรงข้าม · **เทสควรเช็คจำนวนนิยามด้วย ไม่ใช่แค่เนื้อใน**
- **"แสดงผลอย่างหนึ่ง กรองอีกอย่างหนึ่ง" คือบั๊กที่ผู้ใช้เจอก่อนเราเสมอ** — `_spIsProspect` เขียนไว้ใน `renderTbl` ที่เดียวตั้งแต่ v393 · พอมีหน้าที่สองที่ต้องกรองสถานะเดียวกัน ก็ลอกไม่ครบ · **ถ้า UI แปลงค่าก่อนแสดง ตัวแปลงนั้นต้องเป็นฟังก์ชันกลาง ไม่ใช่ inline ในตัว render**
- **"นับซ้ำ" เป็นบั๊กประจำของโปรเจกต์นี้ เจอรอบที่ 3 แล้ว** — Excel SP Forecast (V660.6) · การ์ด PIPELINE (V661) · และ `_spDupSkip` ที่เคยแก้ไว้ก่อนหน้า · **รากเดียวกันหมด: ค่าที่ตั้งมือเป็น "ยอดรวมทั้งก้อน" แต่เอาไปบวกกับของจริงที่เป็น "รายใบ"** · เจอ config ที่เป็นยอดรวมเมื่อไร ให้ถามทันทีว่ามันทับกับใบจริงตรงไหน
- **มีค่าที่ระบบคำนวณไว้แล้ว อย่าไปนับใหม่** — ผมเขียนลูปนับ SP เองถึง 3 รอบ (V660.6 / V661.0 / V661.1) ผิดทั้งช่วงเดือนและนิยามสถานะ · พอเปลี่ยนไปใช้ `monthlyActualSP + monthlyBacklogSP` ที่การ์ด CONFIRMED ใช้อยู่แล้ว ปัญหาหายหมดในครั้งเดียว · **ถ้าตัวเลขต้องตรงกับที่จออื่นโชว์ ให้ใช้ตัวแปรเดียวกัน ไม่ใช่คำนวณให้ได้ผลเท่ากัน**
- **ค่าที่ต้อง "ถูกทุกที่" ต้องหักที่ต้นทาง ไม่ใช่ที่จอ** — V661.0 หัก SP Forecast ใน `dbRender` ผลคือการ์ดถูกแต่แถว Monthly Forecast ยังผิด แถมยังใช้ตัวกรองเดือนคนละชุดจนกวาดทั้งปีมาหัก · พอย้ายไปหักตอนสร้าง `monthlyFCSP[]` ใน `getStats` ทุกจอถูกพร้อมกันโดยไม่ต้องไล่แก้ · **ถ้าต้องเขียนตรรกะเดียวกัน 2 ที่ แปลว่าวางผิดที่แล้ว**
- **ลำดับการเรียกใน render function สำคัญกว่าที่คิด** — `otTabs()` อ่านตัวแปรที่ `otRows()` เป็นคนตั้ง พอเรียกก่อนก็ได้ 0 เงียบๆ ไม่ error · ฟังก์ชันที่ต้องพึ่งค่าจากฟังก์ชันอื่น ควรรับเป็น argument ไม่ใช่อ่านจาก global
- **ตรรกะอยู่ฝั่งไหน เป็นตัวกำหนดว่า feature ทำได้แบบไหน** — อยากได้ snapshot อัตโนมัติตี 0 แต่ `Code.js` จัดสถานะไม่ได้เลย · ทางเลือกคือ "ยกตรรกะไป server แล้วเสี่ยงเลข 2 ชุดไม่ตรงกัน" กับ "ถ่ายตอนเปิดหน้าแล้วยอมมีวันหาย" — เลือกอย่างหลังเพราะ **เลขที่ตรงกันสำคัญกว่าเก็บครบทุกวัน**
- **ลำดับการเขียนคือกลไกกู้คืน** — เขียน change ก่อน state เสมอ · พังกลางทางแล้วยังตามเก็บวันถัดไปได้ ถ้าสลับลำดับ ส่วนต่างวันนั้นหายถาวรโดยไม่มีใครรู้
- **หน่วยของ config แต่ละตัวไม่เหมือนกัน ห้ามเหมา** — `BUD` เก็บพันบาท (`* 1000` ตอนใช้) แต่ `FCSP_FIXED` เก็บบาท · ผมเห็น budget เป็นพันแล้วเหมาว่า FCSP เป็นพันด้วย ออกมาเป็น **5 พันล้าน** · วิธีเช็คที่ถูกคือดูว่า**โค้ดเดิมที่ใช้ค่านั้นคูณอะไรบ้าง** (`getStats:936` อ่านตรงๆ ไม่คูณ = บาท) ไม่ใช่เดาจากตัวข้างเคียง
- **"หักแล้ว" ต้องถามว่าหักกับอะไร** — ผมถามว่านับ "เปิดบิลแล้ว" ยังไง แล้วได้คำตอบว่า `st=Billed` · แต่คำถามที่ถูกคือ **"forecast นี้ควรเป็นส่วนที่ยังไม่มีใบ ใช่ไหม"** ซึ่งคำตอบคือหักใบ QSP ทุกสถานะ · ถามผิดคำถามก็ได้คำตอบที่ถูกแต่แก้ปัญหาไม่ตรง — เห็นตัวเลขจริงถึงรู้
- **เลขที่ผิดหน่วยดูออกยากในโค้ด แต่ดูออกทันทีในผลลัพธ์** — เทสรอบแรกผ่านหมดเพราะผมเขียนเทสด้วยสมมติฐานเดียวกับที่เขียนโค้ด (ป้อน 5000 คาดหวัง 5 ล้าน) · **เทสยืนยันได้แค่ว่าโค้ดทำตามที่คิด ไม่ได้ยืนยันว่าที่คิดถูก** — ต้องเอาผลจริงมาดู
- **"บันทึกแล้วไม่อัปเดต" มี 2 ชั้น ต้องแก้ให้ครบทั้งคู่** — ชั้นแรกคือไม่ได้เรียก render (แก้ที่ V660.0) ชั้นสองคือ**ส่งข้อมูลผิดรูปเข้า applyConfig** จน apply ไม่ติด · รอบแรกเทสผ่านหมดเพราะเทสแค่ว่า "เรียก callback ไหม" ไม่ได้เทสว่า "ข้อมูลที่ส่งเข้าไปหน้าตาถูกไหม" — ผู้ใช้จับได้จากของจริง
- **payload ที่ backend "เติมให้เอง" คือหลุมพราง** — `saveConfigSection('budget', flat)` ทำงานได้เพราะ server เติมปีให้ แต่พอเอา payload เดียวกันไปใช้ฝั่ง client ที่ไม่มีตัวเติม มันพังเงียบ · รูปข้อมูลที่ save กับที่ apply ไม่จำเป็นต้องเหมือนกัน ต้องเช็คทั้งสองฝั่ง
- **คอมเมนต์บอกว่ามี ไม่ได้แปลว่ามี** — หัวฟังก์ชันเขียน "Excel 2 sheets: Ordered + Pipeline" แต่ `pipelineRecs` ไม่เคยถูก append เข้า workbook เลย · เจอเป็นครั้งที่สองในโปรเจกต์นี้ (ครั้งแรกคือ V601 cd_cleared)
- **ปิดฟีเจอร์ = ตัดทางเข้า ไม่ใช่ลบโค้ด** — เก็บฟังก์ชันไว้ทั้งหมด แล้วคอมเมนต์บรรทัดเดิมไว้ข้างๆ ทำให้เปิดกลับได้ใน 1 บรรทัด และไม่เสี่ยงพังจากที่อื่นที่อาจอ้างถึง · เหมาะกับกรณี "ปิดไว้ดูก่อนสักพัก"
- **ก่อนตัดทางเข้า ต้องเช็คว่า "ทางออก" ไม่ได้อยู่ในนั้นด้วย** — ถ้าปุ่มคืนค่าเดิมอยู่ในโมดัลแก้ Status การปิดโมดัลจะทำให้ undo ใบเก่าไม่ได้เลยตลอดกาล · รอบนี้รอดเพราะปุ่มอยู่คนละที่ แต่เป็นเรื่องที่ต้อง grep ก่อนเสมอ
- **CSS specificity: กฎใหม่ต้องอยู่ "หลัง" ไม่ใช่ "ใกล้ของเดิม"** — เอากฎ `.ip-*.on` ใหม่ไปวางตรงตำแหน่งที่อ่านง่าย (ข้าง `.ip-pill.on`) ผลคือโดนกฎเดิมที่อยู่ล่างกว่าทับทั้งชุด specificity เท่ากัน ตัวหลังชนะ · **เทสที่วัด `getComputedStyle` จริงจับได้ ถ้าเช็คด้วย regex ว่า "มีบรรทัดนี้ในไฟล์ไหม" จะผ่านฉลุยแล้วส่งของเสียไปให้ผู้ใช้**
- **ก่อนตั้งชื่อค่าใหม่ ต้อง grep ว่าชื่อนั้นมีอยู่แล้วหรือยัง** — `SC Pending` มีอยู่ในโค้ดแล้วที่บรรทัด 93 (คนละความหมาย) เกือบกวาดเข้ากลุ่ม Backlog ผิด · รอดเพราะทุกจุดมี `st==='Backlog'` คุมอยู่พอดี ไม่ใช่เพราะออกแบบมาดี
- **ค่าที่ระบบไม่รู้จัก ต้อง default ไปทางที่เสียหายน้อยกว่า** — สถานะงานที่ไม่มีในตารางแมป ให้เป็น `pending` ไม่ใช่ `done` · เดาว่าจบแล้วนับเงินเข้า Billing ผิด เจ็บกว่าเดาว่ายังไม่จบ
- **ฟิลด์เดียวกันคนละระบบใช้คนละคำ** — `sn` เป็น `Completed` ใน SC แต่เป็น `PO Completed` ใน SP · เทียบตรงๆ ตัวเดียวคือทำฝั่งหนึ่งพังเงียบทั้งฝั่ง
- **ตัวเตือนที่ถูกต้องทางตรรกะ อาจผิดทางบริบท** — ปุ่ม ⚙ สีส้มกันคนเข้าใจผิดได้จริง แต่หน้า Summary เอาไว้ present ให้ผู้บริหารดู ป้ายเตือนสีส้มกลางจอเลยกลายเป็นปัญหาแทน · ย้ายไป tooltip ได้ประโยชน์เดิมโดยไม่รบกวน — **ถามก่อนว่าหน้าไหนใช้ทำอะไร ก่อนตัดสินใจว่าอะไรควรเด่น**
- **คำที่ลงท้ายว่า Completed ไม่ได้แปลว่าจบงาน** — ผมเดาเองว่า `PO Completed` = จบ (เพราะมีคำว่า Completed) ผู้ใช้ทักว่า **PO เสร็จ = สั่งซื้อจบ แต่อะไหล่ยังไม่ถึงลูกค้า ยังบิลไม่ได้** · ชื่อสถานะบอกว่า "ขั้นตอนไหนจบ" ไม่ได้บอกว่า "งานจบ" — ต้องถามคนใช้งานเสมอ ห้าม match ด้วย regex `/Completed/`
- **"ทับแถวเดิมเพื่อความสะอาด" = ทำลายหลักฐาน** — `sales_manual_edit` ออกแบบให้ 1 rowKey = 1 แถว ดูสะอาดดี แต่พอถามว่า "ใครแก้ใบนี้ตอนไหนบ้าง" ตอบไม่ได้เลย · ตารางที่มีคำว่า Editor/EditedAt อยู่ในหัวคอลัมน์ = ตาราง log ต้อง append เท่านั้น
- **ลำดับ dedupe กับ filter สลับกันไม่ได้** — กรอง Cancelled ก่อนแล้วค่อย dedupe จะทำให้แถวเก่าที่ยัง active โผล่มาแทนใบที่เพิ่งยกเลิก = override ผีที่ไล่หาไม่เจอ · ต้อง dedupe เอาล่าสุดก่อนเสมอ แล้วค่อยดูสถานะ
- **ค่าคงที่ดีกว่า CUR_YR เมื่อความหมายคือ "จุดเริ่มต้นของข้อมูลที่เชื่อได้"** — ใช้ `y >= CUR_YR` จะดูฉลาดกว่า แต่พอขึ้นปีใหม่มันจะกินปีเก่าทิ้งเงียบๆ
- **⚠️ บทเรียนแพงสุดของวัน: อ่านโค้ดครึ่งเดียวแล้วสรุป ผิด 3 รอบติด**
  1. บอกว่า Customer Accept โดนดันไป ธ.ค. → ถูก แต่เหตุผลผิด (ไปเช็ค `st` ทั้งที่ bucket CA แยกด้วย `qs`)
  2. แก้เป็น "CA ไม่โดนดัน" → ผิด
  3. บอกว่า "SP ไม่มี carry-forward เหมือน SC" → **ผิด** เพราะดูแค่ในบล็อก SP ไม่ได้อ่านถึงท้ายฟังก์ชันที่มี `SKIP_OD` carry แบบรวมอยู่แล้ว
  → **กฎใหม่: ก่อนสรุปว่า "ไม่มี logic X" ต้องอ่านฟังก์ชันจนจบก่อน** และถ้าเป็นเรื่องข้อมูล ให้ขอภาพจริงก่อนเสมอ อย่าอนุมานจากโค้ด
- **ยอดเป็น 0 ให้เช็คตัวกรองก่อนไล่โค้ด** — เสียไป 4-5 รอบกับการ์ด SP ORDERED = ฿0 สุดท้ายคือลืมติ๊ก status ใน ⚙ · `_sfOn()` บังคับเป็น 0 โดยไม่มีสัญญาณอะไรบนจอเลย (V656 ใส่ตัวเตือนแล้ว)
- **UI ที่กรองอยู่แล้วไม่บอก = กับดักถาวร** — ไม่ใช่แค่เสียเวลา แต่ทำให้ตัดสินใจผิดได้ถ้าเชื่อว่ายอดนั้นคือทั้งหมด
- **CSS ที่ "ไม่ได้ตั้ง" คือค่าที่ตั้งไว้เหมือนกัน** — `.dtbl td` ไม่มี `font-size` ทำให้รับ 14px จาก `body` ส่วน badge ตั้ง inline ไว้ 9–12.5px · ปัญหาไม่ได้อยู่ที่ใครตั้งผิด แต่อยู่ที่ **ไม่มีใครตั้งฐาน** · เจอ "ฟอนต์ไม่เท่ากัน" ให้ไล่หาว่าฐานอยู่ตรงไหนก่อน อย่าไล่ไล่แก้ทีละ inline
- **`overflow:visible` ในตาราง = เลื่อนปัญหาไปทับคอลัมน์ข้างๆ ไม่ใช่แก้** — V621 ใส่ไว้กันวันที่โดนตัด แต่รากคือฟอนต์ใหญ่เกินคอลัมน์ · พอลดเหลือ 12px ปัญหาเดิมหายไปเอง แล้วคืน ellipsis ได้ตามปกติ
- **โค้ดที่แปะซ้ำ 2 รอบ ตรวจไม่เจอด้วยการอ่าน** — บล็อก `r._orig_st` ซ้ำอยู่ห่างกัน 20 บรรทัดในสตริงเดียว คอมเมนต์ `[V547]` เหมือนกันเป๊ะ · เจอได้เพราะผู้ใช้ส่งภาพมา ไม่ใช่เพราะอ่านโค้ด · **ฟีเจอร์ที่โผล่เฉพาะบางเงื่อนไข (ต้องเคยแก้ก่อน) คือจุดบอดถาวร**
- **ตัวเลขที่ "ผลรวมไม่ตรงกันในการ์ดเดียว" = คนละ query ไม่ใช่คำนวณผิด** — `67+17+0=84` ถูกในตัวเอง แต่ `SC 94 · SP 2` มาจากอีก filter · เจอแบบนี้อย่าไปไล่แก้สูตร ให้ดูว่านับมาจากชุดไหน
- **`rows` vs `rowsAll` พลาดง่ายมาก** — ชิป Units ใช้ `rows.length` (หลังกรอง) เลยเท่ากับชิปที่กดอยู่เสมอ ดูเหมือนถูกจนกว่าจะกดตัวอื่น
- **แก้คำที่เดียวไม่พอ** — เปลี่ยนชิปเป็นไทยแล้วเหลือ badge ในแถวกับ Weekly Report เป็นอังกฤษ กลายเป็นระบบเดียวใช้ 2 ภาษาปนกัน · ป้ายเดียวกันมักถูกเขียนซ้ำหลายที่ ให้ `grep` คำเดิมให้ครบก่อนแก้ · แต่ต้องแยกให้ออกว่าอันไหนเป็น **ป้ายแสดงผล** อันไหนเป็น **ค่าข้อมูลที่ใช้เทียบ** (dropdown filter ห้ามแตะ)
- **ลบ UI ออกไม่ต้องแตะ JS ถ้า setter เขียน guard ไว้ตั้งแต่แรก** — ทั้ง `_set`/`_urgSet`/`if(svcEl)` มี `if(e)` ครบ เลยถอดบล็อกทิ้งได้เลย · guard ที่ดูเกินความจำเป็นตอนเขียน คือสิ่งที่ทำให้รื้อทีหลังได้ถูก
- **jsdom โหลด CSS จริงเข้าไปแล้ววัด `getComputedStyle` ได้** — เทสฟอนต์ไม่ต้องเดาจาก regex อีก · วิธีนี้จับได้ทั้ง inline ที่ลืมลบ และ cascade ที่ไม่คาดคิด
