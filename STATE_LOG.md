
## V594.0 - 25 ก.ค. 2569
เคส: popup สลับแท็บความสูงเด้ง · การ์ดอะไหล่ vs คอลัมน์คนละเกณฑ์ · ศัพท์ใหม่ · Predict รื้อใหม่
- C1 sp-ibd-body เพิ่ม min-height:60vh — flex:1 = flex-basis:0% มีศักดิ์เหนือ height ใน flex column -> height:60vh ถูกเมิน
- C2 แก้คอมเมนต์ V592 ที่ encoding พัง
- C3 _ibWarrOf() เกณฑ์เดียว การ์ดสรุป = คอลัมน์ (เดิมการ์ดเช็คแค่ qn ว่าง -> เลข "เบิกซื้อ" สูงเกินจริงทั้งระบบ)
- C4 ศัพท์ใหม่ 5 จุด: เคลมสัญญาประกัน / เสนอขายอะไหล่ · หัวคอลัมน์+CSV = เคลม/เสนอขาย
- C5 หมวด 4 เขียนใหม่: ฐาน G.Model เท่านั้น (ตัด fuzzy ตามที่ Eak สั่ง) · คอลัมน์อายุตอนเปลี่ยน p25-median
     + แถบช่วงอายุ 0-15 ปี (เส้นแดง = อายุเครื่องนี้) · เลิกตัด 20% เหลือ Top5 + ปุ่มดูทั้งหมด
     + ฐาน 0 = บอกตรงๆ ว่าทำนายไม่ได้ ให้ไปจัด G.Model (เดิมขึ้น "ไม่พบอะไหล่เสี่ยง" อ่านแล้วเข้าใจผิดว่าปลอดภัย)
- C6 โหมด A (เครื่องอยู่ในสัญญารวมอะไหล่ active) = ตัวเลขเงินคือต้นทุน BJH ไม่ใช่ยอดขาย
     sales ซ่อนตัวเลขเงินทั้งหมดในโหมด A · manager/admin เห็นเต็ม · ป้าย "ใช้ภายใน" ทุก role
     เหตุผล: ลูกค้าเห็นแล้วถามว่า "รู้ว่าเสี่ยงทำไมไม่เปลี่ยนให้ในสัญญา"
- C7 ปุ่ม "วิธีคิด" อธิบายผู้บริหาร 5 ข้อ + ข้อจำกัด

DATA (probe จากเครื่องจริง 25 ก.ค. 69):
  _spInstallAll 17,487 rows · วันติดตั้งเติม 95% · อนาคต 7 · SN 81%
  join parts<->install ด้วย SN = 77% (2,620/3,401) ครอบคลุม 1,196 SN · เบิกก่อนติดตั้ง 15
  อายุตอนเปลี่ยน n=2,605 -> p10 1.6 · p25 2.9 · median 5.6 · p75 8.9 · p90 12.4 ปี
  -> เริ่มเปลี่ยนปีที่ ~3 พีคปีที่ ~6

บทเรียน:
- curl raw.githubusercontent ต้องต่อ ?t=<timestamp> เสมอ ไม่งั้นได้ 404/ของเก่าจาก CDN cache
  (เคยหลงว่า STATE_V592.md ไม่มีใน repo ทั้งที่มีอยู่แล้ว)
- anchor ที่เป็นคอมเมนต์ mojibake ห้ามพิมพ์เอง ต้องอ่านบรรทัดจริงจากไฟล์ (Lao codepoint เดาไม่ได้)
- _ibAllRows ผ่านตัวกรอง UI แล้ว ห้ามใช้เป็นฐานสถิติ ให้ใช้ _spInstallAll
- Claude clone repo อ่านโค้ดเองได้ (github.com/raw.githubusercontent อยู่ใน allowlist) ไม่ต้องอัปไฟล์

ทดสอบ: node --check ผ่าน · logic test 19/19 ผ่าน (เกณฑ์เคลม, การ์ด=ตาราง, p25/median, โหมด A, กันหลุดขอบ)
ค้าง: perf (sheets 4.7s + etl 3.9s) · 6 mobile menus · Renewal.html · M3 SR (blocked) · M8 mobile black screen

## V595.0 - 25 ก.ค. 2569
- C8 popup รายละเอียดเครื่องใหญ่ขึ้น (default เดิมเล็กไป)
    กล่องนอก max-width 1200 -> 1500px · width 96% -> 97% · max-height 90vh -> 92vh
    body 60vh -> 74vh (คงรูปแบบ height+min-height+max-height เท่ากันทั้ง 3 ค่า = ล็อกนิ่ง)
    headroom: header+tabs ~94px ; 74vh+94px < 92vh ทุกความสูงจอตั้งแต่ 700px ขึ้นไป (เผื่อ tab ขึ้น 2 แถว)
- ยืนยัน V594 ขึ้น repo จริงแล้ว (_BUILD_VER=V594.0 · min-height:60vh · _ibWarrOf x4 · STATE_LOG.md 200)
  หมวด 4 ใหม่ทำงานถูก: ฐาน Proactive US 10 รุ่น 72 เครื่อง 92 งานเบิก · เครื่องอายุ 6.0 ปี
  -> ถึงคิว 21 / ใกล้ถึง 6 / ยังไม่ถึง 28 · ป้ายตรงกับ p25-median ทุกแถวที่ตรวจ

## กติกาใหม่ - 25 ก.ค. 2569 (ไม่ใช่ code change ไม่ bump version)
รวมเคสก่อนยิง อย่างน้อย 3 เรื่องต่อ 1 patch — ห้ามยิงทีละเรื่อง
  เหตุผล: 1 patch = clasp push + create-deployment + git push ยิงถี่เปลืองและเสี่ยง
  V595 (modal ใหญ่) + V596 (ตัดทศนิยม) ควรเป็นก้อนเดียว = ตัวอย่างที่ไม่ควรทำซ้ำ
เคสที่ไม่ต้อง confirm (สี/ข้อความ/label/spacing/ไฟล์เดียวไม่แตะ logic) -> แก้เลย ไม่ต้องถามซ้ำ
  confirm เฉพาะงานใหญ่ตามนิยามเดิมใน CLAUDE.md (2 ไฟล์ขึ้นไป · Code.gs · classify/filter · tab/status ใหม่ · UI ใหญ่ต้อง mockup)
ระหว่างสะสมเคส: สืบต้นเหตุ + เตรียม anchor ไว้ก่อน แต่ยังไม่เขียนไฟล์
CLAUDE.md แก้ 2 จุด: ชี้ไป STATE_LOG.md แทน STATE_V559.md (ค้างเก่า) + เตือน curl ต้องต่อ ?t=<timestamp>

## V596.0 - 25 ก.ค. 2569  (รวม 3 เรื่องตามกติกาใหม่)
- C9  อายุตอนเปลี่ยน ตัดทศนิยม "3.1-8.2 ปี" -> "3-8 ปี" ปัดแล้วเท่ากันแสดงเลขเดียว
      ป้ายอายุเครื่อง 6.0 -> 6 ปี  ตรรกะเทียบ p25/median ยังใช้ค่าจริงไม่ปัด
- C10 ยกเลิกแยกสิทธิ์ sales/manager ในหมวด 4 (_hideMoney=false) ทุก role เห็นเท่ากัน
- C11 เปลี่ยนคำให้ลูกค้ามองเห็นได้ แทนการซ่อนตัวเลข (Eak เสนอ - ตรงประเด็นกว่า)
      หัวหมวด -> แผนดูแลอะไหล่ตามอายุการใช้งาน
      การ์ด: ถึงรอบพิจารณาเปลี่ยน | ใกล้ถึงรอบ | ยังอยู่ในช่วงปกติ
      ป้ายตาราง + กล่องวิธีคิด: ถึงรอบแล้ว / ใกล้ถึงรอบ / ยังไม่ถึงรอบ
      หัวคอลัมน์: ราคากลาง -> มูลค่าโดยประมาณ
      ป้าย "ใช้ภายใน" -> "อยู่ในสัญญารวมอะไหล่" สีเขียว
      banner โหมด A -> "รายการที่ถึงรอบจะถูกวางแผนดูแลภายใต้สัญญา" (เขียว)

BUG ที่เจอรอบนี้ (2 ตัว - ทั้งคู่ ABORT ปลอดภัย ไม่แตะไฟล์):
1) PowerShell 5.1 ConvertFrom-Json ทำ surrogate pair (emoji นอก BMP) เพี้ยน -> anchor count=0
   แก้: base64 หุ้ม find/replace/note/guard ทีละ item แล้ว UTF8.GetString ฝั่ง PS
2) ตัวสร้างคำสั่งใส่ backslash เกิน -> "_BUILD_VER='V595\\.0'" (PS double-quote ไม่ escape ด้วย backslash)
   -> regex ไม่ match -> ABORT: base is not V595.0  แก้เป็น backslash เดียว
